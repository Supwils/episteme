// Shared content types, re-exported from the physics content schema so that
// content modules can import them via the stable "@/lib/content" path.
export type {
  DataCard,
  NarrativeSection,
  SourceRef,
  SceneMarker,
  TierContent,
} from "@/src-physics/lib/content";
