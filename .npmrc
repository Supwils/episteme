# Flat (hoisted) node_modules — no symlinked package directories.
# pnpm's default symlinked layout leaves symlinked dirs in Next.js Output File
# Tracing, which Vercel rejects when packaging Serverless Functions
# ("invalid deployment package ... files in symlinked directories").
node-linker=hoisted
