---
title: DNS：互联网的命名系统
title_en: "DNS: The Internet's Naming System"
status: published
updated: 2026-06-13
category: 系统与架构
order: 32
field: 计算机科学 · 计算机网络
tags:
  - DNS
  - 域名
  - 分布式系统
  - 网络安全
related:
  - networking-protocols
  - distributed-systems
  - computer-security-principles
---

# DNS：互联网的命名系统

1983 年之前，互联网上的所有计算机名字都记录在一个文件里：`HOSTS.TXT`，由斯坦福研究院手工维护，每隔几周更新一次，所有人手动下载同步。当时互联网上大约有几百台主机，这勉强管用。

到 1983 年，主机数量已经增长到让这种方式无法维持。Paul Mockapetris 设计了一个分布式、去中心化的命名系统，解决了这个问题：**DNS（Domain Name System，域名系统）**。今天，它每天处理数万亿次查询，是互联网最关键的基础设施之一。

## 破除误解：DNS 不只是"把名字转成地址"

DNS 被简化描述为"把 google.com 翻译成 IP 地址"。这是真的，但只是它功能的一部分。DNS 同时存储：
- 邮件服务器地址（MX 记录）
- 文本记录（TXT 记录，用于域名验证和 SPF/DKIM 防伪）
- 别名（CNAME 记录）
- 服务发现（SRV 记录）

DNS 是一个**分布式数据库**，记录类型决定查询语义。把它看作互联网的"通用目录服务"更准确。

## 核心：层级结构与分布式权威

DNS 的名字空间是一棵树：

```
               . (根，Root)
          /    |    \    \
        com   org   net   cn   ...
       /   \
   google  example
    /
  www
```

`www.google.com.` 是完整域名（末尾有个点代表根）。从右向左读：根 → com → google → www。

这棵树分布在全球数百万台 DNS 服务器上，每台服务器只负责自己那一部分（称为**区域 Zone**）：
- **根服务器（Root Servers）**：知道所有顶级域（.com, .org, .cn…）的名字服务器地址。全球共有 13 个根服务器**逻辑地址**（A~M），通过 Anycast 技术实际部署了超过 1500 个物理实例。
- **顶级域服务器（TLD Servers）**：负责 .com、.org 等顶级域内的记录。.com 由 VeriSign 运营。
- **权威服务器（Authoritative Servers）**：由域名拥有者（如 Google）自己运营，存储该域名的实际记录。

## DNS 解析过程：递归与迭代

当你访问 `www.example.com`，发生了什么？

**递归查询**（最常见）：

```
你的设备 --> 本地解析器（通常是 ISP 或 8.8.8.8）
                --> 根服务器（"我不知道，但 .com 服务器在这里"）
                --> .com TLD 服务器（"我不知道，但 example.com 权威服务器在这里"）
                --> example.com 权威服务器（"www.example.com 的 A 记录是 93.184.216.34"）
本地解析器 --> 你的设备（返回 IP）
```

本地解析器代你完成所有跳转，你只等最终结果。每一层都有**TTL（Time-to-Live）**缓存：一旦查过，在 TTL 时间内直接返回缓存，不再查询。热门域名的 TTL 通常几分钟到几小时，大幅减少全球 DNS 查询量。

## 关键记录类型

| 记录类型 | 含义 | 示例 |
|---------|------|------|
| A | IPv4 地址 | `www.example.com -> 93.184.216.34` |
| AAAA | IPv6 地址 | `www.example.com -> 2606:2800:220:1:...` |
| CNAME | 别名（规范名） | `blog.example.com -> example.com` |
| MX | 邮件服务器 | `example.com -> mail.example.com (优先级 10)` |
| TXT | 任意文本 | SPF、DKIM、域名所有权验证 |
| NS | 该区域的权威服务器 | `example.com -> ns1.example.com` |
| PTR | 反向 DNS（IP → 域名） | `34.216.184.93.in-addr.arpa -> www.example.com` |
| SOA | 区域起始记录（权威信息） | 包含区域主服务器、管理员邮箱、序列号 |

## DNS 的安全问题

DNS 在 1983 年设计时几乎没有考虑安全性——数据以明文 UDP 传输，没有认证机制。这带来了几类攻击：

**DNS 缓存投毒（Cache Poisoning）**：攻击者伪造 DNS 响应，在解析器缓存中写入假记录，让用户访问钓鱼网站。2008 年，安全研究员 Dan Kaminsky 发现了 DNS 实现的重大漏洞，可以高效地进行缓存投毒，引发了互联网史上最大规模的协调漏洞修复行动。

**DNSSEC（DNS Security Extensions）**：为 DNS 响应添加数字签名，让解析器能验证数据是否来自真正的权威服务器、是否被篡改。但 DNSSEC 不加密数据，仅做完整性验证，且部署极其缓慢——复杂性高、运营成本高、错误容忍度低。

**DNS over HTTPS（DoH）和 DNS over TLS（DoT）**：把 DNS 查询加密，防止 ISP 或网络监听者看到你查询了哪些域名。浏览器已开始内置 DoH 支持，绕过 ISP 的 DNS 服务器——这引发了 ISP 和网络安全社区的争议：DoH 破坏了企业网络的 DNS 过滤机制，也让 ISP 无法监控 DNS 以应对法律义务。

**DNS 作为攻击工具**：
- **DNS 放大攻击**：向开放 DNS 解析器发送伪造来源 IP 的小查询，触发大响应发送到受害者，流量放大比可达 50-100 倍。
- **快速变化（Fast Flux）**：恶意软件域名频繁更换 IP，每次 TTL 极短，让黑名单失效。

## 代价与争议

**中心化风险**：DNS 根服务器虽然分布广泛，但根区域的签名密钥由 ICANN 控制。这使得某种程度的"互联网控制权"集中在一个非政府国际组织中。多个国家（中国、俄罗斯）已建立国家 DNS 基础设施，能在极端情况下与全球 DNS 断开独立运行。

**内容管制的工具**：ISP 和政府经常用 DNS 阻断来实现内容管制（将被封锁域名解析到 127.0.0.1 或不返回）。这推动了 DoH 的采用，也引发了关于谁应该控制 DNS 解析的政策辩论。

**Anycast 与弹性**：13 个根服务器"地址"背后实际上有 1500+ 个物理节点，使用 Anycast 路由：你的请求被路由到地理上最近的实例。这让根服务器几乎不可能被 DDoS 打下去——2002 年和 2007 年都有人尝试过，失败了。

## 跨域连接

- **[[networking-protocols]]**：DNS 运行在 UDP 53 端口（小查询）或 TCP 53 端口（大响应/区域传输），是应用层协议的典型案例。
- **[[distributed-systems]]**：DNS 是人类建造的最大分布式系统之一——无中央数据库、最终一致性（通过 TTL 和缓存）、跨全球的读写分离。
- **[[computer-security-principles]]**：DNSSEC、DoH、DoT 是网络安全与可用性之间权衡的教科书案例。

---

## 参考文献

- Mockapetris, P. *Domain Names — Concepts and Facilities.* RFC 1034, IETF, 1987.
- Mockapetris, P. *Domain Names — Implementation and Specification.* RFC 1035, IETF, 1987.
- Kaminsky, D. *It's the End of the Cache As We Know It.* Black Hat USA, 2008.

## 延伸阅读

- Albitz, P. & Liu, C. *DNS and BIND.* 5th ed. O'Reilly, 2006.
