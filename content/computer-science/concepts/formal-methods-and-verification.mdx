---
title: 形式化方法与程序验证
title_en: Formal Methods and Verification
status: published
updated: 2026-06-28
category: 理论与语言
order: 51
field: 计算机科学 · 软件工程
tags:
  - 形式化方法
  - 程序验证
  - 模型检验
  - 定理证明
  - 软件安全
related:
  - programming-language-design
  - type-systems
  - software-testing
---

# 形式化方法与程序验证

1996 年 6 月 4 日，欧洲航天局的阿丽亚娜 5 号运载火箭在发射 37 秒后爆炸。事故原因是一个 64 位浮点数在转换为 16 位整数时溢出，触发了未处理的异常，导致控制系统崩溃。这个软件 bug 直接损失约 3.7 亿美元。

更讽刺的是：那段代码是从阿丽亚娜 4 号直接移植过来的，在后者上运行完全正常——只是没人用数学方法验证它在阿丽亚娜 5 号更快的速度下是否依然有效。

两年前，另一桩著名的"数学 bug"已经敲过警钟：1994 年，英特尔奔腾处理器的浮点除法单元（FDIV）在某些高精度除法上返回错误结果，根源是芯片内部查找表里漏填的少数项。事件最终迫使英特尔召回芯片，并在 1995 年 1 月计提 4.75 亿美元损失——这是英特尔历史上第一次 CPU 召回。一个未被穷尽验证的算术单元，代价以亿美元计。

形式化方法的核心承诺正是：**用数学严格证明软件满足其规约，而不只是测试"在我试过的输入上它没出问题"。**

## 破除误解：测试不等于证明

软件测试能找到 bug，但不能证明没有 bug。即使对一个函数测试了一百万种输入，仍然可能存在第一百万零一种输入触发错误。

形式化验证则不同：它能对所有可能的输入证明某个性质成立。代价是：你必须用数学语言精确描述"正确"是什么意思，这本身就困难且昂贵。

Dijkstra 说过一句被反复引用的话：*"测试只能证明 bug 的存在，不能证明其不存在。"*（Program testing can be used to show the presence of bugs, but never to show their absence.）

## 核心工具一：Hoare 逻辑

1969 年，Tony Hoare 发表了《计算机程序设计的公理基础》（An Axiomatic Basis for Computer Programming），提出了以他命名的逻辑系统：

**Hoare 三元组** $\{P\}\ C\ \{Q\}$ 表示：若程序状态满足前置条件 $P$，执行程序 $C$ 后，状态将满足后置条件 $Q$。

例如，对于整数交换程序：
- 前置条件：$x = a \wedge y = b$
- 后置条件：$x = b \wedge y = a$

这套逻辑有一套推理规则，允许把复杂程序的正确性分解为子程序的正确性。现代程序验证工具（如 Dafny、Why3）都基于此思想。

## 核心工具二：模型检验

模型检验（Model Checking）由 Clarke、Emerson 和 Sifakis 在 1980 年代初独立发展，三人因此获得 2007 年图灵奖。

它的思路是：把系统建模为**有限状态机**，把性质写成时序逻辑公式（如 CTL、LTL），然后由算法自动穷举所有状态，验证性质是否成立。如果违反，返回一条反例路径。

| 工具 | 用途 |
|------|------|
| SPIN | 并发协议验证（贝尔实验室） |
| CBMC | C 程序有界模型检验 |
| NuSMV / nuXmv | 硬件与协议验证 |
| TLA+ | 分布式系统规约（Leslie Lamport 设计，AWS 大规模使用） |

模型检验的局限是**状态爆炸**：系统状态数随变量数指数增长，真实软件往往无法完整枚举。符号模型检验（用 BDD 或 SAT 求解器紧凑表示状态空间）部分缓解了这一问题。

**工业界的真实战果**：形式化方法早已不只属于航空航天。亚马逊云服务（AWS）的工程师在 2015 年的 CACM 论文《How Amazon Web Services Uses Formal Methods》中报告：他们用 TLA+ 为多个核心系统写规约并做模型检验，在 DynamoDB 的复制与成员管理协议里发现了 3 个需要长达 35 步执行轨迹才能触发的深层 bug，又在 S3 后台数据再分布、EBS 卷管理中各发现若干 bug。这些都是只在罕见时序交错下才出现、传统测试与代码评审几乎无法捕捉的错误。一位 DynamoDB 工程师事后表示，早知道有 TLA+，他会从一开始就用它。这说明：对于并发与分布式系统，"先把设计证明对、再写代码"正在成为大型科技公司的实际工程实践，而不只是教科书里的理想。

## 核心工具三：定理证明助手

交互式定理证明器（Interactive Theorem Prover）允许用户在机器的辅助下构建数学证明：

- **Coq**（INRIA）：法国开发，用依赖类型理论，曾用于验证 CompCert C 编译器（该编译器生成的代码有完整的正确性证明）
- **Isabelle/HOL**：英德团队，用高阶逻辑，验证过 seL4 微内核（一个经过完整形式化验证的操作系统内核）
- **Lean 4**：微软研究院，近年在数学社区获得广泛关注，菲尔兹奖得主 Peter Scholze 曾用它验证一个复杂数学证明

2009 年，seL4 项目发表了迄今最重要的形式化验证成果之一：完整证明了一个 8700 行 C 代码的微内核的正确性，确保它不会出现缓冲区溢出、空指针解引用等内存安全问题。

## 幕后引擎：SMT 求解器

上面这些工具大多不是"从零手写证明"，而是把繁琐的逻辑判定交给一个底层引擎——**SMT 求解器**（Satisfiability Modulo Theories，可满足性模理论）。

它在布尔可满足性（SAT）之上，叠加了对整数、数组、位向量等"理论"的判定能力，能自动回答"这组约束有没有解"。微软研究院从 2006 年起开发的 **Z3** 是其中最有影响力的一个。

Hoare 逻辑风格的验证器（如前面提到的 Dafny）正是把程序的"验证条件"翻译成逻辑公式，再丢给 Z3 自动判定——程序员写下断言，机器负责证明。SMT 求解器把形式化验证从"纯手工证明"推向"可规模化的自动化"，这是近二十年形式化方法真正走进工程的关键一步。

一个典型的现代战场是**智能合约**。2016 年 6 月 17 日，以太坊上的 The DAO 因一个"重入"（reentrancy）漏洞被攻击，约 360 万枚以太币（当时约 6000 万美元）被抽走——合约在更新余额之前就转出资金，使攻击者得以在同一笔交易里反复提款。

这类"代码即法律、且部署后不可更改"的合约，出事后无法打补丁。于是形式化验证从奢侈品变成了刚需：在部署前用数学证明合约不会重入、不会溢出，正成为区块链开发的标准实践之一。

## 代价与争议

**成本极高**：形式化验证的工程成本通常是普通开发的 5 到 10 倍甚至更高。大多数商业项目无法承受。因此，形式化方法主要用于安全关键系统（航空航天、医疗设备、核电控制）。

**规约本身可能错误**：形式化方法只能证明软件满足其规约。如果规约（即数学描述的"正确性"）本身就是错的，证明毫无意义。"在正确的事情上证明了正确"的假设并不总是成立。

**与敏捷开发的张力**：形式化方法需要在开发之初就精确定义所有性质，与需求频繁变化的互联网软件开发文化几乎不兼容。两种方法在领域上的分野正在加深。

**中间道路**：但"全有或全无"是个伪命题。**基于性质的测试**（property-based testing，如 Haskell 的 QuickCheck，2000 年提出）就是一种轻量级折中——开发者像写规约一样写下程序应满足的"性质"，工具再自动生成大量随机输入去试图证伪它。它给不出"对所有输入都成立"的数学保证，却能以极低成本逼近形式化方法的思路，因此被广泛集成进日常工程。

## 跨域连接

- **数学**：形式化验证与数理逻辑直接重叠——它本质上是把逻辑证明工程化。Curry-Howard 同构告诉我们，证明和程序是同一回事的两面。反过来，证明助手也开始反哺数学本身：2005 年，Georges Gonthier 用 Coq 完整形式化验证了"四色定理"（此前 1976 年的证明依赖计算机穷举、长期不被部分数学家接受）；Thomas Hales 领导的 Flyspeck 项目又在 2014 年用 HOL Light 与 Isabelle 完成了开普勒猜想的形式化证明。机器不再只是检查程序，也开始为人类把关最艰深的数学定理。
- **哲学**：Wittgenstein 的"规则遵循悖论"在形式化语义中有精确的对应：什么叫"正确地遵循一条规则"？形式化方法必须给出答案。
- **[[software-testing]]**：测试与形式化验证是互补的，不是替代关系——测试发现 bug，验证证明无 bug。
- **[[type-systems]]**：强类型系统可以视为轻量级形式化验证，自动证明某类性质（如类型安全）。

---

## 参考文献

- Hoare, C. A. R. *An Axiomatic Basis for Computer Programming.* CACM 12(10), 1969.
- Clarke, E., Grumberg, O., Peled, D. *Model Checking.* MIT Press, 1999.
- Klein, G. et al. *seL4: Formal Verification of an OS Kernel.* SOSP, 2009.（seL4 验证原始论文）
- Lamport, L. *Specifying Systems: The TLA+ Language and Tools for Hardware and Software Engineers.* Addison-Wesley, 2002.
- Leroy, X. *Formal Verification of a Realistic Compiler.* CACM 52(7), 2009.（CompCert 编译器验证）
- Newcombe, C. et al. *How Amazon Web Services Uses Formal Methods.* CACM 58(4), 2015.（TLA+ 在 AWS 生产系统的应用）
- de Moura, L. & Bjørner, N. *Z3: An Efficient SMT Solver.* TACAS, 2008.（SMT 求解器，程序验证的底层引擎）
