# 工作日志 — Episteme · 格致

这份日志回答三个问题：**项目现在处在什么状态、哪些约束仍然有效、最近几次会话做了什么**。逐次原文在 `docs/archive/`，本文件只留读者真正需要的部分。

- 状态与待办的真相源是 `docs/任务清单.md`；设计决定写在 `docs/设计方向-观测台与手记.md`；本文件不重复它们，只指过去。
- 每次会话结束在末尾追加一条，**不超过约 20 行**。写做了什么、为什么、踩到什么、怎样验证；不写文件清单、逐函数流水账、字数播报。
- 本文件超过约 400 行时，把较早的逐条记录挪进 `docs/archive/工作日志-<起>至<止>.md`，在「演进」一节补一行摘要。

```markdown
## [YYYY-MM-DD] 标题（T-XXX）

**做了什么**：两三句，说清改动和原因。
**踩到的**：新发现的坑或结论（没有就省略这一行）。
**验证**：跑了哪些门禁，结果如何；没跑的写明原因。
**下一步**：下一个代理的第一件事。
```

---

## 现状（2026-09-24）

- **规模**：22 个学科，3166 篇文章（`generated/site-stats.json` 为唯一计数来源），图谱 3565 节点，1682 个单测。
- **进行中**：前端设计改造（任务书 `docs/superpowers/specs/2026-09-23-前端设计改造任务书.md`，拆解见 `docs/设计审计-基线.md` 第六节）。E0 / E2 / E1 / E3 / E6 / E5 / E4 已完成；E7 格致山 3D **动工前须用户审批**原型；E8 清单已做完。
- **等用户决定**：柏拉图线段喻改用 `levels` 组件（06g，要动内容文件）；`summary` 字段、个别人物分期等「待作者裁定」项见任务清单。
- **线上**：2026-09-24 经 CI run 36086480934 部署到生产（`fd945b6f`），积压自 09-15 的改动已全部提交。待办里最该先做的是拆分知识图谱挂载的主线程工作（已知问题，Lighthouse 预算暂按 CI 定线）。
- **本机环境**：3067 端口上有一个 9 月 19 日启动、已无响应的 `next-server`，不是代理启动的，没有动。开发服改用 3069；验证用的 `next start` 用完即关——共用 `.next-prod` 的旧服务会在 ISR 重验证时把旧页面写回新构建；e2e 的默认配置会复用 3067 而卡死，要用冒烟配置或指向生产服务的临时配置。

---

## 仍然有效的约束与坑

`CLAUDE.md` 第 9 节「高频踩坑」之外，这些是近几个月会话里反复验证过的：

**写内容**

- 化学不在 `MDX_DOMAINS`，`related:` 不被 `check-content` 扫描；入边写在对方正文的 `[[slug]]` 里。
- `related:` 各域规矩不同：生命科学只认同域 `.mdx` 文件名，不能写嵌套 wiki slug；经济学概念 / 理论只能指向经济学家、理论、概念；哲学 experiments 禁止 `related:`；心理学 experiments 没有人物页时 `relatedTheorists` 留空。wiki 别名不等于文件名（`discrimination-and-inequality` 的文件是 `inequality-economics`）。
- 正文字数带 3800–4800 汉字：入边后常被顶破上限，已贴顶的文章只加 `related` 或几乎不增字的 wiki 包裹。
- 跨域连接节至少 400 汉字（审计不计标题行）；对话体与人类历史 7 份编辑元文档豁免。
- 语言学方法板块每加一篇，要同时给最薄的文字板块加一篇，否则 `subject-candidate-matrix` 的平衡测试不过。
- 引文最常见的错是把会议前身当成期刊封面（如 Baker–Gill–Solovay 是 _SIAM J. Comput._ 1975，不是 JACM）；写前核 DOI。
- 去 slop 仍未清完：模板化的「为什么今天仍然重要」节集中在工程、法学、艺术与心理学人物页；「比以往任何时候都」还散在约 19 篇里，嵌在对象段落中，不能全局删签名。
- 奇趣卡没有专页时落到 `/curiosities`，不硬接学科首页。

**工程**

- 验证用生产构建一律加 `NEXT_DIST_DIR=.next-prod`，不要和开发服抢 `.next`；否则会出现 `Cannot find module for page: /_document`，开发服也跟着 500。
- 内容轮中途不要改图谱快照（`aggregate-snapshot.json`）；`corpus-store` 必须在 `gen-search-index` 之后跑。
- 服务端组件从 `"use client"` 模块导入常量会得到 `undefined`，共享常量要单独成文件（例：`lib/design/motion-tokens.ts`）。
- 不要用滚动揭示把内容藏到动画之后（`initial={{ opacity: 0 }}` + `whileInView`）：整页截图、锚点跳转、阅读器模式都会看到空白。E5 已把学科首页上的这类用法清掉。
- 本机统计中文值时 `sort | uniq -c` 要加 `LC_ALL=C`，否则不同中文值会被合并计数。
- zsh 遇到不匹配的通配符会中止整条 `rm`，清理临时文件要逐个写文件名。
- 知识图谱有 4 条 e2e 在 HEAD 上即失败（旋转用例 + 三条手机端导览），不在 CI 冒烟内，已登记。判断是否自己引入的失败时，用 HEAD 另建一份构建对照跑。

---

## 演进

逐次原文按时期归档在 `docs/archive/`，每行对应一份。

- **#1–#82（2026-06 上旬至 06-19）** · [归档](archive/工作日志-完整-至2026-06-19.md)：从 monorepo 收敛为单一 Next.js 15 应用；十个学科建成，通用领域引擎上线；讲解（TTS）原型；部署改为 `vercel deploy --prebuilt --archive`（[复盘](复盘-从一次Vercel部署翻车到CICD最优解.md)）。
- **#83–#188（06-19 至 07-19）** · [归档](archive/工作日志-2026-06-19至2026-07-19.md)：第 11–15 个学科接入；跨域连接引擎与知识图谱认知层；32 项安全加固；学科方法论与五级主干补齐。
- **#189–#275（07-19 至 08-19）** · [归档](archive/工作日志-2026-07-19至2026-08-19.md)：搜索两层检索重建；16 域内容治理；法学、艺术、工程上线；叙事打磨 R1–R6。
- **#276–#304（08-19 至 08-24）** · [归档](archive/工作日志-2026-08-19至2026-08-24.md)：叙事打磨 R7–R13；死胡同治理（搜索、每日、奇趣、随机、sitemap）。
- **#305–#380（08-24 至 09-11）** · [归档](archive/工作日志-2026-08-24至2026-09-11.md)：既有核心文全部收到 3800–4800；六个薄域织入；文学、宗教上线准备。
- **#381–#412（09-11 至 09-12）** · [归档](archive/工作日志-2026-09-11至2026-09-12.md)：宗教、人类学、教育学上线；理工前沿加深到 ≥2800 字。
- **#413 至设计改造 E5（09-12 至 09-24）** · [归档](archive/工作日志-2026-09-12至2026-09-24.md)：
  - **厚薄不均收口（#413–#420）**：五薄域课程矩阵的缺章补齐；非前沿文章的跨域连接节全部达到 400 字。
  - **阅读栏统一（#421–#424）**：各学科详情页改用同一套居中阅读栏与阅读三件套。
  - **对象剖析长程协议（#425–#485）**：每轮五篇真实对象（实验、论文、装置、判决），约六十轮。#475 后做了一次代码审查，教育学随 commit `c4f332a3` 上线。后期选题从「大学生、上班族、好奇的老人」漂向 CS 论文链，09-20 的接管判断把方向转到内容去 slop。
  - **欢乐层（09-18 至 09-20）**：有出处的奇趣卡铺到全部学科（416 张）、40 条跨学科巧合，并在每日、首页、搜索空结果处露出。
  - **动效层与全仓审查（09-19 至 09-20）**：`T-MOTION-02`「仪器苏醒」与知识轨道；逻辑与性能审查的待办全部落地；生产构建与开发服隔离（`NEXT_DIST_DIR`）。
  - **前端设计改造（09-23 至 09-24）**：见下一节。

---

## 前端设计改造（2026-09-23 起）

设计决定与每个 epic 的细节在 `docs/设计方向-观测台与手记.md`（按 epic 分节），这里只记过程中的判断与坑。

- **E0 视觉审计基线**：截图脚本 `scripts/visual-baseline.mjs`；审计写在 `docs/设计审计-基线.md`。发现站点缺省域名指向别人的项目（P0），同一统计在三处互相矛盾，路由 CSS 与首页 RSC 的余量不到 1 KB。
- **E2 正确性速修**：域名修正，并加测试禁止写死 `*.vercel.app`；`gen-site-stats` 成为唯一计数来源；文章元数据、导语、每日知识标签与去重、名言时间线排序、「中世纪」分期都已修。结论：手写数字确实会漂（「6 大灭绝」实为 5 次）。
- **预算复核**（任务清单决策记录第 15 条）：CSS、HTML、RSC、JSON 改按 brotli 计；CSS 拆成共享表、单路由增量、首访合计三条；图谱数据拆成首绘与延后描述两份。本地图谱 LCP 18 秒的根因是 `next start` 不压缩 force-static 输出，不是页面本身。
- **E1 设计系统地基**：调色板真相源 `lib/design/palette.ts`，对比度由测试兜底（揪出 8 个学科的强调色不达标）；知识几何、学科印（字形构建期提取）、功能图标、标本图版、`/design` 样张页。
- **E3 全局外壳**：六簇页头与图册面板、领域子导航、主题切换、⌘K 预览栏与「在图谱中定位」、404 / 出错 / 加载三种状态页。⌘K 对话框改为首次打开时才加载。
- **E6 阅读体验**：渲染器按段型识别（误解卡、事实卡、题词、铜线版洞察、邻域星图）；文末改为三向出口（`gen-article-exits`）；目录刻度轨、问问这篇页签、专注模式、作者组件（`docs/作者组件指南.md`）。顺手修掉代码块遇空行被切碎、标题紧贴正文被当成大标题两个老问题。五个学科样式表里的 `.prose` 旧覆盖一直在压新排版，已删除。
- **E4 首页格致仪**：首页收成六段，星盘服务端画、小控制器只管选择；学科印与图版改走 `<use>` 图标集，否则 22 张图版会把 RSC 撑爆。
- **E5 学科首页**：统一骨架 `components/landing/DomainLanding.tsx`，引导问题取自学习主线；新增「与其他领域的桥」（由反链统计）。14 个引擎域与 8 个定制域全部迁入，定制域的专属模块（名言时间线、情绪轮、宏观诊断矩阵、互动演示、时代带、九层物理、交互时间线）放进插槽。

---

## [2026-09-24] 学科首页收尾与文档瘦身（T-DESIGN-05 完成）

**做了什么**：8 个定制学科首页迁入 `DomainLanding`，专属模块放进插槽（清单见设计方向第十一节）；删掉六个旧首页客户端组件、人类历史的旧首页渲染器与 1101 行样式。工作日志从 3437 行收成本文件的形状，任务清单从 49 KB 收到 26 KB，两者的原文都进了 `archive/`；`CLAUDE.md` 与项目总览里手写的每域篇数表改为指向 `site-stats.json`。
**踩到的**：见设计方向第十一节末的三条（宇宙学层外元素规则、滚动揭示藏住的低对比度、时代数据被并进时间线外壳）。另有两条 e2e 早已过时（首页标题、「参考书目」），已按现状改写。
**验证**：`pnpm prepush` 全绿（1678 测试）；`gen-all` 幂等；生产构建、`bundle-check`、`audit-rendering` 通过；a11y 全量 79 页 × 双主题 × 双视口为 0，另扫 44 个交互状态（演示页签、展开时间线与筛选、展开名言卡、情绪轮）为 0；冒烟 26/26，人类历史、门户、语言学 e2e 全过。截图在 `visual-baseline/e5b-after/`。
**下一步**：见下一条。

## [2026-09-24] 自拼文章外壳并入 ArticleLayout（T-DESIGN-08 部分）

**做了什么**：数学 5 类（概念、定理、数学家、对话、悖论）、生命科学 4 类（科学家、灭绝、时间线、物种）与哲学学派详情页改用 `ArticleLayout`，于是都有了问问这篇、三向出口、反链、目录刻度轨。定制页头折进 `eyebrow` / `meta` / `lede`；与页头重复的事实卡（科学家页的领域、时代、生卒）删掉。`life-science/tree/[domain]` 是分类总览，不是文章，保留原样。
**踩到的**：批量转换脚本按「最后一个 `}`」截断默认导出时误删了文件末尾的辅助组件，已对照 HEAD 逐个检查被转换文件的辅助函数，只有一处，已恢复。
**验证**：`pnpm prepush` 全绿（1678 测试，check-content 0/0）；生产构建、`bundle-check`、`audit-rendering` 通过；12 类页面状态 200、单一 h1、出口齐全；a11y 全量 0；冒烟 26/26。
**下一步**：E4 等用户审批送审稿的四个问题；不依赖 E4 的 E8 项还剩引文页边化。

人类历史的羊皮纸令牌表也纳入了对比度测试（`palette.test.ts`，半透明卡面先合成到 `--bg` 再算）。暗色下 muted 墨在悬停卡与浮层上只有 4.16 / 3.86，提到 `#9a8b65`；secondary 同步提到 `#ad9d79`，保住三级墨的梯度。

## [2026-09-24] 首页格致仪（T-DESIGN-04 完成）

**做了什么**：用户批准送审稿四条建议后落地 E4：首页从八段收成六段（格致仪、六簇二十二域、今天、读完一个主题、知识连续体页签、登上格致山），删掉旧首屏、星座画布、功能格与 GSAP 滚动控制器；环序改为方案 B。细节在设计方向第十二节，取舍在决策记录第 17 条。
**踩到的**：照准规的拖动柄随旋转扫过静止的鼠标，会把选择拉回原扇区，改为不接收指针事件。上午遗留的 3068 旧 `next start` 进程共用 `.next-prod`，在首页 ISR 重验证时把旧首页写回了新构建，冒烟因此看到旧页面；验证前务必先清掉共用构建目录的旧服务。
**验证**：`pnpm prepush` 全绿（1682 测试）；生产构建、`bundle-check`（首页文档 26.1 / RSC 16.7 KB br）、`audit-rendering` 通过；a11y 全量 0；CI 同款冒烟 26/26（含新增的格致仪键盘用例）；首页相关 e2e（门户、连续体、汇流、前沿、a11y）74/74。
**上线**：第一次 CI 挂在 Lighthouse——新加的预热用 Playwright 自带 Chromium，CI 从不下载它，改用系统 Chrome。第二次挂在图谱路由：devtools 真实降速在 CI runner 上测出 TBT 1.3–1.6 s，本地只有 49 ms；这段挂载开销一直存在，以前没被测到。经用户同意，预算按 CI 重新定线并登记已知问题（决策记录第 17 条 ⑤）。第三次 CI（run 36086480934）quality、build、Lighthouse、冒烟全绿，部署成功，线上首页、图标集、读数接口均已核对。
**下一步**：拆分知识图谱挂载的主线程工作并收回预算；E7 格致山需要用户先审批原型；E8 剩引文页边化与视图过渡。

## [2026-09-25] 知识图谱挂载瘦身（第一轮）

**做了什么**：小地图原来把 3565 个节点各渲染成一个 `<div>`，平移缩放时整批重渲染，改为画布一次画完；只有显示路径时才用到的边标签表改为首次查询时构建。Lighthouse 预算暂不收回。
**踩到的**：本机测量噪声极大（同一构建 TBT 587–1926 ms），三种看似合理的优化实测无收益，均已撤回，结论写在已知问题里，免得下一轮重试。宽范围跑 `prettier --write` 会顺手改掉旁边没格式化过的文件，只对改动文件运行。
**验证**：`pnpm prepush` 全绿（1682 测试）；图谱 e2e 与 HEAD 对照失败项完全相同（4 条原有失败）。CI（run 36138431397）图谱页性能 51、LCP 5.4 s、TBT 1.21 s（上一轮 47–50 / 5.5–5.8 s / 1.28–1.62 s），已部署。
**下一步**：渲染数据构建与首帧绘制拆到多帧或 Worker，然后收回图谱 Lighthouse 预算。

## [2026-09-25] E8 收尾与知识图谱挂载第二轮

**做了什么**：按用户指示三件事都做完，改动留在工作区不提交。① 知识图谱第二轮：学习前沿挂载只算状态、单节点详情按需算（与完整快照逐项一致，有测试），邻接表首次使用时才建，构建期布局时不再构建布局输入，画边不再每帧拼高亮键；本机 12 倍降速 TBT 中位数约 640 → 570 ms（首轮前约 840）。② 手机端导览卡片显示当前节点名，三条原失败的手机 e2e 转绿。③ E8：宽屏页边注（窄屏回退为文末脚注）、首页卡片到学科首页的图版视图过渡。细节在设计方向第九、十节与任务清单已知问题。
**踩到的**：先临时逐个 memo、逐个绘制层打点，拿到每段真实耗时再下手——凭剖析的直觉挑过三次都挑错。本机测量噪声大，同一构建要跑 5–6 次看中位数。引文区域有两处老的无障碍问题（返回链接只靠颜色、心理学浅色强调色 4.01），整站扫描清单里没有带脚注的文章才一直没发现，已补进清单。
**验证**：`pnpm prepush` 全绿（1685 测试）；生产构建、`bundle-check`、`audit-rendering` 通过；a11y 全量 80 页 × 双主题 × 双视口为 0，另在 1680 宽双主题扫脚注页为 0；CI 同款冒烟 28/28；门户、连续体、前沿、图谱 e2e 通过（只剩那条原有的旋转用例）。
**下一步**：图谱预算要收回原值只能改架构（Worker 绘制 + 二进制数据），待用户决定；用户说提交时，这批改动可合成一个 commit。

## [2026-09-25] 内容去模板第二轮与工程健康

**做了什么**：① 去模板：改用「跨文件重复句」检测，删掉 ≥5 篇共用的模板段与模板句（约 190 篇），逐句改写「比以往任何时候都」「信息爆炸」一类空话；免责声明保留，文章变短但都在底线以上。② 工程健康：新增夜间全量 e2e 工作流；CI runner 固定 `ubuntu-24.04`，`upload-artifact` 升到 v7；删掉 13 个误提交的 `scripts/_tmp-*` 并加进 gitignore；`audit-rendering` 认 `NEXT_DIST_DIR`。
**踩到的**：第一次全量 e2e 挂了 15 条，只有冒烟进门禁，其余 spec 一直没人跑。多数是写死的篇数和旋转角，已改为从 `content/` 与布局函数推导。真正的 bug 有两个：手机上「连接引擎」面板右对齐触发按钮，左边 130px 在屏幕外，已改为贴视口；空间视图的 URL 同步用 160ms 定时器里捕获的旧参数，会把期间点的导览步骤覆盖回去，已改为触发时读当前地址。
**验证**：`pnpm prepush` 全绿（1685 测试，check-content 0/0）；生产构建、`bundle-check`、`audit-rendering` 通过；a11y 全量 0；CI 参数下全量 e2e 175 过 / 1 flaky（竞态修复前），修复后图谱三个 spec 重复三遍不重试 99/99。
**下一步**：看第一晚夜间 e2e 的结果；图谱预算收回仍待用户决定是否上 Worker 绘制。
