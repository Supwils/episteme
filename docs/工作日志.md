# 工作日志 — Universe Knowledge 平台

> **代理使用说明**：每次会话结束前，在本文件末尾追加一条记录。格式见下方模板。  
> 最新记录在最下方。上一个代理的记录告诉你现在的真实状态。

---

## 日志格式模板

```markdown
## [YYYY-MM-DD] 会话 #N

**执行人**：[Kilocode / Claude Code / 其他]
**任务**：[T-XXX 任务标题]

### 完成内容

-

### 发现的问题

-

### 遗留状态

-

### 下一步（下一个代理的第一件事）

-

### 任务清单变更

- [T-XXX] 已完成
```

---

## 历史里程碑（会话 #1–#82 摘要）

> 完整逐次记录（84 次会话全文）已归档至 [`archive/工作日志-完整-至2026-06-19.md`](archive/工作日志-完整-至2026-06-19.md)。
> 下方为按相位浓缩的里程碑，供快速了解平台演进；需要某次会话的细节请查归档。

- **奠基与迁移（#1–#30 · 2026-06-01 起）**：搭建平台骨架；从 Turborepo monorepo 收敛为**单一 Next.js 15 应用**（应用代码移至仓库根目录，移除 apps/packages/turbo/workspace）；内容统一到根 `content/<domain>/`，类型化数据用 `@/content` 导入、MDX 散文运行时 fs 读取。
- **十大知识领域建成（#30–#69）**：宇宙物理（R3F 3D）、人类历史（时间线+Atlas）、哲学、生命科学、宇宙学、数学、经济学、心理学、计算机科学、政治学；通用领域引擎 `lib/knowledge-domain.ts` + `lib/new-domains.ts` 驱动后接入的域。
- **可发现性与交叉网（#70–#78）**：薄学科扩容至 ~150 篇；残篇/断链质量夯实；「像一本书」的阅读体验；跨域链接 + 搜索镜像管道（gen-\* 系列）；知识图谱补全到 10 域；时间线路由。
- **新功能：讲解（Audio Narration）（#79）**：端到端 TTS 原型 → 接入小米 MiMo TTS + 哲学讲解；门户首页大改版（多子代理协作）。
- **十学科深度优先深化轮（#80–#81）**：10 子代理并行，对各域做深度优先的内容深化。
- **历史去重 + 事实核查 + KB 知识网基建（#82）**：历史近似重复节合并；专项事实核查；知识库（KB）`[[wiki-links]]` 反链网络基建。
- **部署 CI/CD 攻坚（#82 期间 · 2026-06-17）**：修复 Vercel 15000 文件上限（高基数内容路由全部改按需 ISR）；改用 `vercel deploy --prebuilt --prod --archive` 经 GitHub Actions 上线（原生 git 集成拒绝 Next 符号链接去重）；生产构建切 Turbopack、deploy 门控于 CI。完整复盘见 [`复盘-从一次Vercel部署翻车到CICD最优解.md`](复盘-从一次Vercel部署翻车到CICD最优解.md)。

---

## 历史里程碑（会话 #83–#188 摘要）

> 完整逐次记录已归档至 [`archive/工作日志-2026-06-19至2026-07-19.md`](archive/工作日志-2026-06-19至2026-07-19.md)（5241 行）。
> 下方按相位浓缩，需要某次会话的文件路径 / 验证命令 / 问题细节请查归档。

- **第 11–15 个领域建成（#85–#96 · 2026-06-19 → 07-03）**：医学与公共卫生、化学、地球科学、社会学、语言学依次接入通用领域引擎；配套 Mol\* 分子查看器（自托管、按需加载）、SIR 传染病模拟器。
- **旗舰「连接引擎」与知识图谱（#86–#88 · 06-24 → 06-25）**：跨域连接引擎上线；图谱两处致命 bug 修复；注释式桥边、语义缩放 LOD、跨域边过滤；`[[wiki-links]]` 悬停预览进入全平台阅读流。
- **安全审计与加固（#91 · 06-29）**：6 维并行只读审计 → CSP unsafe-inline、JSON-LD 注入、HSTS、innerHTML 等 32 项硬化落地；Mol\* 改自托管以收紧 CSP。
- **内容质量攻坚（#89–#93 · 06-28 → 07-02）**：三轮事实核查；151 篇物理去模板；307 篇传记审计；薄弱学科（地球科学/化学/医学）扩容。
- **知识图谱认知层与学习编排（#105–#144 · 07-06 → 07-12）**：宏观诊断矩阵可视化、路线解释卡片、节点 ID 治理、认知分层布局、知识地形归一化与缺口诊断、个性化学习路线编排、核心知识路径策展。
- **学科方法论与五级主干补齐（#114–#168 · 07-09 → 07-17）**：政治学研究方法、心理学现代研究方法体系；各学科五级递进主干补齐多批；语言学 IPA 与句法交互。
- **收束与发布准备（#171–#188 · 07-18 → 07-19）**：空间知识图谱修复、心理健康双思想路线、青少年服务组合实验、全平台发布冻结与 CI/CD 预检。

---

## 历史里程碑（会话 #189–#275 摘要）

> 完整逐次记录已归档至 [`archive/工作日志-2026-07-19至2026-08-19.md`](archive/工作日志-2026-07-19至2026-08-19.md)。
> 需要某次会话的文件路径 / 验证命令 / 问题细节请查归档。本段只留相位，不重复任务清单里的完成公告。

- **搜索重建与工程债（#189–#192 · 07-19 → 07-24）**：心理健康双思想路线比较；搜索两层检索重建；历史来源与可访问性收口。
- **内容治理冲刺（#193–#208 · 07-25 → 07-31）**：医学 54 篇深化；理科/计算机治理；语言学与哲学 experiments 跨域清零；16 域「全域完成」轮（`T-CONTENT-40`）收官。
- **深度轮与同题合并（#210–#219 · 07-31 → 08-02）**：心理学/哲学/计算机深度轮；哲学同题合并；社会学/语言学扩面。
- **阅读体验与新域接入（#220–#233 · 08-02 → 08-03）**：渲染器、触屏预览、打印、学习主线；法学 / 艺术 / 工程全量上线。
- **图谱扩面与剖析模式（#234–#257 · 08-04 → 08-09）**：被推翻的理论三批；图谱派生全域扩面与嵌套 KB 入图；四域 frontier、五域剖析、哲学同题节点合并；软 404 修复。
- **五薄域课程矩阵与深化轮 R1–R5（#260–#269 · 08-11 → 08-16）**：法学/工程/艺术/语言学/社会学课程矩阵；内容与阅读体验交替推进。
- **叙事打磨 R1–R6（#270–#275 · 08-18 → 08-19）**：每轮 12 篇织入式深化至 3800–4800 汉字；同步阅读体验 R5–R7。

---

## 历史里程碑（会话 #276–#304 摘要）

> 完整逐次记录已归档至 [`archive/工作日志-2026-08-19至2026-08-24.md`](archive/工作日志-2026-08-19至2026-08-24.md)。
> 含若干子代理单篇打磨记录（会话号有重复，以标题为准）。主文件只留相位；约束与纠错见下方，细节查归档。

- **叙事打磨 R7–R13（#276–#303）**：既有核心文织入至 3800–4800 汉字。R7 十二篇（CS 理论 / 巴贝奇·麦卡锡·费孝通 / 社会 / 语言）；R10–R13 各十篇（化学、数学、生命、CS、心理）。累计实质纠错：seL4 验证工具是 Isabelle/HOL 不是 Coq；费马「次年去世」为误（卒于 1665-01-12）；抗生素产量是牛津单位不是「剂」；旁观者/序列位置/可得性/社会认同伪引语改转述；分解整数不是 NP 完全；基因编辑「全部脱离输血」改为 Locatelli ~91% / Frangoul 29/30。
- **阅读体验 T-READ-08（#278）**：发现层按 Shape of World 收口（搜索出口、每日/奇趣深链、随机只落文章深度路径）。
- **死胡同治理（#279–#294）**：搜索 / 每日 / 奇趣 / 随机 / sitemap / 双主题 error。铁律：`curiosityArticleHref` 只认 ≥3 段路径；**不要**把 HISTORY_TODAY 剩余时间线 dumps 强行映射到无关文章；**不要**把肯尼迪就任/遇刺改挂人物页；物理与宇宙学 3D 嵌套 `error.tsx` 保持白字深底。
- **T-CONTENT-74（#287）**：深化轮 R6 + 领域 error / 物种路由。
- **文档对表（#295）**：#189–#275 归档；篇数、图谱、测试与代码对齐。

---

## 2026-08-24 会话 #305 — T-POLISH-14 十篇织入式深化

**执行人**：Grok 4.6
**任务**：`T-POLISH-14` 既有十篇核心文章织入至 3800–4800 汉字（不重写、不新建、不 commit）

### 完成内容

- 化学：光化学 4114；溶液与溶解度 4107。
- 数学：图论 4148；庞加莱猜想 4132；四色定理 3819。
- 生命：细胞膜与跨膜运输 4099。
- CS：CPU 流水线 3846；版本控制 3871。
- 心理：社会认同 4033；曝光效应 3904。
- 织入要点：格罗特斯–德雷珀 / 斯塔克–爱因斯坦钉初级过程；亨利 1803；欧拉 1736 与 R(5,5) 上界 46；Perelman 2002–03；Singer–Nicolson / Skou / Agre；最小群体 vs 罗伯斯山洞、Iyengar 情感极化（指标比较有争议，已对冲）；Zajonc 1968 多刺激、Bornstein 倒 U、Mita 镜像、Nuttin 名字字母；load-use 与 Spectre≠Meltdown；Git blob/tree/commit、Tridgell/BitKeeper、SHAttered；四色平面性门票、Heesch 放电、Tait 边着色。
- 社会认同泰弗尔论断保持转述 +「非逐字引语」。跨域连接均 ≥4 条、≥3 粗体 `[[slug]]`、≥400 汉字。
- `pnpm gen-all` 后索引随正文更新。

### 发现的问题

- 无新阻塞。六优先域仍有约 732 篇处于 2200–3799 汉字，下一轮可从最低的核心概念/KB 取十篇（避免 dialogues）。

### 验证结果

- 十篇汉字均在 3819–4148。
- `pnpm check-content`：0 error / 0 warning。
- `pnpm gen-all`：wiki 3186 slugs；搜索 2833 文档；backlinks 14332 边。
- 未跑完整 `pnpm prepush`（本轮只改内容与索引）。
- 未 commit。

### 下一步

- 用户说继续则开 `T-POLISH-15`：再取十篇 2200–3799 的既有核心文章织入。
- 用户授权后再 commit（工作区含 T-POLISH-10 至 14 及更早测试/文档改动）。
- 不要重映射 HISTORY_TODAY 剩余时间线、不要把肯尼迪事件改挂人物页；不要做 T-VIZ-ENG/ARTS。

### 任务清单变更

- [T-POLISH-14] 状态改为：已完成

---

## 2026-08-25 会话 #306 — 文档对表、门禁与部署

**执行人**：Grok 4.6
**任务**：停 T-POLISH-15；整理文档与开发计数；`prepush` + 生产构建；commit / push / 盯部署

### 完成内容

- 文档对表：`CLAUDE.md` / `README.md` / `任务清单.md` / `项目总览.md` / `聚焦方向.md` / `叙事与引用规范.md` / `前沿创作指南.md`。分域篇数与实测 2674 对齐；frontier 143；图谱 3059 / 20412；测试 1203 / 149。
- 工作日志 #189–#275 已归档（此前未提交的 `docs/archive/工作日志-2026-07-19至2026-08-19.md` 一并纳入）。
- 工作区含 T-POLISH-10–14 五十篇织入、发现路由测试与奇趣深链，一并提交。

### 发现的问题

- CI Playwright 冒烟：`/random` 抽到知识库中文路径时 `redirect()` 把非 ASCII 写进 `Location`，Node `ERR_INVALID_CHAR` → 500。已在 `toRedirectLocation` 按段 decode 再 encode，避免双编码。
- 构建仍提示上层 `/Users/supwils/supwilsoft/package-lock.json` 干扰 Turbopack 根目录推断，与本次改动无关。

### 验证结果

- `pnpm prepush`：typecheck / lint / check-content 0/0 / 五项审计 / **1203 tests / 149 files** 全绿。
- `pnpm build`：Turbopack 通过，266 静态页。
- `pnpm bundle-check -- --skip-build`：共享首载 144.6/180 · 路由 CSS 44.1/48 · 搜索索引 515.9/640 · 单块 167.9/285。

### 下一步

- 盯 GitHub Actions quality → build → deploy。
- 不要做 T-POLISH-15，除非用户再说继续。不要重映射 HISTORY_TODAY 剩余时间线；不要做 T-VIZ-ENG/ARTS。

### 任务清单变更

- 无新任务。T-POLISH-10–14 保持已完成。

---

## 2026-08-25 会话 #307 — 文档收口

**执行人**：Grok 4.6
**任务**：压缩活文档冗余，归档旧会话全文；不 commit

### 完成内容

- `工作日志.md`：#276–#304 全文迁至 `archive/工作日志-2026-08-19至2026-08-24.md`，主文件留相位摘要 + #305/#306 全文。约 1300 → ~200 行。
- `任务清单.md`：「已完成」从逐篇长段改为阶段摘要；已知问题去掉已修复划线项；在办区去掉已完成任务枚举。约 90 KB → ~15 KB。
- 已消费的 `阅读体验诊断-T-READ-01/06.md` 迁入 `archive/`。`archive/README.md` 同步。

### 发现的问题

- 无。活文档里决策记录仍是最长块，因为那是拍板依据，未再砍。

### 下一步

- T-POLISH-15 先不做。不要重映射 HISTORY_TODAY 剩余时间线；不要做 T-VIZ-ENG/ARTS。
- 用户授权后再 commit。

### 任务清单变更

- 无新任务号。文档结构收口。

---

## 2026-08-27 会话 #308 — T-PERF-09 文章滚动反馈性能收口

**执行人**：Codex
**任务**：接管仓库并优化长文章滚动时的主线程、渲染与交互开销；不 commit

### 完成内容

- 新增 `lib/scroll-frame.ts`：用一个被动 `scroll` 监听器和一个 `requestAnimationFrame` 调度器，为全局订阅者共享每帧一次的滚动位置与阅读进度快照。
- `ReadingProgressBar` 与 `TableOfContents` 的纯装饰进度条改为直接更新 `transform: scaleX()`；移除逐帧 React state/render 和 `width` 布局更新。
- `SectionAwareNav`、`ScrollToTop` 接入共享快照；前者在学科专属壳层中不再注册最终不会显示的滚动监听器，两个阈值组件只在布尔状态真正跨界时更新 React state。
- 新增共享监听/帧合并与进度条 compositor 更新回归测试，并将受 rAF 调度影响的既有 `ScrollToTop` 测试改为等待可见状态。

### 发现的问题

- `localhost:3067` 若残留生产 Service Worker 注册，`public/sw.js` 会把开发态 `/_next/static/` chunk 按 cache-first 处理；Turbopack 即使已重编译，浏览器仍可能执行旧代码。本轮改用干净的 `3068` 来源完成验证，登记 `T-DEV-01`，未夹带修复。
- Next.js 仍提示上层 `/Users/supwils/supwilsoft/package-lock.json` 干扰 Turbopack 根目录推断；与既有日志一致，本轮未改构建配置。

### 验证结果

- 接手基线：`pnpm typecheck && pnpm test`，1204 tests / 149 files 全绿；新增测试后 `pnpm prepush` 全绿，内容 0 error / 0 warning，**1206 tests / 151 files**。
- `pnpm build`：Turbopack 通过，266 静态页；`pnpm audit-rendering` 通过。
- `pnpm bundle-check -- --skip-build`：共享首载 144.6/180 KB；通用文章 162.4/220 KB；最大路由 CSS 44.2/48 KB；最大单块 167.9/285 KB；全部预算通过。
- 浏览器：1280px 桌面与 375px 移动端长文章滚动均正确；进度条 `width` 为空且使用同步 `scaleX()`，桌面目录、移动端顶部进度与回到顶部阈值正常。

### 下一步

- 先做 `T-DEV-01`：开发态主动解除或绕过生产 Service Worker 缓存，并验证不会影响生产离线能力。
- `T-POLISH-15`、`T-VIZ-ENG/ARTS` 继续保持暂缓，除非用户明确重启。

### 任务清单变更

- [T-PERF-09] 状态改为：已完成
- [T-DEV-01] 状态改为：待开始

---

## 2026-08-27 会话 #309 — T-DEV-01 开发态 Service Worker 缓存隔离

**执行人**：Codex
**任务**：解除生产 Service Worker 残留对开发态 Turbopack chunk 的缓存污染；保持生产离线阅读能力；不 commit

### 完成内容

- 新增 `lib/service-worker-lifecycle.ts`，将浏览器生命周期按环境集中管理：生产态继续注册 `/sw.js`，开发态查询并撤销当前页面作用域的遗留注册。
- 若旧 Worker 在撤销后仍控制当前页面，使用 `sessionStorage` 一次性哨兵触发单次刷新；控制权释放后清除哨兵，避免刷新环。
- 根布局改用带稳定 `id` 的 Next.js `Script`，以 `beforeInteractive` 输出生命周期脚本；生产离线缓存实现 `public/sw.js` 与其策略测试均未修改。
- 新增 6 项浏览器生命周期回归测试，覆盖生产注册、开发撤销、单次刷新、防刷新环、哨兵清理与不支持 Service Worker 的环境。

### 发现的问题

- Next.js 仍提示上层 `/Users/supwils/supwilsoft/package-lock.json` 干扰 Turbopack 根目录推断；与本任务无关，未扩大范围修改。
- 旧 Worker 的真实撤销会改变浏览器站点数据，本轮没有对用户已有 `localhost` 注册做自动化删除实验；恢复分支由脚本级执行测试覆盖，真实页面改用干净的 `127.0.0.1:3067` 来源做无副作用冒烟。

### 验证结果

- `pnpm prepush`：typecheck / lint / check-content 0/0 / 五项审计 / **1212 tests / 152 files** 全绿。
- `pnpm build`：Turbopack 通过，266 静态页；生产 HTML 含 `/sw.js` 注册脚本；`pnpm audit-rendering` 通过。
- `pnpm bundle-check -- --skip-build`：共享首载 144.6/180 KB；通用文章 163.8/220 KB；最大路由 CSS 44.2/48 KB；最大单块 167.9/285 KB；全部预算通过。
- 开发 HTML 含 `service-worker-lifecycle`、`getRegistration()` 与一次性哨兵；浏览器在 `127.0.0.1:3067` 正常加载，无控制台错误。

### 下一步

- 下一轮继续从用户体验出发做真实交互性能画像，先以证据确定最值得处理的下一处热点，再登记任务。
- `T-POLISH-15`、`T-VIZ-ENG/ARTS` 继续保持暂缓，除非用户明确重启。

### 任务清单变更

- [T-DEV-01] 状态改为：已完成

---

## 2026-08-27 会话 #310 — T-COPY-01 首页文案语气收敛

**执行人**：Codex
**任务**：把首页“探索人类知识的边界”等宏大表述改为更具体、克制、贴近实际产品能力的文案；不 commit

### 完成内容

- 首屏标题改为“从问题出发”，副标题为“顺着知识的线索，慢慢建立自己的理解”；说明文字直接交代文章、图谱与阅读路线，不再使用“一站式探索”“人类最重要的知识”等宣言式表达。
- 首页功能说明同步收敛：阅读路线、知识图谱、3D 交互、跨文明连接、文章库与每日知识均改为描述用户实际能做什么。
- 首页静态 metadata、Open Graph、Twitter 与 WebSite JSON-LD 统一采用新版语气；保留 App Router Server Component 的静态 metadata 结构。
- 新增首屏文案回归断言，并给带高亮 `span` 的 H1 增加精确 `aria-label`，让视觉拆分不影响无障碍名称。

### 发现的问题

- 首版候选“从一个问题开始”在 375px 视口会把最后一个“始”单独换到第二行；真实浏览器验证后进一步压缩为“从问题出发”，桌面与移动端均保持单行。
- Next.js 仍提示上层 `/Users/supwils/supwilsoft/package-lock.json` 干扰 Turbopack 根目录推断；与本任务无关，未扩大范围修改。

### 验证结果

- `pnpm prepush`：typecheck / lint / check-content 0/0 / 五项审计 / **1213 tests / 152 files** 全绿。
- `pnpm build`：Turbopack 通过，266 静态页；生产 HTML 含新版 title、H1 与副标题，旧“探索人类知识的边界”未出现。
- `pnpm audit-rendering` 通过；`pnpm bundle-check -- --skip-build` 全部预算通过：首页 RSC 88.4/95 KB，共享首载 144.6/180 KB，首页 CSS 34.7/48 KB。
- 浏览器：1280×900 与 375×812 双视口无横向溢出、无控制台错误；H1 均保持单行，页面 title 为“Episteme · 格致 — 从问题出发”。

### 下一步

- 下一轮继续按用户体验优先级做交互性能画像，或根据用户反馈继续微调首页语气。
- `T-POLISH-15`、`T-VIZ-ENG/ARTS` 继续保持暂缓，除非用户明确重启。

### 任务清单变更

- [T-COPY-01] 状态改为：已完成

---

## 2026-08-27 会话 #311 — T-POLISH-15 十篇核心内容深化

**执行人**：Codex
**任务**：继续下一轮内容打磨；从生命科学、化学、数学、心理学、计算机科学与哲学选取十篇薄核心文，做事实核查、结构深化与可操作判断补全；不新建文章，不 commit

### 完成内容

- 生命科学：《植物生物学概览》2228 → 4350，《基因测序》2269 → 4636。补植物水运输、根际、C3/C4/CAM、花与种子、免疫协同及实验因果链；补测序误差、参考偏差、T2T/人类泛基因组、临床解释与可复核流程。
- 化学：NMR 2254 → 4436，酸碱中和 2297 → 4321。补时域采集、二维谱证据边界、定量不确定度、混合物/原位监测；补活度、缓冲容量、滴定误差、工业控制、物种分布与废物转移。
- 数学：《证明的艺术》2239 → 4282，《概率与决策》2322 → 4282。补形式化可信计算基、证明的解释功能、独立性与调试；补规范/描述区分、模糊性、校准与区分度、信息价值、阈值和预测复盘。
- 心理学：“化学失衡”之争 2259 → 4487，人格—情境之争 2328 → 4491。把低血清素病因、系统参与与药物疗效拆开；补停药反应、诊断异质性和共同决策。把历史 0.30 口径改为受测量与聚合影响的经验概括；补状态分布、选择效应、文化与纵向机制。
- 计算机科学与哲学：专家系统 2721 → 4347，人格同一性 2779 → 4431。补 MYCIN 评估边界、规则来源/版本治理与现代混合架构；移除“全身原子都换过”的流行误述，补数同一/质同一、动物主义、准记忆、失智伦理与未来自我。
- 十篇合计 23,696 → 44,063 汉字，净增 **20,367**；单篇最终 4,282–4,636，全部落在滚动计划 3,800–4,800 目标区间。执行 `pnpm gen-all`，更新搜索索引及生命科学、心理学、哲学 link-preview 分片。

### 关键事实纠正

- 人类基因组计划总投入与“测一个基因组”的费用不是同一口径；删除“今天不到 200 美元”的无条件断言，并把建库、覆盖度、分析与临床解释成本拆开。
- 2024 IMO 银牌线成绩属于 AlphaProof 与 AlphaGeometry 2 的组合系统；费马大定理 Lean 形式化仍是推进中的协作工程，未写成已经完成。
- 植物 PTI 与 ETI 不是彼此独立的串行双层防线；按 2021 年研究改为相互增效网络。RuBisCO 氧化反应比例改为依温度、CO₂/O₂ 与气孔状态变化，不再给固定错误率。
- MYCIN 的 JAMA 结果限定为 10 个脑膜炎病例、8 名盲评专家的建议可接受度比较，不外推为真实医院常规部署。
- 单胺综述的否定范围限定为简单、普遍的“低血清素导致抑郁”模型；药物急性期平均疗效、个体获益、作用机制与停药处置分别表述。

### 验证结果

- `pnpm check-content`：1829 文件，0 error / 0 warning；14924 条 wiki-link occurrence 全部解析。
- `pnpm audit-cross-domain`：十篇所在六域均保持达标；全库 2503 篇达标。
- `pnpm prepush`：typecheck / lint / 内容检查 / 五项知识与图像审计 / **1213 tests / 152 files** 全绿。
- `pnpm build`：Turbopack 生产构建通过，266 静态页；`pnpm audit-rendering` 通过。
- `pnpm bundle-check -- --skip-build`：共享首载 144.6/180 KB，首页 RSC 88.4/95 KB，通用文章 163.8/220 KB，路由 CSS 44.2/48 KB，搜索索引 516.5/640 KB，全部预算通过。
- 仍有既有 Turbopack 上层 lockfile 根目录推断警告；与本轮无关。未 commit。

### 下一步

- 等用户选择下一轮：可继续从薄核心文做 `T-POLISH-16`，或切回真实交互性能画像；不自动启动新学科、T-VIZ-ENG/ARTS 或 HISTORY_TODAY 重映射。
- 用户授权后再 commit；工作区同时包含 #307–#310 的既有未提交改动。

### 任务清单变更

- [T-POLISH-15] 状态改为：已完成

---

## 2026-08-27 会话 #312 — T-MOTION-01 首页渐进式滚动动效

**执行人**：Codex
**任务**：增强首页 animation / scroll 体验，同时保持原生滚动、服务端正文与 SEO；不 commit

### 完成内容

- 新增无视觉输出的 `HomeMotionController`：首页仍由 Server Component 完整输出，控制器仅在水合后的浏览器空闲时动态加载 GSAP 与 ScrollTrigger。无 JavaScript、动画包加载失败及搜索抓取场景均直接得到可见正文，不以 `opacity: 0` 静态遮挡内容。
- 首页六簇领域卡、每日知识、最新更新、精选内容与功能入口改为进入视口时分批揭示；已经位于首屏或已经滚过的节点不再被水合后突然隐藏，避免首屏闪烁和晚到动画。
- 桌面端给首屏文字与背景加入幅度受控的滚动视差；移动端不启用首屏视差，保留原生滚动和紧凑单行标题。所有动画使用 transform / opacity，完成后清理临时内联属性与 `will-change`。
- `prefers-reduced-motion` 在初始加载和运行时切换两条路径都生效：初始为 reduce 时不下载 GSAP；页面打开后切换时通过 `gsap.matchMedia()` 撤销揭示和视差。
- 未引入 Lenis。当前是以阅读、锚点、键盘导航、sticky 与浏览器滚动恢复为主的内容站，没有必须同步 WebGL 与 DOM 的平滑滚动场景；保留原生滚动能避免扩大交互与无障碍风险。
- 新增 4 项回归断言：固定首页仍是服务端页面、静态 metadata 与 JSON-LD 保留、GSAP 只能动态导入、减少动态效果与空闲调度存在、静态 CSS 不得隐藏可抓取正文；领域卡另加渐进增强属性与无内联隐藏断言。

### 真实页面与 SEO 验证

- 生产 HTML 在脚本执行前已经包含 title、description、Open Graph、Twitter、WebSite JSON-LD、H1、18 个领域与全部下半页内容；首页仍是静态预渲染，266 个静态页构建完成。
- 浏览器桌面 1280×900：首个领域卡进入视口后恢复为完全可见，首屏文字与背景随滚动产生轻量视差；无横向溢出、无 console warning/error。
- 浏览器移动 375×812：首屏视差不启用，领域卡揭示正常，无横向溢出；“从问题出发”保持单行。
- 构建产物确认 GSAP 与 ScrollTrigger 的主体 chunk 不在初始 HTML；首页路由代码 4.3 KB、First Load JS 155 KB，共享首载预算仍为 144.6/180 KB。

### 验证结果

- `pnpm prepush`：typecheck / lint / check-content 0/0 / 五项知识与图像审计 / **1217 tests / 153 files** 全绿。
- `pnpm build`：Turbopack 通过，266 静态页；首页 `○` 静态预渲染，4.3 KB / 155 KB First Load JS。
- `pnpm audit-rendering` 通过；`pnpm bundle-check -- --skip-build` 全部预算通过：首页 HTML 184.2 KB raw / 32.9 KB gzip，RSC 88.1/95 KB，共享首载 144.6/180 KB，首页 CSS 34.7/48 KB，最大单块 167.9/285 KB。
- 仍有既有 Turbopack 上层 lockfile 根目录推断警告；与本轮无关。未 commit。

### 下一步

- 下一轮可把相同渐进增强策略用于高价值领域落地页，但应先逐页验证内容顺序、首屏 LCP 与移动端触控，不做全站统一套用。
- 若未来出现必须同步 WebGL、DOM 与滚动时间线的明确场景，再单独评估 Lenis；不为视觉一致性提前改写全站原生滚动。

### 任务清单变更

- [T-MOTION-01] 状态改为：已完成

---

## 2026-09-02—09-03 会话 #313 — 小范围增量质量循环

**执行人**：Codex（3 个子智能体只读审查，主代理逐项实现和验证）
**任务**：按用户授权持续处理可复现的小缺陷；保持既有产品行为，不提交、不部署。

### 已完成事项

- `T-HARDEN-25`：只为首页及哲学家详情增加服务端 canonical，使用已配置 metadataBase 与已解析 thinker.slug，不携带查询参数。旧产物缺失已在真实浏览器复现；**1337 tests / 165 files**、266 页构建/渲染/预算及 **14 条生产冒烟**通过，带 utm 参数的无脚本页面规范地址正确。

- `T-HARDEN-24`：将无 JavaScript 首页正文/18 个领域/元数据、文章与原生链接，以及初始 reduced-motion 的正文可见性纳入生产 smoke；**1337 tests / 165 files** 与 **14 条桌面/移动冒烟**通过，使用 #23 的真实生产构建（本项只改测试）。亲自查看两种视口搜索截图，无布局回归；canonical 缺失独立登记为 #25，不误当作已有 SEO 契约。

- `T-HARDEN-23`：更换查询时清除两层旧结果；输入框尚领先于防抖 query 时忽略 Enter 导航，避免新词打开旧文章。2 个回归先红后绿，当前查询标题/正文部分结果仍可先显示；**1337 tests / 165 files**、生产构建/渲染/预算通过。

- `T-HARDEN-22`：搜索容器改为原生 modal dialog，清理未受控 rAF，打开聚焦输入框，关闭恢复仍连接的原触发元素；清除 dialog 默认盒模型以保持布局。5 个单测先红后绿，旧生产版 Shift+Tab 背景焦点断言也先失败；**1335 tests / 165 files**、生产构建/渲染/预算和桌面/移动 **10 条冒烟**通过，覆盖空态/历史/结果三种状态。

- `T-HARDEN-21`：预览分片仅在失败时移除共享 promise，下次交互可重试，成功缓存不变；网络/HTTP/JSON 三类回归先红后绿。**1330 tests / 164 files**、生产构建/渲染/预算通过。

- `T-HARDEN-19`：结果 Link 只对普通点击 preventDefault 并调用应用导航，其他修饰键/中键保持浏览器默认；5 个组合点击回归先红后绿，6 个导航测试通过。**1327 tests / 163 files**、生产构建/渲染/预算、桌面/移动 **8 条冒烟**通过，桌面实际验证新标签打开而原页不导航。独立复审 44 项测试通过。

- `T-HARDEN-12`（09-03）：图片预览使用原生 modal dialog，补充关闭按钮、Esc、初始焦点和返回焦点；4 个回归先红后绿，**1321 tests / 162 files**、生产构建/渲染/预算通过。Chrome 真实组件夹具确认顶层模态、Tab 不进入背景、Esc 返回触发按钮，无页面错误；当前内容图像走 RegisteredImage，未把夹具误算为文章页覆盖。

- `T-HARDEN-01`：修复 `app/api/search/route.ts` 将缺省/空白 limit 误算为 1 的问题，恢复默认 20；5 个新增测试覆盖缺省、空串、空白与显式下限。修复前 3 个测试失败，修复后 `pnpm prepush` **1222 tests / 153 files** 全绿，内容 0 error / 0 warning，既有缓存头与最大 50 条限制不变。

- `T-HARDEN-08`：frontier 的 `level` 在 String 转换前限定为 number/string；两个带 toString 的 JSON 对象不再抛异常，数组 `[1]` 不再误通过。8 个新增用例覆盖非法值与兼容值；修复前 3 个失败，修复后 `pnpm prepush` **1241 tests / 154 files** 通过。

- `T-HARDEN-07`：OG / frontier / learning-targets 以 `Object.hasOwn` 限定枚举表取值。修复前 9 个原型键测试失败（OG 实际 PNG 渲染抛错、过滤 API 返回 200），修复后 **1233 tests / 154 files**、生产构建 266 静态页、渲染审计与全部预算通过；共享首载 144.6 KB、首页 RSC 85.1 KB（日期内容会变）。子智能体复审未发现回归。

- `T-HARDEN-15`：同一维护线更新 Next / @next/mdx / eslint-config-next 至 **15.5.25**（不升 React、不迁移架构）；实际安装版本与锁文件一致。1241 tests、266 静态页构建、渲染/预算审计、桌面/移动 6 条生产冒烟通过。共享首载 144.7 KB，首页 RSC 85.1 KB，均在预算内。[官方补丁说明](https://github.com/vercel/next.js/releases/tag/v15.5.25)。
- `T-HARDEN-10`：真实生产浏览器先复现旧 H1 断言失败，再只将断言对齐既有“从问题出发”；6 条端到端用例恢复全绿，独立重跑 prepush 1241 tests 通过。

- `T-HARDEN-13`：URL 枚举恢复使用自身键判断；3 个新增回归用例先红后绿，**1244 tests / 154 files**、生产构建、渲染审计和全部预算通过。
- `T-HARDEN-18`：gray-matter 的 js-yaml 定向锁至兼容的 **3.15.2**；小规模 empty-merge 预算测试先失败后通过，普通别名/元数据与可执行标签拒绝均保留。**1247 tests / 155 files**、全量索引生成、266 页构建与渲染/预算审计通过。[上游公告](https://github.com/nodeca/js-yaml/security/advisories/GHSA-2883-xcg3-v3hh)。

- `T-HARDEN-11`：代码复制捕获 API 缺失/权限拒绝，提供可重试失败反馈；代次和 timer 清理防止迟到结果与卸载后定时器。复制控件提取为单文件，旧导出/外观保留；5 个用例先红后绿。**1317 tests / 161 files**、生产构建/渲染/预算通过。

- `T-HARDEN-04`：用交互代次忽略失效预览，取消离开/失焦/改 href/卸载的迟到结果；触屏等待期间的外部点击也可取消，未中止其他链接共享的 fetch。5 个生命周期用例全部先红后绿；**1312 tests / 160 files**、生产构建/渲染/预算通过。

- `T-HARDEN-05`：阅读模式仅在存储边界隔离异常；读取失败回到标准模式，写入失败仍切换当前页面。5 个测试覆盖有效/无效值、getter/getItem/setItem 失败，3 个先红后绿。**1307 tests / 159 files**、构建/渲染/预算通过。

- `T-HARDEN-17`：每轮正文查询绑定 AbortController，清理时先使结果失效再 abort；关闭弹窗同步清空查询，终止隐藏请求。5 个新增用例中 4 个先红后绿；**1302 tests / 158 files**、构建/渲染/预算通过。取消传输不承诺中止服务端已开始的扫描。

- `T-HARDEN-06`：标题/正文 loading 分开管理并派生合并状态，标题异常在异步边界收敛；4 个新用例覆盖先后顺序、清空和失败，修复前有提前结束 loading 与未处理拒绝。**1297 tests / 158 files**、生产构建/渲染/预算通过；跨日首页 RSC 为 86.6 KB，仍低于 95 KB。

- `T-HARDEN-20`（09-03 接续验收）：重复 q 参数在生产模式复现 `trim is not a function` 与 HTTP 500；页面参数类型改为真实 string/string[]，按首值归一化。6 个用例中 4 个先红后绿；**1293 tests / 158 files**、生产构建、渲染/预算与桌面/移动 **8 条冒烟**全绿。

- `T-HARDEN-16`：输入框外的 Enter/上下键不被搜索 document handler 抢占，历史行忽略子按钮冒泡；3 个回归先红后绿。**1287 tests / 157 files**、生产构建/渲染/预算和独立复审通过。

- `T-HARDEN-09`：集中撤销搜索输入防抖定时器，覆盖 Escape/快捷键/遮罩关闭、重开、历史选择和卸载。4 个回归先红后绿；**1284 tests / 157 files**、生产构建/渲染/预算通过。

- `T-HARDEN-03`：全局搜索的两组快捷键忽略组合输入与 keyCode 229；2 个参数化回归先红后绿，覆盖 Enter/上下键/Escape/Ctrl+K 与普通导航。**1280 tests / 157 files**、构建/渲染/预算通过，桌面/移动 6 条生产冒烟新增组合输入 Enter 场景后全绿。

- `T-HARDEN-14`：SWR 分离 response 与 revalidation promise，同步登记 waitUntil，缓存命中不等网络、未命中不等写盘。4 个真实事件回归用例先红后绿；**1278 tests / 157 files**、最终生产构建及渲染/预算全绿。

- `T-HARDEN-02`：缓存读取/写入/淘汰各自隔离异常；成功网络响应不会被存储失败改成旧页/503，真正断网仍回退且资产不误返首页 HTML。27 个真实 SW 脚本测试中 13 个先红后绿，**1274 tests / 156 files**、生产构建与预算通过；独立只读复审通过。

### 环境与边界

- 接手时工作树已包含 #307–#312 未提交改动，全部保留；本轮不编辑知识文章或部署配置，构建按原脚本重生索引。
- 首次依赖自检遇到非 TTY 下的 store 不匹配，未强制删除 node_modules。后续安全补丁使用既有 `/Users/supwils/Library/pnpm/store/v10` 与 `--ignore-scripts` 成功安装；Node 22.22.3 / pnpm 10.27.0，初始基线 typecheck + 1217 tests 通过。沙箱内生产构建曾停滞，后续改用获批的本地构建权限并全部通过。
- 机会清单和逐项成功标准维护在 `docs/任务清单.md`，只有完整门禁通过才标记完成。

### 最终验收与交接

- **25 个独立事项全部完成**；本次接续完成 #12、#19、#21–#25 共 7 项。基线 1217 → **1337 单测（165 文件）**，共新增 120 个用例；`pnpm prepush` 内容 0 error / 0 warning，知识与图像审计全绿。
- 最新 `pnpm build` 266 静态页；`pnpm audit-rendering`：17 ISR / 2 build-static / 8 on-demand 路由模式 / 8 runtime handler 契约通过。预算：共享首载 **144.7/180 KB**、首页 RSC **86.7/95 KB**、首页 CSS **34.7/48 KB**、最大路由 CSS **44.2/48 KB**、通用文章 JS **164.7/220 KB**、搜索索引 **516.5/640 KB brotli**。
- Chrome 桌面与 iPhone 视口模拟 **14/14 生产冒烟通过**：中文组合输入、普通/组合点击、搜索三态焦点、重复参数、随机文章、图谱巡游、无 JavaScript 正文/元数据/链接/canonical、减少动态效果。图片放大另以真实组件 Chrome 夹具核验；亲自检查桌面/移动搜索截图。未声称真机 Safari、Lighthouse / INP 或全站无障碍扫描通过。
- 独立复审覆盖搜索（44 个相关测试）、弹窗（28 个相关测试）与 Service Worker；主代理整合并重跑全门禁。终审当前范围未再确认可小范围安全修复的高价值缺陷；后续先复核依赖可达风险，再做低端设备长任务与跨浏览器画像。
- 升级后的全量在线依赖审计未获回执：复审受权限限制，不能宣称漏洞清零。初始 audit 含不存在的旧 apps/portal importer；PostCSS / sharp 等公告与原生图像兼容性仍需独立评估。详情已移入任务清单「已知问题」。
- `git diff --check` 通过。保留 #307–#312 全部既有改动，无 commit / merge / push / PR / 部署；完成项已按阶段浓缩回任务清单。

---

## 2026-09-03 会话 #314 — 发布前验收与 CI/CD 交接

**执行人**：Codex
**任务**：接手仓库与发布流水线，核验 #313 的未提交交付并按用户授权提交、推送和跟踪生产部署。

### 完成内容

- 完整阅读接手协议、任务清单、近期工作日志、工程原则、内容规范及部署策略；确认根目录是生产单体应用，GitHub Actions 是唯一生产入口，Vercel 原生 Git 部署保持关闭。
- 重建本地锁定依赖并执行全量索引生成；生成产物保持稳定，`git diff --check` 通过。
- 在推送前完成质量、生产构建、渲染、包体与桌面/移动生产冒烟验收；本地工作树准备提交。

### 发现的问题

- Next/Turbopack 会因父目录的未跟踪 `package-lock.json` 提示工作区根目录推断警告。该文件不属于本仓库 checkout，未影响构建、渲染审计或冒烟测试；本次不为消除非阻断提示扩大配置改动。

### 验证结果

- `pnpm gen-all`：全部派生索引可重生，图像变体 0 张需更新。
- `pnpm prepush`：typecheck、lint、内容 0 error / 0 warning、知识/图像审计均通过；`pnpm test` 为 **1337 tests / 165 files** 全绿。
- `pnpm build`：Turbopack 通过，生成 **266** 个静态页面；`pnpm audit-rendering` 为 17 ISR / 2 build-static / 8 on-demand / 8 runtime handler 契约通过。
- `pnpm bundle-check -- --skip-build`：共享首载 144.7/180 KB、最大路由 CSS 44.2/48 KB、搜索索引 516.5/640 KB，全部预算通过。
- `CI=1 pnpm test:e2e:smoke`：桌面与移动共 **14/14** 生产冒烟通过。

### 下一步

- 跟踪本次 `main` 推送触发的 GitHub Actions Quality、Build 与 Vercel Deploy；若门禁或部署失败，先以日志定位并做最小修复后重新验证。

### 任务清单变更

- 无；#313 的 25 项已完成，本会话仅执行发布验收与交接。

---

## 2026-09-05 会话 #315 — 远端 poll 与工作树同步

**执行人**：Grok
**任务**：查看远端仓库、poll、与 origin 对齐，准备后续开发。

### 完成内容

- `git fetch --prune --tags origin` + `git pull --ff-only origin main`：本地 `main` 与 `origin/main` 同为 `4987dcfc`（`fix(site): harden release paths`），ahead/behind 均为 0。
- GitHub poll：仓库 `Supwils/episteme`，默认分支 `main`；最新生产 CI run `33755386839` 成功（2026-09-03）。
- 远端新出现 5 条未合入 `main` 的 Cursor 分支/PR（#5 开放，#6–#9 draft），与当前 `main` 无关。

### 发现的问题

- 工作树有一处未提交改动：`reference/universe-physics/CLAUDE.md` 把参考路径改到 `/Users/supwils/supwilsoft/live/...`。该文件在 `reference/`，不参与构建/测试/部署；本轮未还原也未提交。
- 5 条 PR 主题重叠（API 限流、atomic write、health smoke），均未合入，后续开发不要默认基于它们。

### 验证结果

- 本地 Node 22.22.3 / pnpm 10.27.0，`node_modules` 与 lockfile 在位。未跑 `prepush` / `build`（本轮只做 git 同步）。

### 下一步

- 等用户指定下一件开发事项。任务清单上 #313 已收口；下一内容轮与 B3 文学与叙事均需用户口令。若要处理远端 PR，先确认是否审阅/合入，不要自动 merge。

### 任务清单变更

- 无。

---

## 2026-09-05 会话 #316 — 关闭 Cursor PR #5–#9 并完成依赖可达复审

**执行人**：Grok
**任务**：审查并按判断关闭远端 5 条 PR；若任务清单仍有可做事项则继续。

### 完成内容

- 审查 [PR #5](https://github.com/Supwils/episteme/pull/5)–[#9](https://github.com/Supwils/episteme/pull/9)（笔记在 `docs/archive/pr-review-5.md` … `pr-review-9.md`），全部 **CLOSE_WITHOUT_MERGE**，并在 GitHub 写了关闭说明。未 merge、未 cherry-pick、未 push。
- 关闭理由摘要：#5 会冲掉 #313 已落地测试且 Build 红；#6/#7 是 Vercel isolate 内存限流（可伪造 IP、与 CDN 缓存冲突）且互相撞文件；#8/#9 的 `/api/health` 读错层、无消费者、Vercel preview 失败；五条都夹带无关 webp/manifest 抖动。
- 跑通 `pnpm audit`（此前 #313 未取得在线回执）。拆分根 importer vs 幽灵 `apps/portal`。删除本机未跟踪的 `apps/portal/node_modules`。未改 lockfile、未加 override。

### 发现的问题

- 锁文件仍保留 `apps/portal` importer（next 15.5.18），会把已在 15.5.25 修好的 Next CVE 混进 audit。等下一次自然依赖 bump 重生，不手剪 YAML。
- 根树仍有 PostCSS / sharp / browserslist / nanoid / fflate 公告；按可达输入评估后不构成当前公网利用面，不覆盖。

### 验证结果

- `gh pr list --state open`：空。
- `pnpm audit`：全树 42 条；`--prod` 19 条，其中 9 条 Next CVE 仅幽灵路径。根 Next **15.5.25**，sharp **0.34.5**，gray-matter 的 js-yaml **3.15.2**。
- 未跑 `prepush` / `build`（无生产代码改动）。

### 下一步

- 无自动可做的开发项。内容轮与 B3 文学与叙事需用户口令；`T-VIZ-*` 仍暂缓。评估项剩余：低端设备 INP/TBT、Safari / 全站无障碍复扫。

### 任务清单变更

- 依赖复审从「无回执」改为「已出回执，不宣称清零」，写入已知问题。
- 记录 Cursor PR #5–#9 已关闭。

---

## 2026-09-05 会话 #317 — T-POLISH-16 十篇核心文织入

**执行人**：Grok
**任务**：下一内容轮（8–12 篇）。从 2200–3799 汉字档选十篇既有核心文织入至 3800–4800 汉字；不新建、不 commit。

### 完成内容

- 十篇：消化系统 2203→4698；世界主义 2208→4792；失眠 2211→4206；工艺放大 2215→4121；选举制度 2225→4673；知情同意与共同决策 2220→4616；密码学的数学 2234→4289；水平基因转移 2240→3925；庞贝·维苏威 79 年 2263→4211；声音与聆听 2292→4689。合计 **22,311 → 44,220**，净增约 **21,909** 汉字。
- 实质纠错包括：消化道活体长度与绒毛面积（Helander 2014）；Sender 2016 菌群数量；博格中译；失眠障碍 vs 症状口径（Morin & Jarrin）；分解不是 NP 完全；HGT「20–30%」改为有出处的菌株估计；庞贝石膏模与 2024 古 DNA；凯奇《4′33″》日期核验。
- `pnpm gen-all`；图谱快照 `ambiguousTargetCount` 1905→1906（`pnpm update-graph-snapshot`）。

### 发现的问题

- 新增 wiki 链使图谱聚合快照漂移 1，属预期，已重生快照。

### 验证结果

- `pnpm check-content`：1829 文件，0 error / 0 warning。
- `pnpm audit-cross-domain`：十篇所在域达标；全库 2505 篇达标。
- `pnpm prepush`：1337 tests / 165 files 全绿。
- `pnpm build`：Turbopack 通过。`pnpm bundle-check -- --skip-build`：共享首载 144.7/180 KB，首页 RSC 86.7/95 KB，通用文章 JS 164.7/220 KB，搜索索引 **516.7/640 KB brotli**，图谱 464.4/600 KB brotli，全部预算通过。

### 下一步

- B3 文学与叙事：先写事实层/解释层分离与引文版权口径，再走新域 Playbook。然后 T-VIZ-ENG/ARTS、低端 INP/图谱 TBT、Safari 与全站无障碍。

### 任务清单变更

- [T-POLISH-16] 状态改为：已完成

---

## 2026-09-05 会话 #318 — B3 文学与叙事立项口径与 W1

**执行人**：Grok
**任务**：B3 文学与叙事。先关闭 launchGate，再注册 `/literature` 并写 W1 六篇。

### 完成内容

- 立项口径：`docs/文学与叙事立项口径.md`（事实层/解释层句式、公有领域优先、保护期内韵文 ≤8 行且 ≤20%、散文单段 ≤200 汉字）。`docs/叙事与引用规范.md` 增加指针。
- 计划：`lib/literature-subject-plan.ts`（36 篇 / 6 板块 / 3 波）。**未**写入 `LAUNCHED_SUBJECT_CANDIDATE_IDS`。
- 引擎注册：`/literature` 门户卡、连续体、图谱 spine、19 个 established 域。
- W1 六篇：`what-is-a-story`、`meter-and-the-line`、`epic-as-public-memory`、`close-reading`、`how-readers-make-meaning`、`canon-wars`。跨域节 6/6 达标。

### 发现的问题

- 覆盖面板测试把「形式推演 × 方法建模」写成死数字 7；细读入图后变成 8。已改为从 snapshot 取值。
- 四件可视化与 W2–W3 共 30 篇未写。

### 验证结果

- `pnpm check-content`：0 error / 0 warning（含 6 篇新文）。
- `pnpm audit-cross-domain`：literature 6/6 达标；全库 2511。
- `pnpm audit-learning-continuum`：19 established。
- `pnpm prepush`：1337 tests / 165 files 全绿。

### 下一步

- T-VIZ-ENG / T-VIZ-ARTS（用户已改判继续）。然后低端 INP / 图谱 TBT，Safari 与全站无障碍。文学 W2 另开一轮。

### 任务清单变更

- B3 launchGate 关闭；`/literature` W1 上线；T-VIZ 从暂缓改待开始。

---

## 2026-09-05 会话 #319 — T-VIZ-ENG/ARTS 与搜索打开时的 INP

**执行人**：Grok
**任务**：继续用户清单：工程 4 个 + 艺术 5 个教学实验室；搜索打开路径的 INP；无障碍扫描名单扩到文学与新实验室。

### 完成内容

- 工程：`/engineering/grid-flow`（五节点直流潮流）、`/structure-lab`（简支梁弯矩）、`/materials-profile`（钢/混凝土/铝/木）、`/chip-process`（八步平面工艺，不含配方）。
- 艺术：`/arts/detail-comparator`、`/perspective-lab`、`/pigment-profile`、`/exchange-map`、`/space-explorer`。均为 SVG 教学模型，挂在领域首页「互动工具」。
- 搜索 `warmup()` 改为 `requestIdleCallback({ timeout: 200 })`；真正查询会取消空闲等待。不恢复 Worker。
- `a11y-scan` 增加文学首页/文章、电网潮流、透视实验室。

### 发现的问题

- 直流潮流各线路有功之和不必为 0（每条边只计一次）；功率守恒只在节点（含平衡节点）上成立。
- 未跑真机 Safari、未跑全量 36×2×2 axe、未在低端设备上测 INP。图谱 TBT 预算未改。

### 验证结果

- 实验室相关单测 16 个先红后绿（标签名、线路功率求和）。
- `pnpm prepush`：**1354 tests / 170 files** 全绿。
- `pnpm build` + `bundle-check --skip-build`：共享首载 144.7/180 KB，首页 RSC 88.9/95 KB，通用文章 JS 166.4/220 KB，搜索索引 516.1/640 KB brotli，全部预算通过。

### 下一步

- 真机 / WebKit 无障碍与 INP 画像；文学 W2。不要把教学实验室写成运行手册。

### 任务清单变更

- [T-VIZ-ENG] [T-VIZ-ARTS] 状态改为：已完成
- 已知问题：搜索 warmup 空闲调度已落地；Safari / 全站 axe 仍未宣称通过

---

## 2026-09-06 会话 #320 — 文学 W2 十四篇发布

**执行人**：Grok
**任务**：把 `lib/literature-subject-plan.ts` 里全部 W2 标目写成 `/literature` 已发布正文；不写 W3、不标 launched、不 commit。

### 完成内容

- 14 篇 `status: published`：`plot-character-conflict`、`narrative-point-of-view`、`time-and-narrative-order`、`metaphor-and-image`、`narrator-and-voice`、`genre-as-contract`、`chinese-narrative-tradition`、`arabic-persian-narrative`、`south-asian-epic-and-poetics`、`narratology`、`textual-criticism`、`translation-as-rewriting`、`distant-reading`、`generated-text-and-authorship`。汉字约 3550–3800。
- `related` 只链已上线的 W1 slug，避免同波互指落空。
- `pnpm gen-all` 后 wiki 3206 slugs、搜索 2853 文档、literature 预览 20 条。图谱节点 3065→3079，已 `pnpm update-graph-snapshot`。
- `textual-criticism` 初稿 4172 汉字，压到 3798，贴 3800 织入上限。

### 发现的问题

- `pnpm wiki-slug` 对「叙事学」「红楼梦」一类中文查询常报不存在；跨域节改用已索引的英文 slug。
- 候选矩阵仍不标 launched（36 篇未满）。W3 十六篇与 4 个可视化仍后置。

### 验证结果

- `pnpm check-content`：0 error / 0 warning。
- `pnpm audit-cross-domain`：literature 20/20/20，节字中位 589。
- `pnpm prepush`：170 files / 1354 tests 全绿。未 commit。

### 下一步

- 文学 W3 十六篇，写满 36 篇后再议 launched。可视化、真机 INP / Safari / 全站 axe 仍不自动做。

### 任务清单变更

- B3 文学：W1+W2 共 20 篇已发布；W3 待开始

---

## 2026-09-06 会话 #321 — 文学 W3 十六篇，首发 36 篇写满

**执行人**：Grok
**任务**：继续 B3。把 `lib/literature-subject-plan.ts` 全部 W3 标目写成 `/literature` 已发布正文；不标 launched、不做 4 个可视化、不 commit。

### 完成内容

- 16 篇 `status: published`（汉字约 3660–3800）：`oral-storytelling`、`fiction-and-make-believe`、`drama-and-performance-text`、`novel-as-a-form`、`african-oral-literature`、`latin-american-and-caribbean`、`east-and-southeast-asian-modern`、`european-canon-and-its-critics`、`reception-aesthetics`、`comparative-literature-method`、`school-canon-and-exams`、`fandom-and-secondary-creation`、`libraries-and-the-book-trade`、`world-literature-as-a-market`、`copyright-and-the-public-domain`、`climate-and-the-literary-present`。
- 文学域合计 36 篇；跨域节 36/36；图谱覆盖 36/36。
- `pnpm gen-all`：wiki 3222 slugs；搜索 2869 文档（2696 文章 + 173 实体）。图谱节点 3079→3095，已 `pnpm update-graph-snapshot`。
- 先补了缺的会话 #320 工作日志（W2）。

### 发现的问题

- 36 篇已满，但 **不** 把 `literature-narrative` 标 launched：候选矩阵没有 B4，标了会使 `RECOMMENDED_SUBJECT_CANDIDATE` 空指针。4 个可视化仍未做。
- 美国公有领域：2026-01-01 起 1930 年发表作品入公有（Duke CSPD）；版权文按此写，未沿用口径里偏保守的「仅 1929」。
- 本轮未做真机点选；dev `:3067` 上抽查了文学首页与 5 篇 W3，HTTP 200。

### 验证结果

- `pnpm check-content`：0 error / 0 warning。
- `pnpm audit-cross-domain`：literature 36/36/36，节字中位 585。
- `pnpm prepush`：170 files / 1354 tests 全绿。未 commit。
- `localhost:3067/literature` 与 `oral-storytelling` / `african-oral-literature` / `reception-aesthetics` / `copyright-and-the-public-domain` / `climate-and-the-literary-present` 均为 200。

### 下一步

- 用户口令再标 launched，或先写入 B4 宗教学候选。文学 4 个可视化、真机 INP / Safari / 全站 axe 仍待办。

### 任务清单变更

- B3 文学：W1+W2+W3 共 36 篇已发布；候选仍不标 launched

---

## 2026-09-06 会话 #322 — 文学四个教学实验室

**执行人**：Grok
**任务**：把计划里后置的四件文学可视化做成 SVG 教学实验室，挂到 `/literature` 首页「互动工具」。不标 launched、不 commit。

### 完成内容

- `/literature/narrative-graph`：守株待兔四事件，对照故事次序 / 从中段起 / 先给结局。
- `/literature/meter-lab`：五言、七言、莎士比亚十四行诗 18 首行的教学音步。
- `/literature/world-map`：椭圆示意，不是底图；上古—现代分期过滤。
- `/literature/translation-comparator`：Giles 1898 与 Bynner 1929 的《静夜思》，Murray 1919 与 Butler 1900 的《奥德赛》起句。
- `a11y-scan` 增加叙事图谱与格律实验室。

### 发现的问题

- 候选矩阵仍无 B4，文学仍不标 launched。
- 实验室是教学模型，不是韵书、底图或译本裁判。

### 验证结果

- 实验室单测 9 个全绿。`pnpm prepush`：172 files / 1363 tests 全绿。
- `:3067` 文学首页与四个实验室路由均为 200。未 commit。

### 下一步

- 用户口令再标 launched 或写入 B4 宗教学候选。真机 INP / Safari / 全站 axe 仍待办。

### 任务清单变更

- 文学 4 个可视化：已完成；候选仍不标 launched

---

## 2026-09-06 会话 #323 — 手记主题深度审查与修复

**执行人**：Grok
**任务**：审查并修复 dark/light（观测台 / 手记）下各主页面、学科页与组件的颜色匹配。

### 完成内容

- 根因：领域 `@theme` 在 `:root` 上重写观测台前景色，压过门户 `.light` 重映射。`body` 仍是浅字叠奶油纸，`.domain-root` 内标题已是墨色。已在 `domain-shared.css` 与哲学/心理/经济/数学 globals 末尾补 `html.light` 未分层前景覆盖。
- 知识图谱 25 个 chrome 文件：`text-white` / `bg-[#111118]` 改为 `text-fg-*` / `bg-bg-*`；tooltip/详情面板改用 `--color-bg-overlay`。
- 哲学概念图/思想树图例、宇宙学三件套 HTML 卡、加载圈、回到顶部、目录遮罩、物理 HUD scrim 改为主题 token。
- 文学实验室 SVG 强调色改 `var(--color-accent-gold)`。
- 续修：医学模拟器浅金字/薄荷绿徽章、语言学 IPA/谱系/句法树当字用的 accent、颜料剖面近白层，改为 `text-fg-primary` 或域 accent token。
- 再修：哲学传承树节点字、心理偏差树/情绪轮、经济学供需图轴标、人类历史首页时间线岛、事件时间线搜索卡，全部从白字改 token。

### 发现的问题

- 宇宙物理 / 宇宙学 / 生命科学仍是刻意的深空画布，不跟手记翻纸。
- 图谱力导向画布本身仍是夜空像素，只修了周围 HTML。

### 验证结果

- Playwright：手记下 `/` `/literature` `/philosophy` `/arts` `/engineering` `/psychology` 的 `body` 与 `h1` 均为 `rgb(44, 42, 38)`，背景 `rgb(250, 246, 239)`。
- `pnpm prepush`：172 files / 1363 tests 全绿。未 commit。

### 下一步

- 真机 Safari 双主题；历史时间线工具条仍有少量 `#fff` 叠在强调色上（可选再收）。

### 任务清单变更

- 手记主题对比度：共享 chrome + 图谱 + 哲学/宇宙学可视化已修

---

## 2026-09-06 会话 #324 — 手记主题第二波

**执行人**：Grok
**任务**：继续 #323 的 dark/light 审查，修完数学 cyan、心理硬编码紫、历史图谱 chrome，以及子智能体扫出的浅色字。

### 完成内容

- 数学 `.light .math-root` 补 cyan/violet-bright 与纸面阴影；哲学/心理/经济同样压暗 `--shadow-md/lg`。哲学 sage、经济 copper/green 在手记下加深。
- 心理学 globals 的 `rgba(155,125,196)` 改为 `color-mix(... --color-accent-purple)`；脑区图例字改 token。
- 历史图谱 HTML chrome（`HistoryGraph` / `HistoryGraphUI` / 加载岛 / Atlas / 地图 loading）对齐知识图谱：`text-fg-*` / `bg-bg-*`。画布像素仍锁夜空。
- 图谱引导、跨域开关、路径查找、详情徽章、搜索学科 chip、每日网格徽章、医学实验室链接、概念 chip、经济图轴标、数学 plotter 轴标全部从浅色字改 token 或 `color-mix` 进墨色。
- 分子图鉴徽章/延伸阅读不再往 `#f5f2ea` 上混（纸上会糊）；阅读路线 CTA 改为 tint + 墨色，不再白字叠浅绿/浅金；Cmd-K 遮罩改 `--color-scrim`；门户补 `color-scheme`。

### 发现的问题

- 宇宙物理 / 宇宙学 / 生命科学仍锁暗，不跟手记翻纸。
- 图谱与历史图谱的力导向画布仍是夜空像素。
- 历史时间线分类色块上的白字、色带上的数据标仍保留（字在色块上）。

### 验证结果

- Playwright 手记：25 个入口（门户、搜索、每日、哲/心/数/经/史/艺/文/工/医/化/计/政/社/法/地/语、概念图谱、思想树、历史图谱、知识图谱）`html.light`，`body` 为 `rgb(44, 42, 38)`，背景 `rgb(250, 246, 239)`。观测台 `/philosophy` 仍为奶油字 `#e6e3da` 叠深底。
- `pnpm prepush`：172 files / 1363 tests 全绿。未 commit。

### 下一步

- 真机 Safari 双主题与全站 axe。用户口令再标文学 launched 或写入 B4。

### 任务清单变更

- 手记主题第二波：学科 token、图谱浅字、历史图谱 chrome 已修

---

## 2026-09-06 会话 #325 — 薄文加深并收口提交

**执行人**：Grok
**任务**：继续内容和站点：加深三篇最薄核心文，然后按用户口令提交工作区全部改动。

### 完成内容

- 加深 `content/engineering/machines/automation-and-industry.mdx`：拆硬接线长段，补 PLC 扫描周期、IEC 62443 / 61511、灯塔网络 2018→238 的可核对口径，以及震网与安全仪表分层。
- 加深 `content/arts/media/performance-and-body-art.mdx`：补《节奏 0》《剪裁乐章》《艺术家在场》《十二平方米》的制度与记录问题，拆跨域长句。
- 加深 `content/literature/narrative-basics/what-is-a-story.mdx`：补拉波夫口语骨架、《桃花源记》反例与普罗普功能表的界限。

### 发现的问题

- 工程/艺术这两篇仍低于织入目标 3800 汉字，只是从约 2300–2500 抬上来；未在本轮硬拉到 4800。
- 文学仍不标 launched：矩阵里没有 B4 宗教学候选。

### 验证结果

- `pnpm gen-all`：wiki 3222 slugs，搜索 2869 文档。
- `pnpm check-content`：0 error / 0 warning。
- `pnpm prepush`：172 files / 1363 tests 全绿。

### 下一步

- 用户口令再标文学 launched 或写入 B4。真机 Safari / 全站 axe 仍待办。

### 任务清单变更

- 工程自动化、行为艺术、文学入门三篇已加深
