# 工作日志全文 — 2026-09-11 至 2026-09-12

会话 #381–#412。主日志只留相位摘要。

---

## [2026-09-11] 会话 #381 — 宗教学实验室加厚 + 文章动手看

**执行人**：Cursor Grok 4.6
**任务**：内容轮之后转阅读体验：把四个宗教学实验室做成能玩的教学装置，并在文章末放实验室邀请卡。

### 完成内容

- 仪式实验室：毕业典礼 / 朝圣结构 / 婚礼位置转移三例，仍是范热内普三步，不写操作。
- 示意地图：点选传统出注解与内链；现代带子补上无宗教身份（链到皮尤 RLS 前沿）。
- 经典开篇：公有领域原文上加「谁在说话 / 它声称什么 / 体裁」三镜。
- 世俗化图：美国皮尤 RLS 与西欧 2018 分样本；参与用 2023–24 频率切片（标明电话→在线不可比）；认同用 78/71/62；制度特权改成法律事实而非假百分比。
- 宗教学文章与前沿文末增加「动手看」卡（`ReligionLabInvite`）。前沿页把 religion 纳入跨域面板。
- `a11y-scan` 补上经典开篇比较器与世俗化指标两页。
- `e2e/smoke.spec.ts` 增加宗教文章 → 仪式实验室路径（桌面+移动）。

### 发现的问题

- 艺术/工程/文学加厚仍在 stash，未验收。
- 真机 INP / Safari / 全站 axe 仍未测。未宣称四实验室全量 axe 通过。

### 验证结果

- `pnpm prepush` 退出 0：**1386 tests / 175 files**（本轮实验室 + 邀请卡测试）。
- `pnpm build` 退出 0。`bundle-check --skip-build`：共享 JS 144.7 / 180；文章 JS 162.4 / 220；首页 RSC **84.1 / 95**；搜索 526.5 / 640 brotli；路由 CSS 44.9 / 48。
- 生产 `pnpm start :3000`：四个实验室、仪式文章、正典文章、前沿 `nones-plateau` 均为 200；文章 SSR 含「动手看 / 打开实验室」。系统 Chrome Playwright：朝圣×阈限出现 communitas、现代×无宗教身份出现 29%、道德经×体裁出现对句与悖论、西欧样本出现十五国中位、制度特权出现英格兰国教会；仪式文章邀请卡进入 `/religion/ritual-lab`。
- `pnpm test:e2e:smoke`：16 passed（桌面+移动），含新加的宗教实验室路径。未装 Playwright 自带 chromium，故未跑全量 `a11y-scan`。

- 已 push `e5c65e94`。CI 全绿并完成生产部署：https://github.com/Supwils/episteme/actions/runs/34592327534

### 下一步

- 不要把 `religion-studies` 标 launched。stash 里的艺术/工程/文学加厚另议。真机 INP / 全站 axe 仍待测。

### 任务清单变更

- B4 四实验室改为已加厚；a11y 清单补两实验室。宗教候选未 launched。

## [2026-09-11] 会话 #382 — 沉浸式阅读冲刺（本地分支）

**执行人**：Cursor Grok 4.6
**任务**：内容纠正、交互缺陷、文章水印 SVG、实验室邀请与文学前沿；工作留在 `feat/immersive-sprint`，不提交、不推送。

### 完成内容

- 拆掉仅剩的三处 run-on（API 设计、海平面变化、食物网）。`check-content` 现为 0 error / 0 warning。
- 文学补两篇 2020s 前沿：`llm-training-corpus-litigation`（训练语料诉讼，写成未决而非判决）、`computational-literary-studies-replication`（计算文学研究复现之争）。CJK 2730 / 2636。图谱 literature 38/38。
- 宗教学 / 文学 / 艺术 / 工程文章与前沿文末统一「动手看」卡（`DomainLabInvite`）。社会板块默认改请世界地图；仪式 SVG 与示意点可键盘激活。
- 文学地图、颜料剖面、艺术交流线改为可点 SVG（aria-label 与芯片按钮不撞名）。
- 引擎域文章页头加静态水印 motif（`DomainHeroMotif` watermark，尊重现有 reduced-motion 全局规则，无新增 CSS `@import`、无文章路由 GSAP）。
- 四域 `[slug]` `generateMetadata` 补 `alternates.canonical`。搜索正文命中显示领域名。
- 冒烟补文学文章 → 叙事图谱路径。

### 发现的问题

- 未从互联网引入第三方 SVG 文件（权利管线要求走 `content-assets/images/`）；本次用已有域内 inline SVG。
- 未加 Lenis / 文章页 GSAP，以免文章 JS 与路由 CSS 预算。
- 真机 INP / Safari / 全站 axe 仍未测。Playwright 冒烟 18 passed（桌面+移动），含宗教邀请→仪式实验室与文学邀请→叙事图谱。
- 不要把 `religion-studies` 标 launched。stash 里的艺术/工程/文学加厚仍未验收。

### 验证结果

- `pnpm check-content`：0 error / 0 warning；前沿 151 篇。
- `pnpm typecheck` / `pnpm lint --max-warnings 0` / `pnpm test`：1390 tests / 175 files。
- 图谱、连续体、候选、语言学、图像权利、跨域审计通过。文学 38/38、宗教 36/36。
- `pnpm build` + `bundle-check --skip-build`：共享 JS 144.7 / 180；文章 JS 162.4 / 220；首页 RSC 84.1 / 95；路由 CSS 44.9 / 48；搜索 528.3 / 640 brotli。
- `pnpm audit-rendering` 通过。生产抽查 200；HTML 含 canonical、水印、动手看。`pnpm test:e2e:smoke` **18 passed**（桌面+移动）。

### 下一步

- 用户回来后在 `feat/immersive-sprint` 上 commit（不要直接 push main）。
- 可选：commit 前再扫一遍 git status；不要 push main。
- 不要把 `religion-studies` 标 launched。

### 任务清单变更

- B3 文学改为 38 篇（含 2 篇前沿）并已接实验室邀请。
- 内容总量 2748；图谱 3133；搜索 2907。宗教候选未 launched。

## [2026-09-11] 会话 #383 — stash 织入回工作树 + 已上线域收口

**执行人**：Cursor Grok 4.6
**任务**：把宗教轮隔离的艺术/工程/文学加厚织入回工作树，并收口已上线三域的实验室清单与冒烟；不标 `religion-studies` launched，不写 B5，不提交。

### 完成内容

- `git stash apply` 恢复 105 篇 MDX（艺术 / 工程 / 文学）。复算后三域非前沿仍全部在 3800–4800，前沿 ≥1800，没有再补薄文。验收后 `git stash drop`。
- `scripts/a11y-scan.mjs` 补齐已上线实验室：工程结构/材料/芯片、艺术细节比较器/颜料/交流地图/空间探索器、文学世界地图/译本比较器。`home-domain-catalog` 加回归。
- 建筑文章「动手看」改指 `/arts/space-explorer`（原先误指透视实验室）。
- 冒烟补艺术透视文章 → 透视实验室、工程电网文章 → 电网潮流（桌面+移动）。

### 发现的问题

- 本机默认 `node` 曾是 v25.9.0，Vitest 里 `window.localStorage.clear` 不是函数（`--localstorage-file` 无有效路径）。门禁用 `.nvmrc` 的 Node 22。
- 不要把 `religion-studies` 标 launched。真机 INP / Safari / 全站 axe 仍未测。

### 验证结果

- `pnpm check-content`：0 error / 0 warning。
- `pnpm prepush`：1391 tests / 175 files。图谱 arts 58/58、engineering 54/54、literature 38/38、religion 36/36。
- `pnpm audit-cross-domain` 通过。`pnpm build` + `bundle-check --skip-build`：共享 JS 144.7 / 180；文章 JS 162.4 / 220；首页 RSC 84.1 / 95；路由 CSS 44.9 / 48；搜索 528.3 / 640 brotli。
- `pnpm audit-rendering` 通过。生产 `:3000` 抽查艺术/工程文章与实验室均为 200，HTML 含「动手看」与 canonical。`pnpm test:e2e:smoke` **22 passed**（桌面+移动）。

### 下一步

- 用户回来后在 `feat/immersive-sprint` 上 commit（不要直接 push main）。
- 不要把 `religion-studies` 标 launched。

### 任务清单变更

- 已上线三域织入从 stash 回到工作树；a11y 清单覆盖四域全部实验室。
- 单测 1391；冒烟 22。宗教候选未 launched。

## [2026-09-11] 会话 #384 — 代码审查收口 + 引擎文章 canonical

**执行人**：Cursor Grok 4.6
**任务**：完整审查 `feat/immersive-sprint` 未提交工作，并继续阅读体验扩展。不标 `religion-studies` launched，不提交。

### 完成内容

- 删除死代码 `ReligionLabInvite`。邀请记录类型与解析集中到 `lib/lab-invite.ts`；`DomainArticle` / 前沿页一律渲染 `DomainLabInvite`（无实验室的域返回 null）。
- 新 SVG 按钮补键盘聚焦环（`SvgLabFocusRing`），并加 SVG 与芯片不撞名的测试。
- 工程前沿邀请按主题分流：水泥/氢还原铁→材料，电池/钙钛矿→电网，数字孪生→结构，机器人可靠性→芯片。默认不再一律请电网实验室。
- `engineArticleMetadata` / `frontierArticleMetadata` 铺到引擎文章、分析页与全部 `frontier/[slug]`。`withCanonicalPath` 改为合并既有 `alternates`。
- 文学入门里误留的英文 “overlapping” 改成「重叠」。

### 发现的问题

- 审查无 Critical。哲学人物、物理对话等非引擎详情仍无这套 canonical helper。
- 真机 INP / Safari / 全站 axe 仍未测。不要把 `religion-studies` 标 launched。

### 验证结果

- `pnpm typecheck` / `pnpm lint --max-warnings 0` 通过。
- `pnpm test`：**1397 tests / 176 files**。
- 改正文后已 `pnpm gen-all`。本轮未重跑生产构建（canonical helper 与 SVG 聚焦环不新增路由 CSS `@import`，不改共享首载形状）。

### 下一步

- 用户回来后在 `feat/immersive-sprint` 上 commit（不要直接 push main）。
- 可选：把 canonical helper 接到哲学/物理等非引擎详情页。

### 任务清单变更

- 引擎文章 canonical 从四域扩到引擎+前沿+分析页。
- 单测 1397 / 176。宗教候选未 launched。

## [2026-09-11] 会话 #385 — 非引擎 canonical、宗教 launched、B5、axe/Safari/INP

**执行人**：Cursor Grok 4.6
**任务**：把哲学人物、物理对话等非引擎详情页接入 `withCanonicalPath`；把 `religion-studies` 标 launched 并写入 B5 人类学候选；在生产构建上跑全站 axe、WebKit 冒烟与实验室 INP。

### 完成内容

- 47 个非引擎 `generateMetadata` 页（含思想家、物理对话、阅读路线、知识汇流）包上 `withCanonicalPath`。覆盖测试钉住全部 `generateMetadata` 页。
- `religion-studies` 写入 `LAUNCHED_SUBJECT_CANDIDATE_IDS`。新增候选 `anthropology-archaeology`（3.80 分）与 `docs/人类学立项口径.md`。推荐下一项现为人类学/考古学。
- `a11y-scan` 清单加上哲学人物页。修文学/宗教暗色强调金、思想家时代色、历史时间线按钮、手记主题 `--color-bg-panel` / `--color-fg-primary` 被 Tailwind `@theme` 压过的问题。
- 新增 `pnpm test:e2e:webkit` 与 `pnpm inp-probe`。Lighthouse 表增加 INP 列（导航模式为 n/a，属预期）。

### 发现的问题

- 手记主题下 `bg-bg-panel` 曾落到观测台面板色，浮动目录对比度失败。根因是 Tailwind v4 `@theme` 后置覆盖分层 remap；已在文件末尾未分层块给表面/前景 token 加 `!important`。
- 本机 Lighthouse `/knowledge-graph` LCP 三次约 16s（确认轨迹仍失败）。与本次 CSS 无关（改 token 前已如此），不提预算。
- 实验室 Event Timing 是桌面引擎代理，不是手机 CrUX。

### 验证结果

- `pnpm prepush`：typecheck / lint / check-content 0/0 / 四项审计 / **1398 tests / 176 files**。
- `pnpm build` + `bundle-check --skip-build`：共享 JS 144.7 / 180；文章 JS 162.4 / 220；首页 RSC 84.1 / 95；路由 CSS 44.9 / 48；搜索 528.3 / 640 brotli。`audit-rendering` 通过。
- `pnpm a11y-scan`：66 页 × 2 主题 × 2 视口，serious/critical 0。
- `pnpm test:e2e:webkit`：8 passed（桌面 Safari + iPhone 13）。
- Lighthouse：`/` 97、思想家 95、物种 93、板块边界 92；图谱 LCP 失败如上。INP 列 n/a。
- `pnpm inp-probe`：Chromium 门户搜索 64ms、苏格拉底 16ms；WebKit 搜索 16ms、苏格拉底 32ms、仪式实验室 16ms。

### 下一步

- 用户回来后在 `feat/immersive-sprint` 上 commit（不要直接 push main）。
- B5 可按 Playbook 先在社会学试板块；不要创建 `app/anthropology` 除非用户拍板。
- 手机 CrUX / 低端真机 INP 仍未测。

### 任务清单变更

- 非引擎详情页 canonical 收口；`religion-studies` launched；B5 人类学为推荐下一项。
- 单测 1398 / 176。全站 axe 与 WebKit 冒烟有回执。

## [2026-09-11] 会话 #386 — 人类学「比较与伦理」六篇

**执行人**：Cursor Grok 4.6
**任务**：撰写 `content/anthropology/comparison-and-ethics/` 六篇已发布 MDX（田野笔记与再现、发掘记录、口头传统与历史、取样与缺失、民族志知情同意、协作与原住民考古）。

### 完成内容

- 六篇均 `status: published`、`updated: 2026-09-11`、`category: 比较与伦理`。只写已出版笔记/报告/规范/项目名称与其所引发的争论；不写田野笔记作法、发掘步骤、访谈脚本或采集。
- 跨域节各 5 条、均以粗体 wiki 领头，节内汉字 517–564。同域链到已有 `what-is-culture`、`ethnography-as-description`、`emic-and-etic`、`stratigraphy-and-context`、`nagpra-and-repatriation` 等。

### 发现的问题

- 同域 wiki 在 `pnpm gen-links` 之前不会进入 `WIKI_LINK_INDEX`；需等本域其余文章落盘后由父任务跑 `pnpm gen-all`。
- 未单独跑 `check-content` / `audit-cross-domain`（索引未重生）。

### 验证结果

- 本地汉字计数（全文 / 跨域节）：见下表。六篇均 ≥2200，跨域节均 ≥400。未 commit。

| slug                                 |  CJK | 跨域节 |
| ------------------------------------ | ---: | -----: |
| fieldnotes-and-representation        | 3903 |    534 |
| excavation-records                   | 3262 |    549 |
| oral-tradition-and-history           | 3094 |    547 |
| sampling-and-absence                 | 3062 |    517 |
| informed-consent-in-ethnography      | 3434 |    564 |
| collaborative-indigenous-archaeology | 3215 |    548 |

### 下一步

- 父任务在全部人类学 MDX 齐备后跑 `pnpm gen-all`，再 `check-content` 与 `audit-cross-domain anthropology`。

### 任务清单变更

- 未改 `docs/任务清单.md`。

## [2026-09-11] 会话 #387 — B5 人类学上线并收口沉浸式冲刺

**执行人**：Cursor Grok 4.6
**任务**：按 Playbook 上线独立 `/anthropology`；对整次 run 做代码审查；commit、本地 merge 进 `main`、push 部署。

### 完成内容

- 独立域 `/anthropology`：36 篇板块 MDX + 4 篇 frontier；跨域节 40/40 达标（中位约 501 汉字）。四实验室（亲属称谓、物质分布示意、年代标尺、互惠）挂首页与文章「动手看」。
- 注册链：`DOMAINS` / `new-domains` / taxonomy / frontier / continuum / 图谱脊柱 `anthropology-nodes.ts`；候选 `anthropology-archaeology` launched；B6 教育学写入矩阵与 `docs/教育学立项口径.md`。
- 加厚 10 篇偏短跨域节；Open Context 2026 引文已核对为真实 _Internet Archaeology_ 71。
- 沉浸式冲刺其余部分一并收口：canonical helper、实验室邀请、WebKit 冒烟、axe 清单含新域。

### 发现的问题

- `audit-learning-continuum` 把「每学科一个 L1 起点」硬编码为 20，21 域时会红；已改为跟 `establishedDomainCount`。
- 人类学首发按 2200 下限，尚未织到 3800。每日知识网格未纳入人类学（Playbook 阶段 7 可选）。
- 本机 3000 端口被旧生产进程占用，验证改走 3010。未见浏览器自动化，路由用 curl 对生产构建抽查。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；`audit-cross-domain` anthropology 40/40/40。
- `pnpm prepush`：1404 tests / 177 files。图谱 anthropology 40/40、连续体 21 established。
- `pnpm build` + `bundle-check --skip-build`：共享 JS 144.7/180；首页 RSC 86.3/95；路由 CSS 44.9/48；搜索 532.4/640 brotli。`audit-rendering` 通过。
- 生产 `localhost:3010`：`/`、`/anthropology`、入口文、NAGPRA、古 DNA 前沿、四实验室均为 200；入口文含 canonical 与跨域节。

### 下一步

- 教育学立项（B6），或人类学织入 3800，或真机 CrUX。
- 不要把每日知识漏加当成阻塞；用户若要日历覆盖再补。

### 任务清单变更

- B5 `/anthropology` 已上线；`anthropology-archaeology` launched。
- `RECOMMENDED_SUBJECT_CANDIDATE` 改为教育学。
- 规模：21 域 · 2788 篇 · 3173 图谱节点 · 2947 搜索文档 · 1404 单测。

## [2026-09-11] 会话 #388 — 修人类学 push 后的 CI，再部署

**执行人**：Cursor Grok 4.6
**任务**：run `34617242298` quality+build 红、deploy 被跳过；修索引幂等与首页 Lighthouse 抖动后再次 push `main`。

### 完成内容

- `pnpm gen-all` 重生 `public/link-previews/literature.json`：文学前沿首句 `_Critical Inquiry_` 与 Linux CI excerpt 对齐。未给 excerpt 剥下划线斜体（会误伤 `w_i` / `M_\odot`）。
- Lighthouse 首次超预算后再确认 **两次**（最多 3 条 trace），首页 TBT 预算仍为 250ms。对照：宗教上线绿跑首页首 trace 也是 73 / ~989ms。
- 首页 metadata「二十个领域」改为「二十一个领域」。
- 冒烟 `e2e/smoke.spec.ts` 无 JS 门户卡数改为 `COVERAGE_DOMAIN_COUNT`（21），并断言 `/anthropology` 卡存在。质量、包体、Lighthouse 已绿；卡在写死的 20 张卡。

### 发现的问题

- 生成索引幂等失败只有 `literature.json` 一处；把 `_italic_` 写进 excerpt 剥离会破坏公式下标。
- 首页 TBT 失败更像 GitHub runner 冷启动，不是第 21 张 DomainCard 的包体回归（共享 JS 仍 144.7/180）。

### 验证结果

- `pnpm gen-all` 后工作区仅有本次手改；再跑一遍无新 diff。
- `vitest` `scripts/performance/performance-budget.test.mjs` 10/10。

### 下一步

- 教育学立项（B6），或人类学织入 3800，或真机 CrUX。

### 任务清单变更

- 决策记录第 2 条补：确认次数改为 2，不提首页 TBT 预算。
- B5 已随 `6932f9fd` 生产部署（Actions `34619154291` quality / build / deploy 全绿）。

## [2026-09-11] 会话 #389 — 本地 `predeploy` 对齐 CI

**执行人**：Cursor Grok 4.6
**任务**：让内容/前端轮次在 push `main` 前本地拦住 CI quality+build 会红的那几类问题。

### 完成内容

- 新增 `scripts/predeploy.sh` 与 `pnpm predeploy`：`gen-all` 后工作区必须干净 → `prepush` → `build` → `audit-rendering` → `bundle-check --skip-build` → 3069 端口 Lighthouse → `CI=1` 冒烟。
- **没有**挂进 husky：构建和 TBT 不该挡住 `docs/` push。
- 写入 `CLAUDE.md` / `AGENTS.md` / `docs/CI-CD与渲染策略.md` / `docs/工程原则.md` / `README.md`。

### 发现的问题

- 上次部署红的三处（索引幂等、首页 TBT、冒烟 20 张卡）都不在 `prepush` 里。
- Lighthouse 仍可能因 runner 抖动在云端偶发；本地绿不能 100% 保证云端首 trace，但确认次数已是 2。

### 验证结果

- `bash -n scripts/predeploy.sh` 通过。完整 `pnpm predeploy` 需工作区已提交；本轮脚本本身未 commit，故未跑全集。

### 下一步

- 用户授权后 commit `predeploy` 相关文件。之后每次内容/前端 push `main` 前先 `pnpm predeploy`。

### 任务清单变更

- 部署前本地门禁从「prepush + 有时 build」改为明确的 `pnpm predeploy`。

## [2026-09-11] 会话 #390 — B6 教育学试写（壳 + 六篇脊柱）

**执行人**：Cursor Grok 4.6
**任务**：按任务清单下一优先开始 B6 `/education`：引擎注册、路由薄壳、图谱脊柱、六篇试写 MDX。不标 launched，不 commit。

### 完成内容

- `lib/new-domains.ts` 增加 `EDUCATION`（六板块，无实验室、无 frontier）；`DOMAINS` / `APP_URLS` / 连续体 / coverage-meta / 搜索 / 图谱颜色与 taxonomy 同步到第 22 域。
- `app/education/` 薄壳：layout、globals、首页、error/not-found，以及六个板块的 list + `[slug]`（`generateStaticParams` 空数组）。
- 策展脊柱 `subjects/knowledge-graph/data/education-nodes.ts`：L1 `what-is-learning` → L5 `pisa-is-not-civilization`，前置与边成对。
- 六篇 published MDX（≥2200 汉字，跨域节 6/6/6，中位约 431）。教育技术板块暂空。
- `audit-learning-continuum` 不再把「已建立学科数」写死为 21。
- 连续体 planner 测试上限 175 KB → 185 KB（实测 177.9 KB）。

### 发现的问题

- 新域入 coverage-meta 后，`audit-learning-continuum` 若仍硬编码 21 会红；应跟 `COVERAGE_DOMAIN_META` 的 established 计数。
- 试写六篇就会抬 planner JSON；不要把这条测试上限当成包体预算。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning。
- `audit-cross-domain education` 6/6/6；`audit-graph-coverage` education 6/6；连续体审计通过。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1405 / 177 files** 全绿。
- 本地 3067：`/`、`/education`、六篇脊柱与空的教育技术列表均为 200；入口文含 canonical、跨域节与已解析 wiki。

### 下一步

- 用户授权后再 commit（本轮还叠着未提交的 `predeploy` 脚本）。
- 不要把 `education-learning-sciences` 标 launched，直到满 launchGate（约 36 篇 + 可视化）。
- 下一内容轮：补教育技术与其余板块，或给教育学加实验室。

### 任务清单变更

- B6 `/education` 试写已创建；候选仍为 recommended。
- 规模：22 域 · 2794 篇 · 3179 图谱节点 · 2953 搜索文档。

## [2026-09-12] 会话 #391 — B6 教育学补到每板块三篇

**执行人**：Cursor Grok 4.6
**任务**：按「continue deeply」把 `/education` 从六篇脊柱扩到每板块至少三篇（12 篇新文），并登记教育技术。不标 launched，不 commit。

### 完成内容

- 十二篇 published MDX（均 ≥2200 汉字，跨域节达标）：`school-is-an-institution`、`working-memory-and-load`、`metacognition-is-a-judgment`、`classroom-as-time`、`tracking-and-grouping`、`formative-assessment-in-use`、`funding-formulas-and-teachability`、`what-edtech-records`、`adaptive-systems-are-not-tutors`、`teacher-labor-and-platforms`、`comprehensive-school-is-a-design`、`colonial-curriculum-and-language`。
- 登记 `education-technology` 到 `derived-node-taxonomy.ts`（L5、technology 类型）、`content-reachability-audit.ts`、`check-content.ts` 的字数/行数下限。
- 覆盖矩阵里的北欧综合学校、殖民课程/语言、经费与可教性已写入比较/政策与评估篇，未写成文明本质。
- 已有六篇脊柱的 `related` 接到新 slug。

### 发现的问题

- 跨域节用「句号+空格」接下一句会被 `check-content` 标成 run-on；同一行多句应写成 `。下一句`，不要 `。 下一句`。
- 歧义 slug `welfare-state` 不能写成 `[[political-science:welfare-state]]`，索引键仍是 `welfare-state`。

### 验证结果

- `pnpm gen-all`；`pnpm update-graph-snapshot`；`pnpm check-content` 0 error / 0 warning。
- `audit-cross-domain education` 18/18/18，节字中位 425；`audit-graph-coverage` education 18/18。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1405 / 177 files** 全绿。
- 本地 3067：领域首页、六板块列表与十二篇新文均为 200；教育技术篇 wiki 链解析到隐私工程、机器学习、算法管理、问责、确认偏误。

### 下一步

- 用户授权后再 commit（工作树仍叠着未提交的 `predeploy` 与试写）。
- 不要把 `education-learning-sciences` 标 launched。下一内容轮可再补 8–12 篇（东亚考试制度、全球南方学校、教师教育等），或开始实验室。

### 任务清单变更

- B6 `/education` 18 篇 published；候选仍为 recommended。
- 规模：22 域 · 2806 篇 · 3191 图谱节点 · 2965 搜索文档。

## [2026-09-12] 会话 #392 — B6 教育学补到每板块五篇

**执行人**：Cursor Grok 4.6
**任务**：继续把 `/education` 从 18 篇扩到 30 篇，补全球覆盖缺口。不标 launched，不 commit。

### 完成内容

- 十二篇 published MDX（均 ≥2200，跨域节达标）：教师教育、作业与家庭时间、样例与自我解释、必要难度、课例研究、双语教育、高利害考试、效度论证、学习分析、生成式模型、条件现金与就学、撒哈拉以南学校扩张。
- 全球覆盖六簇均已有专文：东亚考试、北欧综合学校、撒哈拉以南普及、拉美条件现金、南亚殖民课程、原住民/双语。
- 未写刷题/作弊/监视步骤；生成式篇不写提示词教程或侦测操作。

### 发现的问题

- 开写仍偏短，需在跨域节用「句号后直接接下句」补足汉字，避免 `。 ` 触发 run-on。
- 歧义 slug 仍不能写成 `domain:slug` 当索引键。

### 验证结果

- `pnpm gen-all`；`pnpm update-graph-snapshot`；`pnpm check-content` 0 error / 0 warning。
- `audit-cross-domain education` 30/30/30，节字中位 432；`audit-graph-coverage` education 30/30。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1405 / 177 files** 全绿。
- 本地 3067：十二篇新文均为 200。

### 下一步

- 用户授权后再 commit。
- 不要标 launched。下一轮：再约 6 篇到 36，并开始候选可视化（遗忘/间隔示意、课堂互动结构、测验分数分解、自适应路径）。

### 任务清单变更

- B6 `/education` 30 篇 published；候选仍为 recommended。
- 规模：22 域 · 2818 篇 · 3203 图谱节点 · 2977 搜索文档。

## [2026-09-12] 会话 #393 — B6 教育学凑满 36 篇并开工实验室

**执行人**：Cursor Grok 4.6
**任务**：不用 commit。把 launchGate 剩下的 6 篇写完，并做四个可视化实验室（间隔示意、课堂互动、分数分解、自适应路径）。不标 launched。

### 完成内容

- 六篇 published MDX（每板块第 6 篇，均 ≥2200，跨域节达标）：`early-childhood-is-an-institution`、`randomized-trials-in-education`、`vocational-schooling-is-a-track`、`inclusive-education-and-disability`、`open-resources-are-not-neutral`、`shadow-education-is-a-market`。
- 四实验室：`/education/spacing-lab`、`/education/classroom-talk-lab`、`/education/score-decomposer`、`/education/adaptive-path-lab`。接到 `EDUCATION.tools`、文章末「动手看」、`a11y-scan`、webkit 冒烟（间隔练习键盘可达）。
- 实验室只做示意：不是背词器、课堂话术、真实测量工具或监视设计器。分数分解页写明数字不能解释真实学生。

### 发现的问题

- 开写仍偏短，需在「争议」前补独段、跨域节用「句号后直接接下句」凑满 2200/400，避免 `。 ` 触发 run-on。
- 未把 `education-learning-sciences` 写入 `LAUNCHED_SUBJECT_CANDIDATE_IDS`。

### 验证结果

- `pnpm gen-all`；`pnpm update-graph-snapshot`；`pnpm check-content` 0 error / 0 warning。
- `audit-cross-domain education` 36/36/36，节字中位 433；`audit-graph-coverage` education 36/36。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1414 / 179 files** 全绿。
- `audit-learning-continuum` / `audit-subject-candidates` / `audit-linguistics-foundation` / `audit-image-rights` 通过。候选审计仍把教育学列为 recommended next launch。
- 本地 3067：领域首页、六篇新文、四个实验室、记忆篇「动手看」均为 200。

### 下一步

- 用户授权后再 commit（工作树仍叠着未提交的 `predeploy` 与完整 B6）。
- 用户明确说上线后再把 `education-learning-sciences` 标 launched。不要自行 push。
- 教育学与人类学首发篇仍按 2200 下限，尚未织到 3800；无 frontier。

### 任务清单变更

- B6 `/education` 36 篇 published + 四实验室；候选仍为 recommended。
- 规模：22 域 · 2824 篇 · 3209 图谱节点 · 2983 搜索文档。

## [2026-09-12] 会话 #394 — 阅读路线覆盖四新域，文章页可进入路线

**执行人**：Cursor Grok 4.6
**任务**：B6 内容轮之后转阅读体验。补齐 `/read` 对文学、宗教、人类学、教育学的脊柱路线，并让任意章节文章在没有 `?path=` 时也能邀请「跟着读」。不扩建账号/进度系统，不标 launched，不 commit。

### 完成内容

- `/read` 新增四条路线：`from-story-to-canon`、`from-religion-to-secularization`、`from-culture-to-repatriation`、`from-learning-to-comparison`（教育学 7 章，其余各 5 章，均落在已有 published 文章上）。
- `ReadingPathBar`：无查询参数时，若当前路径已是某章，底栏显示「这篇也在阅读路线里」；点入后仍走原有 `?path=&step=` 上一篇/下一篇。实验室等非章节页不出现。
- 单测覆盖章节反查与邀请/激活两种底栏。WebKit 冒烟增加教育学路线邀请。

### 发现的问题

- 未标 launched。人类学/教育学仍低于 3800 织入目标。
- 连续体 planner / 图谱专题游览未改；本轮只接现有 `/read` 层。

### 验证结果

- `pnpm typecheck`、相关 eslint、`pnpm test` **1419 / 180 files** 全绿。
- 本地 3067：`/read` 与四条路线页均为 200。Chromium 冒烟：从「什么是学习」点邀请进入 `path=from-learning-to-comparison`，目录链可见。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一件可做：织人类学/教育学到 3800、给教育学补 frontier，或真机 CrUX。

### 任务清单变更

- `/read` 覆盖全部 22 域中缺席的四新域；阅读底栏可从章节进入路线。
- 单测 1419 / 180 files。

## [2026-09-12] 会话 #395 — 教育学前沿 + 阅读路线织入 + frontier 与 DOMAINS 对齐

**执行人**：Cursor Grok 4.6
**任务**：不用 commit。补教育学 4 篇 2020s 前沿；把阅读路线 7 章织到 3800；把 `FRONTIER_DOMAINS` 收成与 `DOMAINS` 可测对齐。内容下一轮转向理工，不织人类学、不开新人文域。不标 launched。

### 完成内容

- 架构：`education` 写入 `FRONTIER_DOMAINS` / `FRONTIER_DOMAIN_CONFIG`；薄路由 `app/education/frontier/page.tsx` 与 `[slug]/page.tsx`；taxonomy 与 reachability 加上 `frontier`。首页目录测试断言排序后的域 id 等于前沿清单，并检查每个域都有列表页和详情页。
- 四篇 frontier（均 ≥1800，跨域节 ≥400）：`covid-learning-loss-is-not-one-curve`、`science-of-reading-statutes`、`generative-assessment-after-2022`、`ai-act-and-education-inference`。核对了世界银行 2022/2024、Betthäuser 2023 DOI、NCTQ 2024、UNESCO 2023 指南与不到 10% 机构政策、EU 2024/1689 与 2026/1744 改期。Alabama HB9 写成未走完的法案，不是现行法。禁止提示词/侦测/监考步骤。
- 阅读路线 7 章织到 3800–3906 汉字：`what-is-learning`、`teaching-is-not-pouring`、`memory-spacing-and-transfer`、`classroom-as-time`、`curriculum-as-selection`、`assessment-as-inference`、`pisa-is-not-civilization`。其余 29 篇板块 MDX 仍约 2200。

### 发现的问题

- 未标 launched，未 commit。
- 人类学未织 3800（按用户：下一轮理工）。
- 没有做大重构：22 份 `app/<domain>/frontier` 薄文件保留，用守卫代替合并。

### 验证结果

- `pnpm gen-all`；`pnpm update-graph-snapshot`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `audit-cross-domain education` 40/40/40，节字中位 433；`audit-graph-coverage` education 40/40。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：前沿列表、四篇新文、三篇织入后的阅读路线章均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：物理 / 化学 / 工程 / 生命等理工深化，不开 B7 人文学科。
- 若继续教育学：把剩余 29 篇板块 MDX 织到 3800。

### 任务清单变更

- B6 `/education` 40 篇 published（36 MDX + 4 frontier）；候选仍为 recommended。
- 规模：22 域 · 2828 篇 · 3213 图谱节点 · 2987 搜索文档。
- 决策记录第 12 条：`FRONTIER_DOMAINS` 必须与 `DOMAINS` id 对齐。

## [2026-09-12] 会话 #396 — 加深 CS 两篇前沿：零知识证明 / 可持续计算

**执行人**：Cursor Grok 4.6
**任务**：加深两篇计算机科学前沿，目标约 3000–3400 汉字。不 commit。

### 完成内容

- `content/computer-science/frontier/zero-knowledge-proofs.md`：补 EIP-4844 / Dencun blob 空间如何改写 rollup 账单；Groth16 电路专属可信设置对照 STARK 透明哈希；证明成本与验证成本分工（递归聚合）。未写攻击步骤。`updated: 2026-09-12`。正文 **3356** 汉字。
- `content/computer-science/frontier/sustainable-computing.md`：新增 PUE 对有用功的盲区、反弹分层（直接/间接/经济体）、公司自报 ≠ 独立电表。TWh 仍只用 IEA 2025 的约 415 / 约 945。`updated: 2026-09-12`。正文 **3389** 汉字。

### 发现的问题

- 未跑 `gen-all`（仅改两篇已有 frontier 正文；索引由后续内容轮统一重生）。
- 未 commit。

### 验证结果

- `pnpm check-content`：0 error / 0 warning；前沿 159 篇通过。

### 下一步

- 用户授权后再 commit。内容改动后的 `pnpm gen-all` 可并入下一轮。

### 任务清单变更

- 无新任务号；两篇 CS 前沿加深完成。

## [2026-09-12] 会话 #397 — 教育学 29 篇织到 3800；理工前沿加深一轮

**执行人**：Cursor Grok 4.6
**任务**：把教育学剩余 29 篇板块 MDX 织到 3800–4800，然后加深物理 / 化学 / 工程 / 生命 / 计算机前沿。不 commit，不标 launched，不织人类学。

### 完成内容

- 教育学 36 篇板块 MDX 现全部 **3801–4249** 汉字（含先前已完成的 7 章阅读路线）。29 篇在核心/争议补有出处独段：学校语法与义务教育时间、学前 Starting Strong / 佩里样本限制、作业作为家庭时间转移、洛蒂观察学徒制与 NCTQ 倡导测量、必要难度与间隔、元认知判断校准等。未改跨域 slug 清单。
- 工程 6 篇 frontier 补 2025–2026 核实段（LKAB 许可待投资、Oxford PV 质保路线、QuantumScape Eagle Line 中试等），汉字约 2091–2182，仍按 1800 下限而非 4800。
- 化学：`chemical-recycling-plastics`（Carbios 2026-08-03 财务关闭未达成；PPWR 第 7 条条件日期）、`self-driving-labs`（AlabOS ~3500 样品 ≠ 新化合物）、`pfas-destruction`。
- 物理：`muon-g2-self-correction`（PRL / 127 ppb 分层）、`fusion-ignition-era`（2025-04-07 8.6 MJ；2026-06-20 第 11 次点火）、`dark-matter-direct-detection`（LZ PRL 2025）、`quantum-error-correction`。
- 生命：`brain-computer-interfaces`（Neuralink 公司 21 人 vs NCT06429735 估计 15）、`de-extinction`、`aging-plasticity`、`xenotransplantation`（EXPAND ≠ 同情用药个案）。
- 计算机：`ai-for-science`（AIFS Single 2025-02-25 / ENS 2025-07-01 / v2 2026-05-12）、`inference-time-compute-and-reasoning`；另见会话 #396 的 ZK / 可持续计算。

### 发现的问题

- 未标 launched，未 commit。
- 工程前沿仍偏薄（~2100），值得下一轮继续按装置/许可/中试口径加深，不要写成已量产。
- 非前沿理工 MDX 已在 3800–4800，再织会顶上限。

### 验证结果

- `pnpm gen-all`（二次，避免子代理仍在写时索引漂移）；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `audit-cross-domain education` 40/40/40，节字中位 433；`audit-graph-coverage` education 40/40。
- `pnpm typecheck`、`pnpm lint` 通过。`pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：教育学首页与三篇织入文、化学回收、NIF 点火、BCI、AI for Science、氢还原铁均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：继续工程/化学/物理/生命前沿加深（有来源的 2020s 事实，目标约 2800–3500，不必凑 4800）。
- 不要把 `education-learning-sciences` 标 launched，除非用户说上线。

### 任务清单变更

- B6 36 篇板块 MDX 全部 3800–4800；候选仍为 recommended。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #398 — 加深 CS 两篇前沿：AI for Science / 大语言模型

**执行人**：Cursor Grok 4.6
**任务**：加深 `ai-for-science` 与 `large-language-models`，目标 YAML 后 3000–3400 汉字。不 commit。

### 完成内容

- `content/computer-science/frontier/ai-for-science.md`：在「代价与争议」补预测≠验证；A-Lab 2026-01-19 作者更正（新颖=对预测平台新、40 项中 36 确认 / 4 项 XRD 不定、撤回训练数据中的 Zn₂Cr₃FeO₈）；AF3 权重 2024-11-11 非商用、非开源。未编 CASP 分或 TWh。`updated: 2026-09-12`。正文 **3107** 汉字。
- `content/computer-science/frontier/large-language-models.md`：基础模型侧补 C4 语料管道（Dodge 2021）、评测污染（Sainz 2023、GPT-4 技术报告、GSM1k）、开放权重≠开源（Llama 3 Community License 2024-04-18）。推理时扩展压缩为指向姐妹篇。未写越狱步骤。点明 2024 诺贝尔化学奖不是 LLM。正文 **3308** 汉字。

### 发现的问题

- 未跑 `gen-all`（仅改已有 frontier 正文）。
- 未 commit。

### 验证结果

- `pnpm check-content`：0 error / 0 warning；前沿 159 篇通过。

### 下一步

- 用户授权后再 commit。`pnpm gen-all` 可并入下一内容轮。

### 任务清单变更

- 无新任务号；两篇 CS 前沿加深完成。

## [2026-09-12] 会话 #399 — 工程 6 篇前沿收到约 2800；化学/计算机 4 篇收到约 3100–3400

**执行人**：Cursor Grok 4.6
**任务**：用户「请继续」。教育学 36 篇 MDX 已在 3800 口径内。本轮把偏薄理工前沿按许可/中试/质保口径加深，目标约 2800–3500 汉字，不必凑 4800。不 commit，不标 launched，不织人类学。

### 完成内容

- 工程 6 篇 `content/engineering/frontier/`：许可≠投资（LKAB 耶利瓦勒上限吨数、600 MW 电解是规划配套）、热压块铁物流、废钢稀释 vs 初生铁、Oxford PV 质保窗口（Gen2 十年；Gen3 寿命目标会改）、QuantumScape Eagle Line / PowerCo 2026-07-16 里程碑上限约 7540 万美元、ISO 23247 不能抄到桥梁、水泥工艺排放 vs 燃料、具身机器人危险失败表。禁止施工/配制/拆解步骤。正文 **2805–2864** 汉字。`updated: 2026-09-12`。
- 化学：`self-driving-labs.md` **3196**（样品≠发现；2026-01-19 更正 36/40、撤回 Zn₂Cr₃FeO₈）；`chemical-recycling-plastics.md` **3393**（PET 解聚 vs 聚烯烃热解；实施决定 (EU) 2026/1425 只管一次性饮料瓶，不是 PPWR 第 7 条）。无实验操作步骤。
- 计算机：见会话 #398；本轮把 `gen-all` 一并跑完。`ai-for-science` **3107**，`large-language-models` **3308**。

### 发现的问题

- 未标 launched，未 commit。
- 理工前沿仍低于约 2800 的主要是数学（球堆积 2207 起）与宇宙学（中微子质量 2211 起），以及地球碳封存、医学 GLP-1。物理/生命本会话未再加厚。
- 不要把人文域前沿（语言学/法学/艺术/宗教约 1800）拉进下一轮，除非用户改口。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：氢还原铁、钙钛矿叠层、固态电池、化学回收、自驱动实验室、AI for Science、大语言模型均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：数学/宇宙学最薄前沿，或物理/生命若仍要按 2800 口径再核一遍。不开新人文域，不织人类学。

### 任务清单变更

- 工程 6 篇前沿约 2805–2864；化学/计算机 4 篇约 3107–3393。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。
- 候选 `education-learning-sciences` 仍为 recommended。

## [2026-09-12] 会话 #400 — 数学/宇宙学最薄 6 篇前沿收到约 2800

**执行人**：Cursor Grok 4.6
**任务**：用户「请继续」。工程/化学/计算机上一轮已加厚。本轮加深仍低于约 2800 的数学与宇宙学前沿。不 commit，不标 launched，不织人类学。

### 完成内容

- `sphere-packing-and-codes.md` **2827**：格 vs 非格量词；Klartag 椭球过程是存在性；_Inventiones_ 244 (2026)；Abuya–Gargava–Zhao 只在无穷多维上加 $\log\log$。存在性下界 ≠ 格密码已破。
- `ramsey-and-combinatorics-progress.md` **2833**：书算法打破的是贪心底数，不是 $R(5)$；$r(4,t)$ 量级 ≠ 下一个具体数。
- `kakeya-conjecture.md` **2805**：IHES 任职改为 2025-09-01；补 2026 年 7 月菲尔兹奖。维数定理 ≠ 限制猜想；奖杯 ≠ 期刊接收函。
- `neutrino-mass-cosmology.md` **2813**：膨胀史上限 vs 全形状；DESI 指南 0.0642 / 放开暗能量约 0.163 eV；KATRIN 不是质量之和；CMB-S4 于 2025-07-09 停资。
- `k2-18b-atmosphere.md` **2853**：3σ 不是粒子物理发现阈值；hycean 假说 ≠ 海洋探测；甲基简并与截面外推。
- `interstellar-objects.md` **2815**：离心率是来自太阳系外的证件不是技术签名；IAWN 演习 ≠ 威胁；3I 已过近日点。

### 发现的问题

- 未 commit。教育学候选仍为 recommended。
- 理工前沿仍薄的主要是几何朗兰兹、JWST 早期星系、DESI 暗能量、解析数论、地球碳封存等。不要把语言学/法学/艺术约 1800 的人文前沿拉进下一轮，除非用户改口。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：上述六篇路由均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：几何朗兰兹 / JWST 早期星系 / DESI 暗能量，或地球碳封存。不开新人文域。

### 任务清单变更

- 数学/宇宙学最薄 6 篇约 2805–2853。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #401 — 几何朗兰兹 / 解析数论 / 欧拉奇点 / JWST / DESI / 地质碳封存收到约 2800

**执行人**：Cursor Grok 4.6
**任务**：用户「好的 请继续」。把仍低于约 2800 的六篇理工前沿加深到约 2800–3400。不 commit，不标 launched，不织人类学。

### 完成内容

- `geometric-langlands.md` **2814**：无分歧、特征零曲线上的范畴等价 ≠ 数域朗兰兹；穿刺与函数域翻译仍开放；八百页验证靠分工，接缝处最危险。
- `analytic-number-theory.md` **2803**：Guth–Maynard 已刊 _Annals_ 203 (2026)；改进的是零点密度（Huxley $12/5$ → $30/13$），不是黎曼假设；张益唐 arXiv:2211.02515 截至 2026-09 仍无修订版。
- `euler-singularity.md` **2812**：Chen–Hou PNAS 2025-07 钉在带光滑边界的三维轴对称欧拉；不是纳维–斯托克斯千禧题；区间算术 ≠ Lean。
- `jwst-early-galaxies.md` **2810**：MoM-z14 光谱红移 $14.44\pm0.02$（OJAp 2026-01）；单天体 ≠ 推翻 ΛCDM；亮端超额误差棒仍宽。
- `desi-dark-energy.md` **2986**：3.1σ 仍依赖早期声视界 $r_d$；尺子刻错可以伪装成 $w(a)$ 在动；中微子质量之缝与暗能量纠缠，不要合成一句改写。
- `carbon-storage-geology.md` **2956**：Northern Lights 2025-08-25 首批入 Aurora，一期 1.5 Mt/年已订满；二期 FID 目标至少 5 Mt/年；订满 ≠ 气候规模。无注入操作步骤。
- 六篇 `updated: 2026-09-12`。未改跨域 slug 清单。

### 发现的问题

- 未 commit。教育学候选仍为 recommended。
- STEM 前沿仍低于 2800 的只剩 `ml-guided-mathematics.md`（2762）与 `glp1-incretin-obesity.md`（2797）。不要把语言学/法学/艺术约 1800 的人文前沿拉进下一轮，除非用户改口。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：上述六篇路由均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：机器学习辅助数学、GLP-1，或纳赫兹引力波等刚过 2800 的理工前沿。不开新人文域。

### 任务清单变更

- 六篇约 2803–2986。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #402 — 用户离开后长跑（上）：八篇理工前沿收到约 2946–3042

**执行人**：Cursor Grok 4.6
**任务**：用户离开前授权继续深写并拉长跑。先把仍刚过或略低于 2800 的八篇理工前沿加深到约 2800–3400。不 commit，不标 launched，不织人类学。

### 完成内容

- `ml-guided-mathematics.md` **3013**：Davies 人机相关、FunSearch/AlphaEvolve 可打分构造、AlphaProof Lean 核验三条路分开；IMO 银牌建立在专家赛后手工形式化 + 多日计算。
- `glp1-incretin-obesity.md` **3010**：SURMOUNT-5 开放标签、礼来资助；FLOW 用的是 1.0 mg 糖尿病剂量不是 Wegovy 2.4。无开药步骤。
- `nanohertz-gravitational-waves.md` **3006**：Hellings–Downs 是角指纹不是来源清单；IPTA-DR3 是 2026 会议综述里的数据集构造，不是 5σ 公报。
- `cmb-inflation-tests.md` **2946**：SO 2025-03 首光是火星图不是 \(r\) 后验；CMB-S4 2025-07-09 停资。
- `hubble-tension.md` **3042**：JWST 划掉拥挤场，没有把 73 与 67 合成一个数。
- `ai-weather-climate-models.md` **2953**：AIFS 与 IFS 并列不是替换；气候归因要反事实。
- `green-hydrogen.md` **3019**：IEA GHR 2025：宣布 ≠ FID ≠ 开车。无电解液/施工步骤。
- `formal-verification.md` **3013**：与 ML-数学文分工，钉内核验证范围。

八篇 `updated: 2026-09-12`。未改跨域 slug 清单。

### 发现的问题

- 未 commit。教育学候选仍为 recommended。
- `pnpm gen-all` 之后若再改正文，必须重跑 `gen-links` + `gen-search-index`，否则 phrase corpus 测试会红。
- 不要把语言学/法学/艺术约 1800 的人文前沿拉进下一轮，除非用户改口。

### 验证结果

- `pnpm gen-links` + `gen-search-index`（天气文最后一处合并后补跑）；随后完整 `pnpm gen-all`。
- `pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：上述八篇路由均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：尚未在当天加深、约 2900 出头的理工前沿。不开新人文域。

### 任务清单变更

- 八篇约 2946–3042。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #403 — 用户离开后长跑（下）：六篇约 2913–2986 的理工前沿收到约 3069–3220

**执行人**：Cursor Grok 4.6
**任务**：STEM 已全部 ≥2800。继续加深当天尚未改过、仍在 2800–3000 的六篇：AI 形式化证明、气候临界点、后量子密码、阿秒物理、中微子质量顺序、行星防御。不 commit，不标 launched，不织人类学。

### 完成内容

- `ai-formal-proof.md` **3220**：IMO 2025 Gemini Deep Think 35/42 是自然语言金牌，不是 Lean 内核核验；与 2024 AlphaProof 银牌拆开。Mathlib 23 万（Lean Finder, 2025-10）与约 28.8 万（社区统计页, 2026-09）计数口径不同。
- `climate-tipping-points.md` **3175**：GTP 2025-10-13 把暖水珊瑚写成正在越过热力临界，是评估不是讣告；冰岛 2025-11-12 列入国家安全议程 ≠ AMOC 已停摆。SPG 与 AMOC 不是同一开关。
- `post-quantum-cryptography.md` **3111**：HQC 2025-03-11 选定（IR 8545），到 2026-09 仍无 FIPS；FN-DSA/FIPS 206 仍在草稿管道。选定 ≠ 可买的正式标准。
- `attosecond-physics.md` **3130**：19.2 as 是检索脉宽（Ardana-Lamas, Ultrafast Science）；Kuschel et al. Nat. Commun. 16:847 (2025) 是氙纳米粒子衍射亮度，不是器件已经改完设计。
- `neutrino-mass-hierarchy.md` **3101**：JUNO _Nature_ 654 (2026-06-10) 未报告 \(|\Delta m^2\_{31}|\) 与质量顺序；JHEP 04 (2026) 089 是独立探测性拟合。Hyper-K 衬里 2026-07-31 完工 ≠ 开始读符号。
- `planetary-defense.md` **3069**：Hera 截至 2026-09 仍在巡航，2–3 月深空机动 \(\Delta v\approx 367\) m/s，抵达提前至 11 月；质量未测。NEO Surveyor CDR 2025-02-06，发射不早于 2027-09。

六篇 `updated: 2026-09-12`。未改跨域 slug 清单。

### 发现的问题

- 未 commit。教育学候选仍为 recommended。
- STEM 前沿目前全部 ≥2800；尚未在当天加深、约 3000 出头的还有计算材料设计、碳捕集化学、阿尔茨海默免疫、单细胞图谱、量子算法等。
- 不要把语言学/法学/艺术约 1800 的人文前沿拉进下一轮，除非用户改口。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm test` **1421 / 180 files** 全绿。
- 本地 3067：上述六篇路由均为 200。

### 下一步

- 用户授权后再 commit。上线教育学需明确口令。
- 下一内容轮：计算材料 / 碳捕集化学 / 阿尔茨海默免疫，或用户改口后再动人文前沿。不开新人文域。

### 任务清单变更

- 六篇约 3069–3220。STEM 前沿全部 ≥2800。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #404 — 教育学标 launched；五篇约 3000 出头的理工前沿收到约 3154–3224

**执行人**：Cursor Grok 4.6
**任务**：按用户口令把 `education-learning-sciences` 写入 `LAUNCHED_SUBJECT_CANDIDATE_IDS`，并加深当天尚未改过、约 3000 出头的五篇理工前沿。不 commit。

### 完成内容

- 候选矩阵：`education-learning-sciences` 已 launched。`RECOMMENDED_SUBJECT_CANDIDATE` 允许为 `undefined`；池空是刻意的，不要发明 B7。`audit-subject-candidates` 打印 `Recommended next launch: none`。
- 文档收口：`docs/任务清单.md`、`docs/教育学立项口径.md`、`docs/聚焦方向.md`、`docs/学科版图与导航架构.md` 不再写「待确认 launched」。
- 五篇理工前沿（`updated: 2026-09-12`，未改跨域 slug）：
  - `computational-materials-design.md` **3154**：MatterGen 4 选 1；Rietveld 是无序 TaCr₂O₆；目标 200 GPa、实验约 158±11。A-Lab 2023 是 57→36，不是把 GNoME 38 万烧完。补回 Butler 2018。
  - `carbon-capture-chemistry.md` **3164**：Mammoth 铭牌约 3.6 万吨；公司称 2026 上半年约 675 吨。不写吸附剂配方。
  - `alzheimer-amyloid-immunotherapy.md` **3165**：皮下维持 2025-08、皮下起始 2026-07-14；Kisunla 欧盟 2025-09-24 限 ApoE ε4 非纯合子。不写注射步骤。
  - `single-cell-human-cell-atlas.md` **3171**：Nature 2024-11 专辑不是完整第一稿；STAT 2026-06 仍写年底计划。
  - `quantum-algorithms.md` **3224**：Willow 面码低于阈值 ≠ Shor；RCS 是人工基准。

### 发现的问题

- 无。候选矩阵已全部 launched。

### 验证结果

- `pnpm gen-all`；`pnpm check-content` 0 error / 0 warning；前沿 159 篇通过。
- `pnpm typecheck` / `pnpm lint` 绿。
- `pnpm test` **1421 / 180 files** 全绿。
- `pnpm audit-subject-candidates`：教育学 `[launched]`；下一推荐 none。
- 四项知识审计与图像审计绿。图谱 education 40/40。
- 本地 3067：`/education` 与上述五篇均为 200，正文含 MatterGen / Mammoth / LEQEMBI / 还不是完整 / Willow。

### 下一步

- 用户授权后再 commit。不要发明 B7。不要把语言学/法学/艺术约 1800 的人文前沿拉进理工加深轮。不开新人文域，也不织人类学。

### 任务清单变更

- `education-learning-sciences` 已 launched；候选池空。
- 五篇约 3154–3224。STEM 前沿全部 ≥2800。
- 规模仍为 22 域 · 2828 篇 · 159 篇前沿 · 2987 搜索文档。

## [2026-09-12] 会话 #405 — 七项增量加固（输入边界、搜索 href、Markdown URL、阅读路线 a11y）

**执行人**：Cursor Grok 4.6
**任务**：用户要求持续小范围打磨。完成 7 项独立改进。不 commit。

### 完成内容

- **P1** `/api/learning-targets`：`q` 截到 120 字（与搜索/前沿一致）；`id` 超过 200 直接 404。
- **P2** `/api/og`：title ≤200、description ≤120；未知 `section` 不再把任意字符串画进图。
- **P3** CI `e2e/smoke.spec.ts` 补教育学文章 → 间隔练习实验室，与宗教/文学/艺术/工程同口径。
- **P4** 阅读路线激活态补 `nav` 地标；窄屏上一篇/下一篇有 `aria-label`。
- **P5** 搜索结果（弹层 + `/search`）只接受站内相对路径，拒绝 `//host` 与 `javascript:`。
- **P6** `/search` 元数据用 `DOMAINS.length`，不再写死「19 个学科」。
- **P7** Markdown `[text](href)` 与图片 `src` 只允许站内路径、hash、http(s)。

### 发现的问题

- Satori 仍把 OG `zIndex: 1` 写成 `1px` 并告警；改字符串无效，属库行为，未再动。
- `package.json` / `docs/项目总览.md` 仍写 twenty-one / 21 个领域（非用户可见主路径）。
- 锁文件幽灵 importer `apps/portal` 的 Next advisory：按既有决策不手工剪 lockfile。
- 新 smoke 需生产 `next start`（3068）；本轮用 3067 核对了教育学实验室邀请链路。

### 验证结果

- `pnpm typecheck` 绿。
- `pnpm test` **1432 / 181 files** 全绿。
- 本轮改动文件 eslint 0 warning。

### 下一步

- 用户授权后再 commit。
- 可选：`package.json` 领域数文案；手机 CrUX / 搜索主线程解析；OG Satori zIndex 告警等库侧问题。不要发明 B7。

### 任务清单变更

- 单测口径改为 1432 / 181。

## [2026-09-12] 会话 #406 — 路径包含、阅读路线 pathname、reduced-motion

**执行人**：Cursor Grok 4.6
**任务**：三份只读排查落地：内容 loader 路径包含、阅读路线激活态、部分 a11y。不 commit。

### 完成内容

- 路径包含：`isPathInsideRoot` 用 `root + path.sep`，堵住 `kb` vs `kb-extra` 兄弟前缀。铺到 `knowledge-base` / `knowledge-domain` / `frontier` / `generic-kb` / `species-prose`。`getNarration` 拒绝含 `..` `/` `\` 的段，再做根目录包含检查。
- 阅读路线：href 剥尾斜杠；Active 条以 pathname 落在 `path.steps` 为准，实验室带 `?path=` 不再显示错误进度。Chromium `smoke.spec.ts` 补教育学阅读路线 invite。
- `seededSelect` 空池抛错，不再 `items[NaN]`。连续体 planner 超预算时带上实际字节数。
- a11y：`Reveal` / 图谱 tooltip 尊重 `prefers-reduced-motion`；相关内容与 ConnectedNodes 箭头在 `focus-visible` 下也出现。

### 发现的问题

- `/daily` 仍无教育学 facts（人类学同缺）。未做：会动 snapshot 与每日页字段。
- 哲学/经济/心理学等专题 loader 仍有裸 `startsWith(dir)`，本轮只改通用引擎那几处。
- 人类历史 `innerHTML`、KaTeX `maxSize`、CSP nonce 未动。

### 验证结果

- `pnpm typecheck` 绿。
- 改动文件 eslint 0 warning。
- `pnpm test` **1437 / 182 files** 全绿。

### 下一步

- 用户授权后再 commit。不要发明 B7。
- 可选：教育学每日知识卡；专题 loader 同样换成 `isPathInsideRoot`。

### 任务清单变更

- 单测口径改为 1437 / 182。

## [2026-09-12] 会话 #407 — 内容路径守卫解耦为共享 API

**执行人**：Cursor Grok 4.6
**任务**：工程质量 / 可维护性 / 解耦。把各域 loader 复制的 slug + `startsWith` 守卫收进 `lib/content-paths.ts`；哲学死副本改再导出。不 commit。目标仍进行中。

### 完成内容

- 共享 API：`isSafeContentSegment` / `resolveContentPath` / `existingContentFile`（含 `path.sep` 根目录包含）。
- 迁入：哲学 `lib/{mdx,concepts,schools,isms,experiments,dialogues}`、通用引擎、frontier、KB、讲解、物种散文、经济/心理/生命科学/数学 loaders。
- 解耦：`subjects/philosophy/lib/{mdx,concepts,schools,isms,experiments,dialogues,types,cross-references}` 改为再导出 `@/lib/*`，去掉第二套实现（其中 cross-references 副本还曾用错误的相对路由）。

### 发现的问题

- 数学五个 loader 仍各自复制 cache + gray-matter 形状；心理学已有 `getBySlug` 助手，下一刀可收那一层，不要再复制路径守卫。
- `/daily` 仍无教育学 facts。人类历史 `innerHTML`、KaTeX 上限未动。

### 验证结果

- `pnpm typecheck` 绿。
- 改动文件 eslint 0 warning。
- `pnpm test` **1443 / 182 files** 全绿。

### 下一步

- 用户授权后再 commit。不要发明 B7。
- 可继续：数学 MDX loader 抽成与心理学同构的助手；教育学每日知识卡。

### 任务清单变更

- 单测口径改为 1443 / 182。

## [2026-09-12] 会话 #408 — 内容列举也收进共享路径层

**执行人**：Cursor Grok 4.6
**任务**：把各域 loader 复制的 `readdir + endsWith(.mdx)` 收进 `listContentSlugs` / `existingContentArticle`。不 commit。

### 完成内容

- `lib/content-paths.ts` 新增 `listContentFilenames` / `listContentSlugs` / `existingContentArticle`：最长扩展名优先，跳过 `*.narration.md`。
- 哲学 / 数学 / 经济 / 心理 / 生命科学 / 通用引擎 / 前沿 / 跨引用列举都改走这套。嵌套 KB 与讲解仍用多段 `existingContentFile`（本来就不是扁平 slug）。

### 发现的问题

- 图谱派生节点扫描（philosophy-nodes 等）和 `scripts/valid-routes.ts` 仍各自 readdir，不在内容 loader 热路径上。
- 数学五个文件仍复制 cache + gray-matter 形状，那是解析层不是路径层。

### 验证结果

- `pnpm typecheck` 绿。
- 改动文件 eslint 0 warning。
- `pnpm test` **1444 / 182 files** 全绿。

### 下一步

- 用户授权后再 commit。不要发明 B7。

### 任务清单变更

- 单测口径改为 1444 / 182。

## [2026-09-12] 会话 #409 — 内容读取层收进 content-article

**执行人**：Cursor Grok 4.6
**任务**：工程质量 / 可维护性 / 解耦。把各域复制的 cache + `readFile` + gray-matter 收进 `lib/content-article.ts`；图谱扫描去掉已不存在的 monorepo root。不 commit。

### 完成内容

- 新建 `lib/content-article.ts`：`loadContentBySlug`（缺文件不缓存；解析/map 失败缓存 null）、`loadAllContent`、`readContentEntries`。gray-matter 不进 `content-paths.ts`。
- 哲学 `lib/{mdx,concepts,schools,isms,experiments,dialogues}`、数学五个 loader、经济 `readMdxFile`、心理 `getBySlug`/`getAll` 改走共享读取；对外函数名不变。
- 图谱 `philosophy-nodes` / `psychology-nodes` / `economics-nodes` 改 `getDomainContentDir` + `readContentEntries`；删掉找 `pnpm-workspace.yaml` 的 `findMonorepoRoot`，以及哲学对 `apps/portal/content/philosophy/dialogues` 幽灵路径的拼接。

### 发现的问题

- 生命科学 / frontier / knowledge-base / cross-references / search articles 仍各自 gray-matter，下一刀可迁。
- 三份图谱 `extractDescription` 仍重复；`scripts/valid-routes.ts` 仍各自 readdir。
- `/daily` 仍无教育学 facts。人类历史 `innerHTML`、KaTeX 上限未动。

### 验证结果

- `pnpm prepush` 全绿（typecheck · lint · check-content 0/0 · 五项审计 · test）。
- `pnpm test` **1448 / 183 files** 全绿。

### 下一步

- 用户授权后再 commit。不要发明 B7。
- 可继续：生命科学与通用引擎读取层迁入 `content-article`。

### 任务清单变更

- 单测口径改为 1448 / 183。

## [2026-09-12] 会话 #410 — 生命科学 / frontier / KB / 跨引用读取解耦

**执行人**：Cursor Grok 4.6
**任务**：把生命科学、frontier、knowledge-base、cross-references 各自 gray-matter 收进 `lib/content-article.ts`。不 commit。目标完成。

### 完成内容

- `content-article` 增补 `parseMatter`（strict / safe / lenient）、`readContentBySlug`、`readParsedFile`、`recoverBrokenFrontmatter`。`content-utils.safeParseMatter` 改为再导出 `parseMatterSafe`。
- 生命科学 dialogues / scientist-article / event-detail 改 `loadContentBySlug`。
- frontier 改 `readContentEntries` / `readContentBySlug`（safe，坏 YAML 仍出篇）；摘录/标题助手改走已有 `content-utils`。
- 人类历史嵌套 KB 改 `readParsedFile(..., "lenient")`，walkDir 与 SKIP_FILES 不动。
- 哲学 cross-references 改 `readContentEntries`；缺 title 时仍回退到 `` `${slug}.mdx` ``。

### 发现的问题

- 运行时 lib 里 gray-matter 只剩 `content-article.ts`；`lib/search/articles.ts` 与 `scripts/gen-*` 仍直接解析，属索引管道不是这篇热路径。
- `knowledge-domain` / `generic-kb` / 讲解仍是 `safeParseMatter` + 自己 `readFile`，下一刀可收。
- `/daily` 仍无教育学 facts。不要发明 B7。

### 验证结果

- `pnpm prepush` 全绿。
- `pnpm test` **1452 / 183 files** 全绿。

### 下一步

- 用户授权后再 commit。
- 可选：通用引擎与 search articles 也改走 `readContentBySlug`。

### 任务清单变更

- 单测口径改为 1452 / 183。

## [2026-09-12] 会话 #411 — 通用引擎读取层收进 content-article

**执行人**：Cursor Grok 4.6
**任务**：把 knowledge-domain、generic-kb、generic-dialogues、narration、species-prose 的 `readFile` + `safeParseMatter` 收进共享读取层。不 commit。

### 完成内容

- `knowledge-domain` / `generic-dialogues` / `species-prose` 改 `readContentEntries` / `readContentBySlug`（safe）。
- `generic-kb` 与讲解改 `readParsedFile(..., "safe")`；嵌套 walk 与 `*.narration.md` 路径守卫不动。
- 运行时文章 loader 不再直接 `safeParseMatter`；`content-utils` 仍再导出以保持旧入口。

### 发现的问题

- `lib/search/articles.ts` 与 `scripts/gen-*` 仍直接 gray-matter（索引管道）。
- 三份图谱 `extractDescription` 仍重复。`/daily` 仍无教育学 facts。不要发明 B7。

### 验证结果

- `pnpm prepush` 全绿。
- `pnpm test` **1452 / 183 files** 全绿。

### 下一步

- 用户授权后再 commit。
- 可选：search articles / gen-\* 改走 `parseMatter`；或收图谱 `extractDescription`。

### 任务清单变更

- 单测口径保持 1452 / 183。

## [2026-09-12] 会话 #412 — 每日知识补上教育学/人类学；KaTeX 膨胀上限

**执行人**：Cursor Grok 4.6
**任务**：诚实评估平台规模与下一步；修访客能撞上的缺口。不 commit。

### 完成内容

- `/daily` 补上教育学、人类学事实卡（各 6 条，链到三字段文章，不是域首页）。`DailySelected`、分享文案、`DailyDomainGrid` 同步 22 域。
- 教育学卡图标用 `▤`，避免与宗教学的 `◎` 撞车。
- `MarkdownRenderer` 的 KaTeX 加上 `maxSize: 500` / `maxExpand: 1000`，公式宏循环不再能挂住渲染。
- 表征快照与路由断言已更新。

### 发现的问题

- 首页已是 22 域 launched，每日知识此前只有 20 条事实车道——这是访客能直接看见的不一致，不是内容缺失。
- 真正的产品缺口仍是厚薄不均（工程 54 / 法学 57 vs 哲学 358），不是再开 B7。
- 没有发现会砸 200/崩溃的文章路由；低端设备 CrUX 仍未测。

### 验证结果

- `pnpm prepush` 全绿：typecheck / lint / check-content 0/0 / 五项审计 / **1455 tests / 183 files**。
- 开发机 `localhost:3067/daily` 200，页面含「自适应系统在选下一步」「过去由谁主张」；对应两篇详情 200。

### 下一步

- 用户授权后再 commit。不要发明 B7。交互加深走已有实验室与 `/read`，不要做账号/进度。
- 若下一轮要「更好学」，优先让实验室从文章页更容易被点到，而不是新学科。

### 任务清单变更

- 单测口径改为 1455 / 183。
- `/daily` 现为 22 域事实卡。
