# 任务清单 — Universe Knowledge 平台

> 最后更新：2026-08-03
> 架构：仓库根目录单一 Next.js 15 应用 + `content/`（内容）+ `subjects/`（领域前端）
>
> **本文件只登记「在办与待办」。** 已完成任务按阶段浓缩在文末；逐次细节见 `工作日志.md`，更早的全文见 `archive/`。

---

## 当前状态

17 个知识领域 · **2479 篇内容** · 325 条路由 · 1049 单测（132 文件） · 知识图谱 1788 节点 / 240 骨架 · 反链边 13026

| 领域     | 篇数   | 领域     | 篇数   | 领域           | 篇数   |
| -------- | ------ | -------- | ------ | -------------- | ------ |
| 哲学     | 351    | 政治学   | 173    | 医学与公共卫生 | 120    |
| 心理学   | 219    | 数学     | 169    | 化学           | 98     |
| 经济学   | 195    | 计算机   | 179    | 地球科学       | 84     |
| 宇宙物理 | 173    | 人类历史 | 168    | 社会学         | 58     |
| 宇宙学   | 163    | 生命科学 | 132    | 语言学         | 51     |
| **法学** | **34** | **艺术** | **40** | **工程与技术** | **36** |

> 篇数为 `content/` 下 `.md`/`.mdx` 实测（排除 `*.narration.md` 与 `CREDITS.md`）；搜索索引 2635 文档 = 2445 文章 + 190 实体。哲学较上轮 -2 为同题文章合并（`T-CONTENT-44`），非内容损失——合并后两篇分别为 6579 / 8219 汉字。6 篇 human-history 编辑元文档（非知识内容）已从搜索/wiki 索引与详情路由排除（2026-08-01 会话 #217）。

`pnpm dev` → localhost:3000。部署经 GitHub Actions（Quality → Build → Vercel Deploy 三级门禁），push `main` 即触发生产部署。

---

## 🔵 在办 / 待办

| 任务 ID      | 优先级 | 状态   | 交付范围                                                                                                                          |
| ------------ | ------ | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| `T-VIZ-ENG`  | P3     | 待开始 | **工程 4 个可视化**（电网潮流模拟器、结构受力实验室、材料性能剖面、芯片制造流程图——`lib/engineering-subject-plan.ts` 的承诺件）。 |
| `T-VIZ-ARTS` | P3     | 待开始 | **艺术 5 个可视化**（细节比较器、透视实验室、颜料剖面、交流地图、空间探索器——`lib/arts-subject-plan.ts` 的承诺件）。              |

`T-SUBJECT-LAW`（法学 34 篇，#230–#231，已随 f1212498 提交）、`T-SUBJECT-ARTS`（艺术 40 篇，#232，已随 a29eecf1 提交）、`T-VIZ-LAW`（法学 3 个可视化）与 `T-SUBJECT-ENG`（工程与技术 36 篇 + 图谱集成，#233）均已完成并全量验证通过，见下文「已完成」。

下一学科按路线图为 **B3 文学与叙事**（候选已于 #233 入矩阵，4.00 分，`RECOMMENDED_SUBJECT_CANDIDATE` 已指向它）；launchGate：确立「事实层与解释层分离表述」的写作规范与引文版权口径。

`T-IMAGE-01`、`T-CONTENT-48`（#228，prepush 全绿待 commit）与 `T-INFO-01`（学科版图与导航架构，#229）已于 2026-08-03 完成并全量验证通过，见下文「已完成」。新学科接入路线（法学 → 工程与技术 → 文学与叙事 → 宗教学 → 人类学 → 教育学）见 `docs/学科版图与导航架构.md` 第五节，待用户拍板后立项。

`T-IMAGE-01`（图像权利管线）与 `T-CONTENT-48`（化学/地球科学深度轮，+3 篇）（#228）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`–`T-UX-14`、`T-TEST-02`、`T-CONTENT-46`（已随此前 commit 提交）、`T-UX-15`（引擎域「最近更新」条）与 `T-CONTENT-47`（社会学/语言学扩面第四批，+4 篇）（#227）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`–`T-UX-09`、`T-TEST-02`（已随 2ac1352b 提交）、`T-UX-10`/`T-UX-11`（#224）、`T-UX-12`（打印样式）/`T-UX-13`（静态 chips 可交互化）（#225）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`/`T-UX-06`（#220/#221）、`T-TEST-02`/`T-UX-07`（#222）、`T-UX-08`（触屏 wiki 预览）/`T-UX-09`（正文 h1 全局裁定）（#223）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`/`T-UX-06`（会话 #220/#221）、`T-TEST-02`（渲染完整性守卫）与 `T-UX-07`（数学渲染器统一）（会话 #222）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`（阅读体验与领域页面深化，会话 #220）、`T-UX-06`（渲染器收敛与排版细节，会话 #221）已于 2026-08-02 完成并全量验证通过，见下文「已完成」。

`T-UX-05`（阅读体验与领域页面深化）已于 2026-08-02 完成并全量验证通过（会话 #220），见下文「已完成」；路由 CSS 预算按决策记录第 1 条预设触发条件提至 48 KB（已同步改写该条）。

`T-CONTENT-43`（社会学/语言学扩面第一批，+6 篇）已于 2026-08-02 完成（会话 #218）；`T-CONTENT-44`（哲学两对同题文章合并）、`T-CONTENT-45`（社会学/语言学扩面第二批，+4 篇）已于 2026-08-02 完成并全量验证通过（会话 #219），均见下文「已完成」。

**上一批在办任务已于 2026-08-01 全部清零**（会话 #217，含此前多会话遗留的未提交成果一并验证收口）：`T-PERF-05`、`T-TEST-01`、`T-CONTENT-36`、`T-CONTENT-39`、`T-DOC-01`、`T-HISTORY-04`、`T-GRAPH-12`、`T-UX-04`、`T-TOUR-03`。各任务结论见下文「决策记录」与「已完成」。

---

## 📌 决策记录（已拍板，作为后续执行依据）

1. **性能预算：路由 CSS 已按预设触发提至 48 KB**（2026-08-02，`T-UX-05`）。40 KB 时代收官水位：共享首载 144.6/180 KB · 路由 CSS 39.8/40 KB · 搜索索引 515.5/560 KB brotli · 单块 179.4/285 KB。**触发依据**：六个领域（economics/medicine/chemistry/earth-science/computer-science/philosophy）的 layout 引入 katex.min.css（+~2.6 KB gzip）——132 篇含公式文章此前经 MarkdownRenderer 服务端渲染 KaTeX 却无样式表，公式呈无排版 HTML；备选方案（运行时注入样式表）会把预算红线换成每页公式 FOUC。提额后水位 42.4/48 KB。其余预算不变：索引超 90% → 提至 ~640 KB；**大数据 barrel 守卫**：`lib/__tests__/big-data-barrel.test.ts` 从 493 个 `"use client"` 模块做 import 闭包遍历（含动态 import），带阳性对照防 vacuous pass。
2. **Lighthouse TBT 门禁维持现状**（2026-08-01）。`/knowledge-graph` 常态 219–255ms、预算 400ms；runner 间方差可达 3 倍属 CI 环境问题，脚本的 trace 确认机制已能过滤单次抖动。决策：接受偶发重跑，不提预算、不改 workflow。若日后失败率显著上升，再按 `T-PERF-05` 记录的 (b)/(c) 方案复议。
3. **dialogues 体裁豁免已统一**（2026-08-01）。虚构对话体一律豁免跨域连接节：economics 1 篇、cosmology 6 篇的旧式空句节已删除（两域标题变体归 1，达标数不变）；psychology 8 篇已按标准写达的保留（删除达标内容是破坏性回退）。三态问题消除。
4. **`T-CONTENT-39` 余 2 对同题文章：已于 2026-08-02 按预定解法完成内容层合并**（`T-CONTENT-44`，会话 #219）。philosophy-of-mind、utilitarianism 的 concepts/isms 份独家材料（内格尔蝙蝠、赖尔行为主义、玛丽的房间、现象概念策略、利贝特实验；西季威克、三大经典反驳、黑尔两层理论、EA 与气候折现等）已逐条核实后织入 schools 规范节点版（合并后 6579 / 8219 汉字），旧文件与 data 条目删除，3 处旧路由引用改为 wiki 链或改指 schools 路由。slug 冲突与 2 条 `check-content` 告警随之消除，`[[philosophy-of-mind]]`、`[[utilitarianism]]` 裸链接恢复解析（合并前因重复 slug 被索引整体剔除）。`PHILOSOPHY_CANONICAL_NODE_SOURCES` 映射保持不变（canonical 仍指向 schools 份）。
5. **`T-HISTORY-04` 结论：不拆**（2026-08-01）。事件/人物/时间线详情数据已按「时代 × 内容类型」分片到 `content/human-history/data/generated/` 并经 per-era 动态 import 按需加载；human-history 全部 19 页共享首载 180KB gzip 且历史数据为 0（预算 ≤200KB），`references.js`（40KB）的消费模式是按 key 跨时代解析，按时代拆只会打散查找表、给 4 个消费点增加合并逻辑。三个后续优化点登记在「已知问题」。
6. **篇幅衡量标准统一为汉字数主口径**（`T-DOC-01`，2026-08-01）：`聚焦方向.md` 已改写——MDX/KB ≥2200 汉字（分档见 `check-content.ts` 的 `MIN_CJK_CHARS`）或达到行数下限；frontier ≥1800 汉字或 ≥60 非空行。「双薄才告警」，`MIN_LINES` 保留为公式/表格密集型文章的替代通道。

---

## 🟢 可选扩展（P3，无排期）

国际化（英文）· 用户系统 · human-history Canvas→React 迁移 · Physics 3D 场景补全 · 更多非洲/拉美/日本历史事件 · sociology/linguistics 继续扩面（两批 +10 篇已落地，见 `T-CONTENT-43`/`T-CONTENT-45`；两域 54/47 篇仍为全平台最薄，可继续增补）

---

## 已知问题

| 问题                                     | 影响 | 说明                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `check-content` 不校验 wiki 链目标存在性 | 中   | Link Integrity 只查 CJK slug 编码往返，仍不查 `[[X]]` 目标是否存在；未解析的 `[[X]]` 由 MarkdownRenderer 降级为强调文本（刻意设计，不会 404），属可长期静默的质量信号。2026-08-01 已用 `.agent-tmp/scan-up-deadlinks.mjs`（与 `resolveWikiLink` 同一解析路径）把 universe-physics 域 17 个死链清零，该脚本可复用于其他域的定期扫查。                                       |
| e2e mobile 一例 flaky                    | 低   | `e2e/home-knowledge-continuum.spec.ts` mobile「loads each deep continuum dataset」：IntersectionObserver/lazy-load 请求计数行为本身不稳定（预存问题，两次运行失败点漂移），与聚合断言无关；CI 只跑 smoke.spec.ts，不受影响。                                                                                                                                               |
| human-history 数据后续优化点             | 低   | 来自 `T-HISTORY-04` 评估：(a) `event-relationships.ts` 可从 `EVENTS`（138KB）换 `EVENT_CATALOG`（27KB），首页 EventTimeline 异步 chunk 省 ~30KB gzip（需先比对 title 集合）；(b) 死代码 `figure-components.js`/`graph-panel.js`/`global-search.js` 及 figures 桶（~700KB 源码，仅测试引用）可清理；(c) `/scholarly` 178KB gzip 异步 chunk 可按批动态化。均为优化而非缺陷。 |
| `pages/timeline.css` 残留死选择器        | 低   | 仍有引用 `map-page`/`figures-page` 的既有死选择器（`T-PERF-05` 只清了 responsive.css 的 5 条），可随下次 CSS 工作顺带清理。                                                                                                                                                                                                                                                |
| 审计脚本会误判旁白稿                     | 低   | `content/computer-science/` 下有 6 个 `*.narration.md`（TTS 旁白，由 `pnpm gen-narration` 消费）。按文件统计篇幅的审计脚本必须排除 `*.narration.md`。                                                                                                                                                                                                                      |
| Mol\* 探测噪声                           | 低   | `localhost:9000` 探测属噪声，功能不受影响；RCSB 真实结构加载需在生产真实浏览器 + 网络下做最终冒烟。                                                                                                                                                                                                                                                                        |
| human-history Canvas 非 React            | 低   | 使用 PageWrapper + 原生 JS，功能正常。                                                                                                                                                                                                                                                                                                                                     |
| `reference/` 旧源码                      | 无   | 保留供查阅，已从 tsconfig 与 Vitest 排除，不参与构建与测试。**禁止自行删除。**                                                                                                                                                                                                                                                                                             |

---

## ✅ 已完成（按阶段浓缩）

> 共 160+ 项已完成任务。逐条记录（含文件路径与验证命令）见 `工作日志.md` 与 `archive/` 下的日志归档，此处只留阶段结论。

- **奠基与架构收敛**（2026-06-01 起）：平台骨架；从 Turborepo monorepo 收敛为单一 Next.js 15 应用；内容统一到 `content/<domain>/`。
- **16 个知识领域建成**：十大主域 + 医学与公共卫生、化学、地球科学、社会学、语言学；后接入者由通用领域引擎（`lib/knowledge-domain.ts` + `lib/new-domains.ts`）驱动。
- **部署 CI/CD 攻坚**（06-17）：修复 Vercel 15000 文件上限（高基数路由改按需 ISR）；改用 `vercel deploy --prebuilt --prod --archive`。复盘见 `复盘-从一次Vercel部署翻车到CICD最优解.md`。
- **知识图谱与学习编排**（07-06 → 07-12）：认知分层布局、节点 ID 治理、知识地形与缺口诊断、个性化学习路线、核心知识路径策展。
- **安全加固**（06-29）：6 维只读审计 → 32 项硬化（CSP、JSON-LD 转义、HSTS、innerHTML）；Mol\* 改自托管。
- **中文全文检索重建**（07-23 · `T-SEARCH-01`）：bigram 分词两层架构，标题中段 recall **9% → 100%**；并修复搜索 UI 因过期白名单静默丢弃 119 篇的 bug。
- **历史来源加强**（07-24）：100 个事件全部有恰当来源，单源 64→29；修复 8 个死引用键；人物页首次渲染参考文献。
- **产品边界清理**（07-24）：按 `聚焦方向.md` 删除书签 / 阅读进度 / 标签 / 分享；双主题经用户决定保留。
- **可访问性**（07-31 · `T-A11Y-03`，会话 #216）：重建为 36 个入口 × 双主题 × 双视口的 144 状态无豁免扫描；修复全局主题层叠、对比度、键盘/表单/SVG 语义、移动端溢出与重复导航后，serious/critical WCAG A/AA 违规为 0。
- **UX 与性能**（07-24）：正文字号统一 16px；cs / ps 补齐路由骨架屏；反链体系改 server 组件（详情页 First Load JS **153 → 113 kB**）并按域分组呈现跨域连接。
- **内容广度补强**（07-24）：社会学 + 语言学新增 8 篇（17,880+ 汉字），两域入链文章数 4→15、15→25。
- **医学薄内容收尾**（07-25 · `T-CONTENT-30`）：54 篇偏薄文章（50–74 → 全部 ≥110 非空行），覆盖疾病/人物/技术/概念/传统五类；事实全部 WebSearch 核实并修正 6 处原文硬伤；新增 wiki-link 零失效，搜索索引 2253 篇。
- **理科 + 计算机内容治理**（07-25 · `T-CONTENT-32/33/34/35/38`，会话 #194–#196；全文见 `archive/工作日志-2026-07-25-理科与计算机内容治理.md`）：
  - `T-CONTENT-32` 计算机 `algorithms/` **30/30** 深化至 3173–5190 汉字（每篇含手算走查表 + 工程现场数字 + 失败现场）；修正 4 处事实错误——Dijkstra 与所配图不一致的 trace 表、`pagerank` 的「细致平衡」（Google 矩阵不可逆）、`huffman-coding` 的「霍夫曼树即最优 BST」（两者不同问题）、`a-star` 的「更准启发必然扩展更少节点」（Holte 2010 已推翻）。
  - `T-CONTENT-33` 引用语义分离 **271 篇**：1389 条同行评审来源从 `## 延伸阅读` 归位到 `## 参考文献`，54 条科普留原处；7 域「学术源误归」全部归零。另为 **53 篇虚构对话体**补体裁声明。
  - `T-CONTENT-34` 宇宙学 **25 篇**补破除误解 + 现场（域内 12→31 篇）；**254 处**写成 inline-code 裸文件名的交叉引用转为真 wiki 链（65 文件），零链接文件 111→60。
  - `T-CONTENT-35` 数学：**39 条**模板化跨域 bullet 改写为实质内容、**10 条**不可核实的声称删除；`divergence-theorem` 结构去重。
  - `T-CONTENT-38` 修复 **157 处** wiki 引用（文章存在、引用写中文标题而文件用英文 slug）。
  - 化学 **25/25** 深化至 ≥2092 汉字；碎片化段落全平台 206→136（理科+计算机已清完）。
- **内容工作暴露并修好的 4 处工程缺陷**（07-25）：`check-content` 新增 slug 唯一性检查；frontier 篇幅判据补齐汉字替代；`lib/citations.ts` 移除 `引用`；`.prettierignore` 排除 `public/search-index.json`。
- **跨域连接节全域手写**（07-28 → 07-31 · `T-CONTENT-40`，会话 #202–#208）：16 域全部清零，全平台 2168/2278 达标（其余为 dialogues 体裁豁免 99 篇 + human-history 元文件口径 7 篇）。确立方法论：按主题族切组（非按目录）+ AgentSwarm/Workflow 并行编排 + brief 三件套（BRIEF_generic + 域 ADDENDUM + 组 ANGLES）+ 主会话独立复验（verify / 对拍 HEAD / 事实抽查）；`pnpm audit-cross-domain` 上线（达标口径：≥4 条 bullet、≥3 条粗体 `[[slug]]` 领头、≥400 汉字、无自链），取代一切临时 grep。教训沉淀：变体口径 ≠ 质量口径（philosophy 曾"有节 223 篇、达标 0"）；BSD grep `-o` 对多字节不可靠，度量一律用 python；删旧节前先分辨空句与实质内容（实质段落换名保留）；「被链条目作依据」必须真读那篇确认，不许假定；标题变体共四种（`跨域连接`/`跨领域连接`/`跨学科连接`/`跨领域关联`），后统一为 `跨域连接`。
- **三域额外深度轮**（07-31 · `T-CONTENT-42`，会话 #210–#212）：psychology / philosophy / computer-science 逐篇再加深一档 + 领域覆盖度审计补缺口（含 philosophy 分析哲学人物页 Putnam/Kripke/Searle/Nagel/Chalmers 等——`聚焦方向.md` 自 06-08 挂账的最后一处确认空白清零）。
- **碎片化段落收尾**（07-31 · `T-CONTENT-37`，会话 #213）：理科+计算机范围清完（206→136，余 136 全在范围外或属检测器误报）；检测器 `audit-fragmented-prose.mjs` 已修——排除紧跟公式/围栏代码/表格/列表的引导句，并并入 `check-content`。
- **社会学/语言学补广度**（07-31 · `T-CONTENT-31`，会话 #214）：+5 篇（robert-merton / talcott-parsons / socialization / discourse-analysis / origin-of-writing，各 3500–4500 汉字带来源清单）；确立 linguistics 新条目四层注册链（发布计划 → 计数矩阵 → 图谱节点 → 图谱边）。
- **同名 slug 与自链治理**（07-26 → 07-28 · `T-CONTENT-39` / `T-CONTENT-41`）：4 对撞名 slug 更名解歧；201 处文章自链（79 文件）清除，`check-content` 0 告警。余 2 对刻意保留，见「决策记录」第 4 条。
- **本地门禁与 CI Quality 对齐**（07-31 · `T-BUILD-01`，会话 #215）：`pnpm prepush` 八条确定性质量命令与 CI 对齐；确立推送前真实 Turbopack `pnpm build` + `bundle-check -- --skip-build` 规程。
- **待办清零与工程收尾**（08-01 · 会话 #217，本会话）：
  - `T-PERF-05`：大数据 barrel 静态守卫上线（`big-data-barrel.test.ts`）；human-history 全局 CSS 按路由拆分，路由 CSS 40.1（违规）→ 39.8 KB 回到预算内；三项贴顶预算的提额/瘦身决策落定（维持现状 + 触发条件，见「决策记录」第 1–2 条）。
  - `T-TEST-01`：图谱聚合计数快照化——`subjects/knowledge-graph/data/aggregate-snapshot.json` + `pnpm update-graph-snapshot`，8 个测试文件改断言「快照与实际一致」；`learning-targets` 筛选测试按新规范重写（不再假设具体 (域, 阶段, 置信度) 单元非空）；两个 e2e spec 的硬编码聚合文案改从 live builder 派生。反向验证（改节点层级 → 红 → update → 绿）通过。
  - `T-GRAPH-12`：化学 57→78/97（80.4%）、地球科学 54→70/82（85.4%）图谱锚点补足（+37 节点、+71 边，metadataGap 归零）；刻意不铺满，余量为次级专题。
  - `T-UX-04`：跨域可见性推进——通用领域引擎列表页（9 域 40 个列表页）新增 `CrossDomainBadge`（server 组件，领域色圆点 +「被 N 个领域引用」）；图谱节点详情面板标注跨域邻居与跨域边计数。
  - `T-TOUR-03`：本地路线档案统一导入/导出——新档案格式 `episteme-local-route-archive` v1（journeys + 双路线核对记录 + 服务实验快照三分节，兼容旧版 journey-only 档案）；服务实验结果首次获得本地持久化（快照存输入参数、确定性重算）；导入逐字段校验并与「已掌握」档案严格隔离（有测试断言）。
  - `T-HISTORY-04`：评估完成，结论不拆（见「决策记录」第 5 条）。
  - `T-CONTENT-36`：核实关闭——universe-physics 跨域节 158/158 达标（`T-CONTENT-40` 已覆盖，15 篇 dialogues 豁免）。
  - `T-DOC-01`：`聚焦方向.md` 篇幅口径统一为汉字数（见「决策记录」第 6 条）。
  - 已知问题清理：CS 五篇算法文章（floyd-warshall / bellman-ford / knapsack-problem / network-flow / dynamic-programming）全部手算表逐格重新演算，仅 network-flow 最小割枚举表漏 `{s,c}` 行（已补）；医学 12 篇近阈文章（75–79 → 全部 110 非空行、3168–4645 汉字，事实经 WebSearch 核实）；universe-physics 17 个死链清零；6 篇 human-history 编辑元文档从详情路由与搜索/wiki 索引排除（此前公开可达且已泄露进搜索索引）。
- **社会学/语言学扩面批次**（08-01 → 08-02 · `T-CONTENT-43`，会话 #218）：社会学 +3（anthony-giddens / james-coleman / michel-foucault-sociology）、语言学 +3（sign-language-research / lexicography / stylistics），两域仍为最薄但分别达 52 / 45 篇；全部事实经 WebSearch 核查（共修正 12 处，书目全部真实）；语言学新条目走完四层注册链（发布计划 → 计数矩阵 → 图谱节点 → 图谱边），社会学图谱 52/52 = 100%。
- **哲学同题文章合并**（08-02 · `T-CONTENT-44`，会话 #219）：决策记录第 4 条的预定解法落地——philosophy-of-mind（concepts → schools，合并后 6579 汉字）与 utilitarianism（isms → schools，8219 汉字）两对内容层合并；迁入材料逐条 WebSearch 核实（边沁图钉游戏引文出处、《奖赏原理》1825、威廉斯「吉姆与印第安人」原典名等），空泛与重复段落舍弃；slug 冲突消除，两个裸 wiki 链接恢复解析。
- **社会学/语言学扩面第二批**（08-02 · `T-CONTENT-45`，会话 #219）：社会学 +2（jurgen-habermas 4333 字 / norbert-elias 3748 字——核查发现哈贝马斯已于 2026-03-14 去世，按已故人物撰写）、语言学 +2（constructed-languages 3694 字 / speech-technology 3521 字）；两域达 54 / 47 篇，社会学图谱 54/54 = 100%，语言学四层注册链复用顺畅。
- **阅读体验与领域页面深化**（08-02 · `T-UX-05`，会话 #220）：阅读模式（标准/专注/宽松）从 1 个路由铺开至全部 60+ 文章页；新增页顶阅读进度条；移动端 TOC 从沉底 details 改为浮动按钮 + 底部抽屉；代码复制按钮触屏/键盘可见；脚注加回跳链接；修复 132 篇含公式文章缺 KaTeX 样式表的渲染缺陷（6 域 layout 引入，路由 CSS 预算按决策记录第 1 条预设触发提至 48 KB）；5 域补齐 loading/error/not-found、cs/ps 补 error/not-found（token 化组件，浅主题安全）；引擎列表页（7 域 40 个列表页）新增客户端搜索 + 分类筛选（server 预算 CrossDomainBadge 数据，不泄 server-only 索引），附 5 个组件测试。
- **渲染器收敛与排版细节**（08-02 · `T-UX-06`，会话 #221）：哲学 thinkers 页弃用弱渲染器（无表格/公式/脚注/wiki 链接——**117 篇正文 `[[slug]]` 全部以裸文本示人**），统一接入 MarkdownRenderer（wiki 链、表格、公式、脚注即刻生效），顺手修掉双 h1 与阅读时长口径；**顺带发现并处决一类预存断链 bug**：ThinkersList 卡片、ThinkerNav 上下篇、SchoolsList 卡片、school-detail 相关学派节共 4 处链接缺 `/philosophy` 前缀全部 404（哲学家/学派两个列表页的主链接全灭）；清理 3 处死代码（components/thinker-detail 旧解析器两件套、subjects/philosophy 的 thinker-detail 与 school-detail 死副本、life-science 重复 TOC）；species 正文行长 1200→900px；门户 STATS 从硬编码改为读 `generated/search-stats.json`（gen-search-index 同步产出），组件测试改断言快照而非字面量。
- **渲染完整性守卫 + 数学渲染器统一**（08-02 · `T-TEST-02` / `T-UX-07`，会话 #222）：新增 `lib/__tests__/render-integrity.test.ts` 两道静态守卫——(A) 全部 [slug] 文章页必须经共享 MarkdownRenderer（白名单例外须注明理由，防弱渲染器回归）；(B) 全部 tsx 的 href 字面量/模板首段对照 `buildValidRoutes` 真实路由表（防前缀断链），阴性对照（红→绿）通过。数学域弃用客户端 MathMarkdownRenderer（**149/169 篇裸 wiki 链**，同类 bug 第二现场），5 个 [slug] 页接入共享渲染器——公式改服务端渲染（~260KB 客户端 KaTeX chunk 归零，CSS 经 layout 引入 +2.6KB 在预算内），`performance-budget.test.mjs` 的 KaTeX 架构断言同步改写。
- **触屏预览与单 h1 大纲**（08-02 · `T-UX-08` / `T-UX-09`，会话 #223）：wiki 链接预览触屏化——粗指针设备首点显示预览卡（含「前往阅读 →」入口）、二点跳转、点外关闭（`hover: none` 检测，桌面悬停行为不变），附 2 个组件测试；MarkdownRenderer 全局裁定正文 h1——首块重复标题（~2000 篇）剥离、后续 `# `（~50 篇）降级 h2，全部文章页恢复单 h1 大纲（改造前逐一核实所有消费页都有页头 h1），附 3 个渲染器测试 + 1 个旧断言改写。
- **排版细节与引擎域视觉差异化**（08-02 · `T-UX-10` / `T-UX-11`，会话 #224）：正文启用 `text-autospace` 中西文自动间距（渐进增强零回归）；新增 `DomainHeroMotif`——7 个引擎领域首页各配专属 SVG 水印（化学苯环、语言学 IPA、社会学网络、计算机代码符、政治学天平、地球科学地层、医学心电），纯静态零动效、accent 着色，Playwright 截图 5 域 × 双主题人工复核。
- **打印样式与静态列表可交互化**（08-02 · `T-UX-12` / `T-UX-13`，会话 #225）：全站打印支持——token 重映射为纸面黑白（踩中 Tailwind v4 `@theme` 变量**未分层后置输出**的坑：分层 print 重映射压不过，改文件末尾未分层块 + `!important`），chrome 全隐（`print-hidden` 标记 + `header.sticky`/`aside` 选择器），标题/表格/代码分页规则，Playwright 打印模拟截图复验；economics economists + mathematics 四列表页（theorems/mathematicians/concepts/paradoxes）从纯静态升级为客户端搜索 + 分类 chips（共享 `ListSearchFilter` 控件，卡片视觉逐字保留），附 5 个组件测试。
- **学习主线预览条与内容第三批**（08-02 · `T-UX-14` / `T-CONTENT-46`，会话 #226）：引擎域首页新增 `DomainSpinePreview`——策展 L1→L5 学习主线五步卡（标题 + 引导问题 + 链接到节点文章），覆盖 7 个有 spine 的引擎域，截图复核；社会学 +2（welfare-state 4138 字 / zygmunt-bauman 3598 字，图谱 56/56 = 100%）、语言学 +2（reading-and-dyslexia 3474 字 / input-methods 3239 字，四层注册 49/49），两域达 56/49 篇；事实核查一如既往扎实（俾斯麦三法年份与出资比例、明快打字机专利号 US2613795、GB2312 字数构成、Bradley & Bryant 1983 卷期等均逐一核实）。
- **「最近更新」条与内容第四批**（08-02 · `T-UX-15` / `T-CONTENT-47`，会话 #227）：引擎域首页新增 `DomainRecentUpdates`——按 frontmatter `updated` 自动派生最新 4 篇（零策展成本）；顺带修复 gray-matter 把未加引号日期解析为 Date 导致 `updated` 一直取空串的隐性缺陷（文章侧栏同受其益）。社会学 +2（mark-granovetter 3652 字 / science-and-technology-studies 3158 字，图谱 58/58）、语言学 +2（speech-perception 3276 字 / llm-and-linguistic-theory 3303 字，51/51），两域达 58/51 篇；subject-candidates 的「30–50 篇」发布门禁按事实放宽为 30–60（原为立项期上限，语言学已长到 51）。
- **法学全量接入**（08-03 · `T-SUBJECT-LAW`，会话 #230–#231）：首个走完整立项路径的新学科（候选档案早已备好）——34 篇三波内容（launchGate 辖区/日期/免责声明字段齐备）、6 板块路由、门户入口、34 图谱节点 + 51 边、law-justice-spine、frontier/continuum 全集成；关键决策：图谱/frontier/spine 随第三波一次性启用（半成品接入必破 learning-continuum 审计）。
- **「被推翻的理论」第三批**（08-04 · `T-CONTENT-52`，会话 #238）：9 篇，全部投向系列中只写过一篇的域。生命自然发生说（**雷迪的纱布与巴斯德的鹅颈瓶是同一个实验思想隔两百年用在小两个数量级的对象上**，另含 1862 年评奖程序争议与普歇的芽孢）、地球开尔文年龄之争（**击中要害的批评来自他自己的学生佩里、来得很早、被忽略了**）、经济剑桥资本争论（**萨缪尔森 1966 年公开承认输掉逻辑，而教科书至今照用总量生产函数**）、语言学爱斯基摩雪词神话（引用链被逐环追溯：博厄斯的四个词根 → 沃尔夫 → 数字自我放大）、工程"稀释是解决污染的办法"（高烟囱把局地问题转成跨国酸雨，且当年正是为达标而建）、法学洛克纳时代（**各方都同意它错了，而对"错在哪"给出四种互相冲突的诊断**）、哲学感觉材料（塞拉斯的"所与的神话"）、人类历史"黑暗时代"分期（彼特拉克用拉丁文体判定一千年）、医学卫生假说的过度延伸（**名字本身在造成公共卫生危害**）。系列至此共 36 篇，覆盖全部 17 个域。

- **「被推翻的理论」第二批**（08-04 · `T-CONTENT-51`，会话 #237）：延续上一批的体裁，**优先补齐第一批未覆盖的四个域**（数学、哲学、艺术、人类历史）。12 篇：物理热质说（**卡诺用错误本体论推出正确定律**——错在本体、对在结构）、医学放血疗法与路易 1828 年的数值方法（临床统计的起点）、宇宙火神星（**勒维耶同一方法：海王星成功、火神星失败**）、化学聚合水与朗缪尔的病态科学清单、心理学教育神经神话（学习风格、莫扎特效应、左右脑）、数学第五公设两千年证明史（**萨凯里推出了非欧几何却不认识它**）、哲学心理主义（弗雷格与胡塞尔从两个方向给出同构论证）、艺术进步论、人类历史水利社会论（魏特夫因果链的时间顺序反了）、社会学世俗化理论（**伯格本人 1999 年公开撤回**）、CS 专家系统与第二次 AI 寒冬（与第一次寒冬的成因结构对照表）、政治学历史终结论（**福山本人用两卷本作出的修正**）。本批的贯穿线索是"作者撤回"与"同一方法两种结局"这两类罕见的科学诚实样本。图谱同步补 arts / sociology 两个 100% 底线域的节点。

- **「被推翻的理论」跨域批次**（08-04 · `T-CONTENT-50`，会话 #236）：一次刻意选题的体裁实验——**全平台此前没有一篇专门写"错误理论"的条目**，而这条矿脉横跨所有学科。15 篇覆盖 13 个域：化学活力论（含"维勒神话本身是神话"的史学修正）、物理以太、地球大陆漂移被拒五十年、医学瘴气说与溃疡菌（一对结构相反的案例）、生命拉马克主义、心理颅相学、宇宙稳恒态、经济马尔萨斯陷阱、CS 感知机与 AI 寒冬、工程塔科马大桥的教科书错误、语言学"原始语言"神话、社会学社会达尔文主义与优生学、政治学现代化理论、法学「分离但平等」。**统一的写法是"它当时有多合理 → 什么真正击穿它 → 代价 → 留下的判断力"**，并刻意在条目之间互相对照（大陆漂移的拒绝合理 vs 溃疡菌的拒绝拖沓；颅相学方法错而直觉对 vs 优生学方法对而前提错）。选题阶段主动排除了 4 个与既有文章重复的候选（燃素说撞 lavoisier-oxygen-revolution、菲利普斯曲线撞 stagflation-1970s、哥德尔撞既有悖论条目、萨丕尔-沃尔夫撞 language-thought-debate）。图谱同步补 law / engineering / sociology / linguistics 四个 100% 覆盖率底线域的节点与锚点边。

- **搜索索引瘦身与图谱回填**（08-04 · `T-PERF-06` / `T-GRAPH-13`，会话 #235）：两件卡住后续内容轮次的基础设施。**索引瘦身**——先量清成分（inverted index 461.8 KB 占 84%，其中「参考文献」「跨域连接」等结构性标题的 bigram 各自出现在 1000–2300 篇里），实测发现单纯做停用词只省 7 KB（brotli 对重复 posting 压缩极好），真正的杠杆是 heading 索引本身占 375 KB。改为**按文档频率剔除结构性标题 + 篇内去重 + 每篇 100 字 heading 预算**，让索引随文档数而非文档长度增长：**548.1 → 468.1 KB**（余量 12 KB → 92 KB，约够再写 400–500 篇），标题召回仍为 **100%**（2409/2409），并加了两条回归断言钉住这套规则。**图谱回填**——CS 120 篇、心理学 59 篇现有文章无节点，手写 179 条会在下一轮再次失效，改为沿用 `philosophy-nodes.ts` 先例新建 `mdx-derived-nodes.ts` 从 frontmatter 派生节点与边（新文章自动进图谱）；curated 节点优先，重复 id 与悬空边在 `graph-data.ts` 汇总时过滤。首次运行暴露 112 个孤儿节点（`related` 只连同板块同级条目），用**板块层级表 + 声明式前置锚点**一次性解决，取代此前五轮的手工补边。CS 覆盖率 33.0% → **100%**（179/179），心理学 → **99.5%**（218/219）。

> 已知内容问题：`psychology/knowledge-base/intelligence-theories` 与 `psychology/phenomena/intelligence-theories` 是同题不同稿的重复文章（3655 / 3280 汉字），slug 相同导致图谱节点只能指向其一（心理学覆盖率 218/219 的唯一缺口），wiki 链接亦有歧义。属编辑层面的合并决策，待内容轮次处理。

- **三域深度轮：哲学 / 心理学 / 计算机科学**（08-04 · `T-CONTENT-49`，会话 #234）：诊断先于写作——三域的文章深度并不缺（最短 2314 汉字），真正的缺口是 **L4–L5 板块系统性偏薄**（CS frontier 仅 5 篇、心理学 methods 仅 7 篇、哲学 frontier 仅 6 篇）与明确的宽度空白。据此写 21 篇（各域 7 篇，2863–3557 汉字）：**CS** 后量子密码 / RAG 与智能体 / 机密计算与 TEE / 神经形态与后 CMOS / 统计学习理论（PAC）/ 在线算法与竞争分析 / 近似最近邻搜索；**心理学** 心理物理学与信号检测论 / 经验取样 EMA / 神经影像方法及其限度 / 认知计算建模 / WEIRD 样本与可推广性 / 情绪普遍性之争 / 精神病理网络理论；**哲学** 神经伦理与心理隐私 / 形式认识论 / 气候正义与代际伦理 / 数字人格与心灵上传 / 认识论危机（证言与深度伪造）/ 道德运气 / 伊斯兰哲学。事实核实到 2025–2026 口径（NIST HQC 2025-03、TEE.fail DDR5 攻击 2025-10、UNESCO 神经技术建议书 2025-11、EU AI Act 情绪识别禁令、MICrONS 小鼠皮层立方毫米 2025）；**主动剔除无法核实的说法**（Loihi 3 仅见于内容农场稿、Intel 官方无发布，未写入）。**结构性修复**：哲学 frontier 板块 10 篇文章此前从未进入图谱（`philosophy-nodes.ts` 只读 schools/concepts/isms 三个 `.mdx` 板块），新建 `philosophy-frontier-nodes.ts` 全部接入；顺带补了 5 个 CS 既有文章的节点，CS 图谱覆盖率 27.3% → **33.0%**。锚点缺陷第五次出现（4 个节点前置被"严格低一级"过滤清空），按同一套办法修复。

- **工程与技术接入**（08-03 · `T-SUBJECT-ENG`，会话 #233）：36 篇内容全量上线（前序 27 篇 + 本轮补齐 9 篇：储能/航天/高铁/供排水/韧性基础设施/工程伦理/能源转型/超级工程/生物工程边界，3351–3804 汉字，数据全部 WebSearch 核实到 2025–2026 年口径）；36 图谱节点 + 62 边 + 10 跨域桥 + `engineering-building-spine`（简单机械→材料强度→摩天楼→韧性基础设施→超级工程）；continuum/coverage-meta/spine-atlas/frontier 升 18 域，graph-coverage engineering 36/36 = 100%。**顺带修掉三类既有缺陷**：① `SECTION_PREFIXES` 手抄清单在 law/arts 上线时未更新，导致三域同时渲染门户 chrome 与域内导航，且门户 header 在浅色主题下继承深色 `--color-fg-primary`（近白字压近白底，12 组 SERIOUS 对比度违规）——改为从 `APP_URLS` 派生并加回归测试；② a11y 扫描域清单漏掉 law/arts（正是漏检原因），已补三域；③ 工程域 category 三种写法混用会在板块页生成重复筛选 chip，已按板块标签统一，并修正 energy 板块 order 冲突。候选矩阵：engineering 标记上线，新增 **文学与叙事**（4.00 分）继任 RECOMMENDED——否则候选池清空会让 `audit-subject-candidates` 崩在 `RECOMMENDED_SUBJECT_CANDIDATE` 上。

- **艺术、建筑与美学接入**（08-03 · `T-SUBJECT-ARTS`，会话 #232）：图像管线首战——40 篇内容每篇配 1 张登记主图（Wikimedia 公有领域/CC0，逐件许可证核实 + 预算压缩），40/40 通过 audit-image-rights；40 图谱节点 + spine（观看→构图→形式分析→归属研究→生成艺术）；全球覆盖刻意避开纯西方正典（东亚书画/南亚宗教艺术/伊斯兰视觉文化/非洲/拉美/大洋洲）。候选矩阵同步：arts-aesthetics 标记上线，engineering 候选（3.8 分）继任 RECOMMENDED。
- **图像权利管线与化学/地球科学深度轮**（08-02 · `T-IMAGE-01` / `T-CONTENT-48`，会话 #228）：建成 T-SUBJECT-NEXT 前置管线——图像权利 schema（Zod 六类许可证）+ 构建期响应式生成（sharp 三档 WebP + manifest）+ 审计脚本（登记完整性/引用一致性/变体新鲜度/性能预算/外链热图禁令，并入 prepush）+ 文章内 `![alt](/images/<id>)` 响应式 figure 渲染；试点为林语堂明快打字机专利图（公有领域）挂入输入法文章；规范见 `docs/图像管线指南.md`。化学补全配位化学（螯合效应/均相催化两节）+ 新写电分析化学（3380 字），地球科学新写岩溶洞穴（3216 字）与冰期轨道周期（3231 字）；图谱 chemistry 81.6% / earth-science 85.7% 均升。
- **学科版图与导航架构**（08-03 · `T-INFO-01`，会话 #229）：22 域六簇分类法落地（设计见 `docs/学科版图与导航架构.md`）——DOMAINS 加 `cluster` 字段成为唯一真相源，新增 `lib/domain-clusters.ts` 派生层；桌面导航改 4 超组 + 探索下拉（两栏簇小节面板）、移动抽屉加簇组头、领域内 SubjectHeader 新增「全部领域」下拉（**此前领域页零跨领域入口**）、门户首页按 6 簇分区、页脚按簇分列。顺手修复三处预存缺陷：NAV_GROUPS 手工清单漏掉 sociology/linguistics（导航双端缺失数月）、DesktopNav/MobileNav 断点重叠（lg/xl 区间双导航）、页脚与 manifest 域清单漂移。
