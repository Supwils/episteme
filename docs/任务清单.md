# 任务清单 — Episteme · 格致

> 最后更新：2026-09-24（E5 完成，未 commit）
>
> 本文件只登记**在办与待办、决策记录、已知问题**。做过什么、怎么做的，见 `工作日志.md`（「演进」一节按时期摘要，逐次原文在 `archive/`）。

---

## 当前状态

- 计数一律读 `generated/site-stats.json`（`pnpm gen-site-stats`，经 `lib/site-stats.ts`），不在文档里手写每域篇数。2026-09-24：22 个学科、3166 篇文章、图谱 3565 节点。
- `pnpm dev` 默认 **localhost:3067**，生产模式 `pnpm start` 为 3000；验证用生产构建加 `NEXT_DIST_DIR=.next-prod`。push `main` 即经 GitHub Actions（Quality → Build → Deploy）上线生产。

---

## 🔵 在办 / 待办

### 前端设计改造「观测台与手记」

一次会话一个 epic。基线与分解见 `docs/设计审计-基线.md`，设计决定写入 `docs/设计方向-观测台与手记.md`。每个 epic 前后用 `node scripts/visual-baseline.mjs <label> --only=<路由,…>` 截图（产物在 gitignore 的 `visual-baseline/`）。

| 编号          | Epic                                                                                                           | 状态   |
| ------------- | -------------------------------------------------------------------------------------------------------------- | ------ |
| `T-DESIGN-00` | E0 视觉审计基线                                                                                                | 已完成 |
| `T-DESIGN-02` | E2 正确性速修                                                                                                  | 已完成 |
| `T-DESIGN-01` | E1 设计系统地基                                                                                                | 已完成 |
| `T-DESIGN-03` | E3 全局外壳                                                                                                    | 已完成 |
| `T-DESIGN-06` | E6 阅读体验                                                                                                    | 已完成 |
| `T-DESIGN-05` | E5 学科首页统一骨架（22 域全部迁入）                                                                           | 已完成 |
| `T-DESIGN-04` | E4 首页格致仪（送审稿 `docs/superpowers/specs/2026-09-24-E4-格致仪-线框与几何规格.md`，2026-09-24 批准并落地） | 已完成 |
| `T-DESIGN-07` | E7 格致山 3D（**动工前须用户审批原型、帧时、JSON 体积**）                                                      | 待开始 |
| `T-DESIGN-08` | E8 打磨与全量验证                                                                                              | 进行中 |

已知要进 E8 的：

- ~~自拼文章外壳并入 `ArticleLayout`~~：2026-09-24 完成 10 个（数学 5、生命科学 4、哲学学派 1）；`life-science/tree/[domain]` 是分类总览不是文章，保留原样。
- 引文挪进页边（先解决窄屏回退）。
- ~~人类历史令牌表纳入对比度测试~~：2026-09-24 完成，`palette.test.ts` 把半透明卡面先合成到底色再算。
- 视图过渡（卡片图版 → 学科首页图版）：首页卡片图版走图标集、学科首页是内联图版，先统一再做。

等用户决定：

- 柏拉图线段喻改用 `levels` 组件（06g，要动 `content/philosophy/thinkers/plato.mdx`）。
- 依赖本地阅读记录的功能（最近访问、继续阅读、已读标记、我的足迹、稍后读）触碰 `聚焦方向.md` 禁区；用户 2026-09-23 已说暂不做。「以此为起点规划路线」触碰冻结中的学习编排边界。

### 内容

- **去 slop（进行中）**：紧迫性三族套话已清零，守卫在 `scripts/cleanup-boilerplate.mjs`；经济学家、哲学家人物页的「比以往任何时候都」为 0，心理学人物页「为什么」节已对象化。下一寸：法学、艺术、哲学主义与问题页、心理现象页里模板化的「为什么今天仍然重要」；「比以往任何时候都」还散在约 19 篇。不要把它加进全局守卫，其余域仍有对象专用句。
- **对象剖析长程协议**：只接受真实对象剖析，不新写总览，不发明 B7。协议见 `docs/长程内容迭代协议.md`。#485 为最后一轮，不新开 #486。
- **缺稿（邻页已点名、尚无对象页，先登记）**：Fiat–Shamir 1986；ElGamal 1985（IEEE TIT 31(4) 469–472）；Shoup 2001 _OAEP Reconsidered_；Fujisaki–Okamoto–Pointcheval–Stern 2001；Bellare–Rogaway PSS；Canetti–Goldreich–Halevi 随机预言机不可实例化；Goldwasser–Kalai 2003；Berlin & Kay 1969 基本色词。
- **待作者裁定**：① frontmatter 的 `summary`（或 `dek`）填了才会显示为文章导语，目前全部为空。② 人物 `era`：奥古斯丁、商羯罗与罗摩奴阇、周敦颐 / 朱熹 / 王阳明 / 李滉仍标「古代」，待定分期口径。③ 名言时间线把阿维罗伊标「东方」，是否增加「伊斯兰」传统。④ 社会学、经济学的学习主线问题偏窄，做学科首页标题时像专题线；`DomainLanding` 支持 `question` 覆盖。

### 落库

- 工作树压着 2026-09-15 之后的全部改动，未 commit：内容 #476–#485、欢乐层、动效层 `T-MOTION-02`、审查修复、构建隔离、设计改造 E0–E6。用户说 commit 时按主题拆成几个 commit。

### 执行约束

- 不要重映射 HISTORY_TODAY 剩余时间线 dumps；不要把肯尼迪就任 / 遇刺改挂人物页；物理 / 宇宙学 3D 嵌套 `error.tsx` 保持白字深底；`curiosityArticleHref` 只认 ≥3 段路径；不做用户系统 / 进度 / 分享 / i18n。
- 审计通过只说明被审的部分通过：#248 的「308 篇离图」只覆盖 5 域，扩到全域后是 1062 篇。加新板块时先确认它在审计 scope 里。
- B3–B6（文学、宗教、人类学、教育学）均已上线。不要发明 B7；文学 38 / 宗教 36 保持立项规模。

---

## 📌 决策记录（已拍板，作为后续执行依据）

1. **性能预算：路由 CSS 已按预设触发提至 48 KB**（2026-08-02，`T-UX-05`）。40 KB 时代收官水位：共享首载 144.6/180 KB · 路由 CSS 39.8/40 KB · 搜索索引 515.5/560 KB brotli · 单块 179.4/285 KB。**触发依据**：六个领域（economics/medicine/chemistry/earth-science/computer-science/philosophy）的 layout 引入 katex.min.css（+~2.6 KB gzip）——132 篇含公式文章此前经 MarkdownRenderer 服务端渲染 KaTeX 却无样式表，公式呈无排版 HTML；备选方案（运行时注入样式表）会把预算红线换成每页公式 FOUC。提额后水位 42.4/48 KB。其余预算不变：索引超 90% → 提至 ~640 KB；**大数据 barrel 守卫**：`lib/__tests__/big-data-barrel.test.ts` 从 493 个 `"use client"` 模块做 import 闭包遍历（含动态 import），带阳性对照防 vacuous pass。
2. **Lighthouse TBT 门禁维持现状**（2026-08-01；2026-09-11 补确认次数）。`/knowledge-graph` 常态 219–255ms、预算 400ms；首页仍为 **250ms**，不因第 21 张领域卡提额。runner 间方差可达 3 倍属 CI 环境问题：宗教上线那次首页首 trace 亦为 performance 73 / TBT ~989ms，第二次确认才到 97 / 52ms；人类学 push 的第二次确认停在 85 / 477ms 仍超线。决策：不提预算、不改 workflow；脚本在首次超预算后再跑 **两次**确认（共最多 3 条 trace），吸收冷启动抖动。若失败率仍显著上升，再按 `T-PERF-05` 记录的 (b)/(c) 方案复议。
3. **dialogues 体裁豁免已统一**（2026-08-01）。虚构对话体一律豁免跨域连接节：economics 1 篇、cosmology 6 篇的旧式空句节已删除（两域标题变体归 1，达标数不变）；psychology 8 篇已按标准写达的保留（删除达标内容是破坏性回退）。三态问题消除。
4. **`T-CONTENT-39` 余 2 对同题文章：已于 2026-08-02 按预定解法完成内容层合并**（`T-CONTENT-44`，会话 #219）。philosophy-of-mind、utilitarianism 的 concepts/isms 份独家材料（内格尔蝙蝠、赖尔行为主义、玛丽的房间、现象概念策略、利贝特实验；西季威克、三大经典反驳、黑尔两层理论、EA 与气候折现等）已逐条核实后织入 schools 规范节点版（合并后 6579 / 8219 汉字），旧文件与 data 条目删除，3 处旧路由引用改为 wiki 链或改指 schools 路由。slug 冲突与 2 条 `check-content` 告警随之消除，`[[philosophy-of-mind]]`、`[[utilitarianism]]` 裸链接恢复解析（合并前因重复 slug 被索引整体剔除）。`PHILOSOPHY_CANONICAL_NODE_SOURCES` 映射保持不变（canonical 仍指向 schools 份）。
5. **`T-HISTORY-04` 结论：不拆**（2026-08-01）。事件/人物/时间线详情数据已按「时代 × 内容类型」分片到 `content/human-history/data/generated/` 并经 per-era 动态 import 按需加载；human-history 全部 19 页共享首载 180KB gzip 且历史数据为 0（预算 ≤200KB），`references.js`（40KB）的消费模式是按 key 跨时代解析，按时代拆只会打散查找表、给 4 个消费点增加合并逻辑。三个后续优化点登记在「已知问题」。
6. **篇幅衡量标准统一为汉字数主口径**（`T-DOC-01`，2026-08-01）：`聚焦方向.md` 已改写——MDX/KB ≥2200 汉字（分档见 `check-content.ts` 的 `MIN_CJK_CHARS`）或达到行数下限；frontier ≥1800 汉字或 ≥60 非空行。「双薄才告警」，`MIN_LINES` 保留为公式/表格密集型文章的替代通道。

7. **`T-RENDER-01` 软 404：已按路径 B 修复**（2026-08-05）。根因：`loading.tsx` 在段级创建 Suspense 边界，Next 先发出 `200`，之后 `notFound()` 改不了状态码。用户拍板移除动态 `[slug]` 祖先链上的 **117** 个 `loading.tsx`，保留 **43** 个无动态子段的重页面骨架。代价：文章页与领域/板块列表页失去客户端导航即时加载态。不可行：给高基数路由返回全部 slug 或关掉 `dynamicParams`（15000 文件上限）。

8. **知识图谱静态数据预算落线**（2026-08-06，`T-GRAPH-17`）：嵌套知识库入图后，`/knowledge-graph/graph-data` 为 **4.31 MB raw / 489.9 KB brotli**，`bundle-check` 新增 **5 MB raw / 600 KB brotli** 双预算。原始体积防止字段或正文意外泄漏，Brotli 体积约束真实传输成本；两条线分别保留约 17% / 18% 余量。提额仍须按既有规则记入本节，不能静默修改。
9. **搜索索引预算按预设触发提至 640 KB brotli**（2026-08-13，会话 #264）。**触发依据**：决策记录第 1 条预设「索引超 90% → 提至 ~640 KB」——五薄域四批内容后实测 **512.3/560 KB（91.5%）**，2801 文档。提额前复核过管线无轻易可挤的水分（T-PERF-06 已做 docfreq 停用词 + 篇内去重 + 100 字标题预算，标题召回 100% 有回归断言钉住）。

10. **human-history 数据层两条旧评估结论作废**（2026-08-11，`T-PERF-08` 勘查复核）：(a) `event-relationships.ts` 换 `EVENT_CATALOG` **不做**——它已不在任何客户端 chunk（唯一组件消费者 `subjects/history/components/EventTimeline.tsx` 是死代码，已删），省 gzip 的前提不成立，且换源会让 `gen-history-route-data.ts` 自引用自己生成的 catalog，威胁生成幂等；(b) `content/human-history/data/figures/` 桶（~651KB 源码）**保留**——它并非"仅测试引用"，而是 `gen-history-route-data.ts` 与搜索索引的构建期源数据，且不进任何客户端 bundle（`big-data-barrel.test.ts` 强制保证）。原评估第三条（/scholarly 按批动态化）已实施，见「已完成」。

11. **连续体 planner 载荷 175 KB → 185 KB**（2026-09-11，B6 试写）。`/api/knowledge-continuum/planner` 在第 22 域脊柱入图后实测 177.9 KB。只抬这一条测试上限，coverage/spine 预算不动。再超 185 KB 时先看 catalog 字段再决定是否拆分。

12. **每个首页学科必须有 frontier 体裁**（2026-09-12，#395）。`FRONTIER_DOMAINS` 排序后必须等于 `DOMAINS.map(d => d.id)`；另测每个 id 都有 `app/<id>/frontier/page.tsx` 与 `[slug]/page.tsx`。不合并 22 份薄路由文件（App Router 需要文件）。语言学/法学/教育学都曾因只建内容目录、不进这份清单而隐身。

13. **已上线学科的矩阵篇数上限 60 → 70**（2026-09-12，#414）。`releaseArticleCount` 原是立项闸门；语言学基础语料因语言记录伦理 + 文字释读到 61，精确核对测试要求矩阵数字跟着库存走。下限 30 与新学科 30–60 的立项指导不变；只把已上线学科的硬上限放到 70，避免用闸门冻结补教科书缺口。

14. **搜索索引预算因奇趣钩子独立入索引提至 800 KB brotli**（2026-09-18；2026-09-19 已执行副标题瘦身；2026-09-20 已去掉奇趣 80 字正文切片）。**触发依据**：把约 300 张奇趣卡从「并进同 URL 文章、钩子搜不到」改成独立检索命中后，实测 **717/640 KB**。不提额就得把欢乐层从搜索拿掉。确认增长来自标题 posting、不是正文泄漏。**已执行的瘦身**：MiniSearch 倒排只编 `doc.t`，不再把副标题「奇趣知识」编进 title；奇趣卡 `text` 留空，钩子仍靠标题召回。416 张钩子后实测 **666/800 KB**（83.3%）。**下一次触发条件**：再扩钩子若重回 720 KB 观察线，先截断过长标题；硬预算仍 800 KB，不要未写决策就提额。

15. **性能预算全面复核：按读者实际付出的字节重写规则**（2026-09-23，用户授权「不合理就按技术判断定新规则」）。逐项复核结论如下。
    - **测量口径改为 brotli**（CSS / HTML / RSC / JSON）。依据：生产环境对这四类响应全部返回 `content-encoding: br`，带 hash 的 chunk 为 `immutable` 缓存一年；按 gzip 计系统性高估约 25–30%。**JS 预算保留 gzip**：JS 的成本主要是解析与执行，gzip 是业界惯例，且现有 180 / 220 / 285 KB 三条线余量合理，不动。
    - **路由 CSS「每路由 ≤ 48 KB gzip」拆成三条**：全站共享表 ≤ 30 KB br、单路由增量 ≤ 10 KB br、单路由首访合计 ≤ 40 KB br。依据：47 KB 里 37.4 KB 是 429 个路由共用、首访后即缓存的同一张 Tailwind 表，领域增量只有约 9 KB。旧规则把「首访付一次的共享表」和「每页都付的增量」混在一条线里，任何门户样式都会挤爆所有路由。新规则让增长去对的地方：页面专属样式放到组件旁的样式表（`@reference` 全局入口），而不是塞进全局表。重复编译 Tailwind（+25 KB）仍会立刻被「增量 10 KB」拦住。
    - **优化同时落地**：门户专属样式（首屏、簇、卡片、今日、精选）移到 `components/home/home.css`，引擎域首页样式移到 `components/domain/domain-home.css`。全局表 27.9 → 24.9 KB br，每个非首页路由首访少 3 KB；最大路由合计 36.0 → 33.0 KB br（43.3 KB gzip）。首页、`/daily`、法学文章逐像素比对无差异。法学首页板块计数标签原来的 `font-mono` 在全局表里从未赢得层叠，读者一直看到的是正文字体；迁移后按线上实际外观去掉，行高回到 22 px。
    - **首页「HTML raw 500 KB / gzip 80 KB」与「RSC raw 95 KB」改为「首页文档 ≤ 40 KB br」与「首页 RSC ≤ 20 KB br」**。依据：95 KB raw 的 RSC 实际只传 12.4 KB br，这条线在守的是可压缩的重复文本；它却会直接挡住 E4 在服务端渲染的星盘 SVG。主线程代价（水合的元素数）由 Lighthouse TBT 守，不由原始字节守。现值：文档 21.0 KB br、RSC 12.4 KB br。
    - **图谱数据 600 KB br 拆为「首绘 ≤ 350 KB br + 延后描述 ≤ 400 KB br」，原始合计 5 MB 不变**（原始字节在主线程 `JSON.parse`，这条仍有意义）。落地的优化：wire v3 把节点描述（占 52%）移到 `/knowledge-graph/graph-descriptions`，图谱画出后再取；全图力导向布局改在构建期计算，随数据下发（布局是确定性的，浏览器里跑要约 2 秒）；`<link rel="preload" as="fetch">` 让数据与 JS 并行下载。首绘数据 575 → 286 KB br。
    - **Lighthouse 门禁的两处测量缺陷已修**。① `next start` 不压缩 force-static 路由处理器的输出，图谱数据在本地以 3 MB 明文传输，本地 LCP 18.6 秒，而线上实测 1.24 秒。`scripts/lighthouse.mjs` 现在在前面挂一个模拟 CDN 的 brotli 代理（`scripts/performance/compressing-proxy.mjs`，先热身一次，使用与线上同等级的 q11 压缩），`LH_NO_CDN=1` 可以绕过。② 图谱路由改用 devtools 实测节流：它的首绘依赖页面自己发起的请求，Lantern 模拟对此不敏感，四次优化把实测 LCP 从 5.7 秒压到 4.2 秒，模拟值始终在 5.73 秒 ±5 ms。其余四条路由仍用模拟节流（CI 方差更小）。修后 5 条路由全部通过：图谱性能分 84、LCP 4226 ms。
    - **维持不变且判定合理**：共享 JS 180 KB gzip（现 144.7）；通用文章 JS 220（166.2）；单 chunk 285；3D 路由 180；搜索索引 800 KB br（671，懒加载进 Worker，只有搜索者付费）；历史时间线两条；图像两条；Lighthouse 各路由阈值。
    - **下一次触发条件**：共享 CSS 超过 27 KB br 时，先把新增的页面专属样式移出全局表；首页 RSC 超过 16 KB br 时，检查是否把数据而非标记塞进了 RSC；图谱首绘数据超过 320 KB br 时，先看节点字段（`tags` 59 KB、`label` 39 KB 为次大项）。任何提额仍须写入本节。

16. **设计系统地基的三个取舍**（2026-09-23，`T-DESIGN-01`，细节见 `docs/设计方向-观测台与手记.md`）。① **新增开发依赖 `opentype.js`**（只在 `pnpm gen-seal-glyphs` 里用，零运行时体积）：学科印的 22 个字从 Noto Serif SC Black（OFL）提取为 SVG 轮廓提交进仓库，不向读者下载任何字体；15 MB 源字体按 SHA-256 校验，缓存在 gitignore 的 `.cache/fonts/`，因此这一步不进 `gen-all`（CI 无需下载），由测试拦住缺字形的印。② **展示网页字体推迟到 E4**：实测 217 个固定展示字的子集就有 48 KB woff2，没有页面真正用上之前不让全站付这笔。③ **领域强调色收成六簇颜料**：`DOMAINS` 的四个颜色字段改由 `domainAccent(cluster)` 派生，暗底从中性近黑改为夜墨 `#0f1622`。各域 `--color-accent-gold` 文字色留给 E5 逐域按 4.5:1 重调。

17. **首页格致仪的三个取舍**（2026-09-24，`T-DESIGN-04`，细节见 `docs/设计方向-观测台与手记.md` 第十二节）。① **学科印与标本图版走 SVG 图标集**：两个 force-static 路由 `/sprites/seals.svg`、`/sprites/plates.svg` 在构建期生成，页面用 `<use>` 引用，缓存 1 小时。不进 `gen-all`、不提交产物；代价是图标集里的印只能用朱文。② **环序改为方案 B**：哲学挨数学、工程挨物理，艺术改挨宗教；格致山沿用同一环序。③ **不引入展示网页字体**。④ **首页 RSC 触发线复核**：E4 后首页 RSC 16.7 KB br，越过第 15 条的 16 KB 检查线。查过：书架传给客户端的 25 本书目录约 2.9 KB br，其余是星盘 SVG 标记；其他学科的读数已移到 `/api/astrolabe`，不在 RSC 里。预算 20 KB 不变，下一次检查线提到 18 KB。⑤ **知识图谱 Lighthouse 预算按 CI 重新定线**：E2 起图谱路由改用 devtools 真实 4 倍降速、并经压缩代理测量，但只在本地校准过；首次上 CI（run 36079706511）测得性能 47–50、LCP 5.5–5.8 s、TBT 1.28–1.62 s（本地 84 / 4.2 s / 49 ms）。这不是本批代码回退：以前数据未压缩、晚于测量窗口到达，图谱挂载根本没被测到。预算暂定性能 ≥ 40、LCP ≤ 6.5 s、TBT ≤ 2 s；图谱挂载拆分后收回 80 / 4.5 s / 400 ms。本地 `predeploy` 测不出这条路由的 CI 读数。

---

## 🟢 可选扩展（P3，无排期）

国际化（英文）· 用户系统 · human-history Canvas→React 迁移 · 更多非洲 / 拉美 / 日本历史事件

---

## 已知问题

### #313 终审后的独立评估项

- **依赖复审（2026-09-05 已出回执，不宣称清零）**：`pnpm audit` 全树 42 条（1 low / 14 moderate / 25 high / 2 critical）；`--prod` 19 条 advisory。其中 **9 条 Next CVE 只出现在锁文件幽灵 importer `apps/portal`（next 15.5.18）**，根应用已是 **15.5.25**，GitHub checkout 不含 `apps/`。本机残留的未跟踪 `apps/portal/node_modules` 已删；**不要手工剪裁 lockfile importer**，等下一次自然依赖 bump 再让 pnpm 重生。根 importer 仍报但不可直接当公网洞：PostCSS 8.4.31 由 Next 内嵌、处理的是我们自己的 Tailwind CSS（不是用户上传 CSS / sourceMappingURL）；sharp 0.34.5 的 libvips 公告针对**非受信输入**，本站只转码策展 `content-assets`；browserslist / nanoid 在 Next 构建链，无用户可控 query/size；fflate 经 `@react-three/drei`，仓库内无 `unzipSync`。dev 树 critical 为 happy-dom / vitest UI，不进生产。未加全局 override。
- **低端设备性能画像**：标题搜索优先走 `public/search.worker.js`（esbuild IIFE，避开 Turbopack 在 Worker 里读 `document.currentScript`）。Worker 404/报错才回落到主线程 MiniSearch；主线程路径的 `warmup()` 仍用 `requestIdleCallback({ timeout: 200 })`。这不等于消除低端机长任务。图谱 TBT 预算维持 400ms，不靠提预算解决。桌面实验室 Event Timing：门户搜索 Chromium 64ms、哲学人物 16–32ms、仪式实验室 WebKit 16ms；Lighthouse 导航模式 INP 为 n/a。手机 CrUX / 真机低端画像仍未测。
- **覆盖边界**：`a11y-scan` 现含哲学人物 `/philosophy/thinkers/socrates` 与物理学对话（域文章即 `/universe-physics/dialogues/bohr-heisenberg`），以及四域全部教学实验室。全部 `generateMetadata` 详情页走 `engineArticleMetadata` / `frontierArticleMetadata` / `withCanonicalPath`。2026-09-11 生产扫描 **66 页 × 双主题 × 双视口** serious/critical 为 0。WebKit/Safari 冒烟 8/8（桌面+iPhone 13）。本机 Lighthouse `/knowledge-graph` LCP 仍偶发 ~16s（确认轨迹仍失败，与既有 CI 方差同类，不提预算）。

| 问题                                                                                                    | 影响   | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 知识图谱 e2e 有 4 条在 HEAD 上即失败                                                                    | 低     | 2026-09-25 用 HEAD 构建对照确认，不在 CI 冒烟内：`knowledge-graph-tour-url` 的空间旋转用例（期望值写于学科更少时），以及 `knowledge-graph-mature-content-tours` 三条手机端用例——路线恢复后详情面板标题在手机上不显示。                                                                                                                                                                                                                                                                                                                                                                                                   |
| 知识图谱挂载在慢 CPU 上阻塞 1.3–1.6 s                                                                   | 中     | CI 上 devtools 4 倍降速测得（决策记录第 17 条 ⑤）。2026-09-25 剖析（本机 12 倍降速）：小地图原为 3565 个 `<div>`、每次平移缩放整批重渲染，已改画布；边标签表改为首次查询时构建。剩余热点：`KnowledgeGraph` 挂载时的渲染与全图 memo（学习前沿快照约 70 ms）、渲染数据构建约 90 ms（边过滤占 60 ms）、首帧绘制约 100 ms。试过无效、已撤回：`startTransition`（重活在 effect 里）、前沿闭包记忆化（输出逐项一致但不提速）、前沿快照挪到空闲（仍是独立长任务）。本机同一构建 5 次测得 TBT 587–1926 ms，噪声太大，以 CI 读数为准。真正要降，需要把渲染数据构建与首帧绘制拆到多帧或 Worker；做完收回预算 80 / 4.5 s / 400 ms。 |
| **代码审查 2026-09-19 余项**（全文见 `archive/代码审查-2026-09-19-逻辑与性能.md`；①③④⑤a11y ⑥⑦⑧⑨⑩ 已修） | 低     | ② 搜索每键清空结果与防抖窗口吞 Enter——**已拍板行为**（#313「换词不误导航」，有单测钉住），不改；⑤ `WikiLinkPreview` 每链接 `matchMedia`+setState（测试按调用 mock，模块级共享会破坏隔离，收益小，不改）；⑧b OG `runtime=edge` 保留（渲染契约计数）；`/random` 保留在 sitemap（既有测试与决策）。图谱 Worker 仍以 `terminate()` 作硬取消（结果带请求 id 兜底），Barnes-Hut 深度上限静默丢体与 `forces.ts` 模块级 scratch 仅文档化。                                                                                                                                                                                       |
| e2e `knowledge-graph-tour-url` 旋转用例过期                                                             | 低     | 期望 CS 正面角 -54°，是 7 月学科更少时写的；22 域下 `rotationForSpatialDomain` 为 -8°，HEAD 上同样失败，CI 只跑冒烟所以未暴露。待 E4/E7 定下学科环顺序后按新顺序重算期望值。                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `/knowledge-graph` CI Lighthouse TBT 余量薄                                                             | **中** | 预算 400ms；CI 慢 runner 曾到 362–800ms，本地生产约 60ms。接受偶发重跑，**不得靠提预算解决**。LCP 偶发 16–18 秒的根因已查明为本地不压缩（决策记录第 15 条），已修。                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Mol\* 探测噪声                                                                                          | 低     | `localhost:9000` 探测属噪声；RCSB 真实结构需生产浏览器 + 网络冒烟。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| human-history Canvas 非 React                                                                           | 低     | PageWrapper + 原生 JS，功能正常。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `reference/` 旧源码                                                                                     | 无     | 已从 tsconfig / Vitest / `.vercelignore` 排除。**禁止自行删除。**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

---
