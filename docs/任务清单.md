# 任务清单 — Episteme · 格致

> 最后更新：2026-09-11（会话 #381：宗教学实验室加厚 + 文章「动手看」；候选仍未 launched）
> 架构：仓库根目录单一 Next.js 15 应用 + `content/`（内容）+ `subjects/`（领域前端）
>
> **本文件只登记「在办与待办」。** 已完成任务按阶段浓缩在文末；逐次细节见 `工作日志.md`，更早的全文见 `archive/`。

---

## 当前状态

**20 个知识领域** · **2746 篇内容** · 343+ 页面路由 + 14 个 route handler · 1378 单测（175 文件） · 知识图谱 3131 节点 · 搜索索引 2905 文档（2732 文章 + 173 实体）

| 领域       | 篇数 | 领域       | 篇数 | 领域           | 篇数 |
| ---------- | ---- | ---------- | ---- | -------------- | ---- |
| 哲学       | 358  | 政治学     | 181  | 医学与公共卫生 | 140  |
| 心理学     | 236  | 数学       | 174  | 化学           | 106  |
| 经济学     | 211  | 人类历史   | 176  | 地球科学       | 94   |
| 计算机     | 213  | 宇宙学     | 168  | 社会学         | 66   |
| 宇宙物理   | 177  | 生命科学   | 140  | 语言学         | 65   |
| 艺术       | 58   | 工程与技术 | 54   | 法学           | 57   |
| 文学与叙事 | 36   | 宗教学     | 36   |                |      |

> 篇数为 `content/` 下 `.md`/`.mdx` 实测（排除 `*.narration.md` 与 `CREDITS.md`）。搜索索引 2905 文档 = 2732 文章 + 173 实体；文件篇数 2746 与索引 2732 之差含人类历史编辑元文档等非检索内容。哲学 359 → 355 为四对跨板块同题合并（`T-CONTENT-64`）。6 篇 human-history 编辑元文档已从搜索/wiki 与详情路由排除（#217）。

`pnpm dev` → **localhost:3067**（生产模式 `pnpm start` 为 3000）。部署经 GitHub Actions（Quality → Build → Vercel Deploy 三级门禁），push `main` 即触发生产部署。

---

## 🔵 在办 / 待办

### #313 增量质量循环已收口

`T-HARDEN-01`–`25` 均已完成；逐项复现、改动和门禁回执见 `工作日志.md` 会话 #313。最终为 **1337 tests / 165 files、14 条生产冒烟、266 静态页构建**，渲染契约与包体预算全绿。没有提交或部署。

当前已审范围内未再确认可安全闭合的高价值小缺陷。2026-09-05 已关闭远端 Cursor PR #5–#9（不合并）；依赖可达复审已取得在线回执，见「已知问题」。低端设备性能画像和跨浏览器覆盖仍待测，不把评估项虚标为完成。

`T-VIZ-ENG` / `T-VIZ-ARTS` 已于 2026-09-05 交付（工程 4 个、艺术 5 个教学实验室）。文学 4 个教学实验室已于 2026-09-06 挂上 `/literature` 首页。手记主题对比度已于会话 #323–#324 收口（共享 chrome、图谱 HTML、历史图谱工具条、医学实验室链接、经济/数学图轴标）。在办栏暂无编码任务；剩余评估项见「已知问题」（真机 INP、Safari、全站 axe）。

`T-POLISH-01`–`16`、`T-READ-01`–`09`、`T-CONTENT-40`–`74` 均已完成。`T-POLISH-16` 十篇织入（医学、政治、心理、化学、数学、生命、地球、艺术），合计约 22,311 → 44,220 汉字。会话 #328 将火电、控制、日常设计、简单机械、《宫娥》、行为艺术、工业 4.0、文学入门拉到 ≥3800。会话 #329 新织入动画、电影、公共艺术、工业工程、增材、材料强度、电机、水电八篇，均 ≥3800。会话 #330 织入艺术进步论、纺织、供应链、电网、公共领域、平台社会、劳动组织、官僚制、城市化、摩天楼十篇，均 3800–4800。会话 #331 按 `content-humanize-chinese` 做全库分诊：助手口吻 0；空收束「具有重要意义」类约 176 处。第一波改心理/哲学/经济 12 篇。会话 #332 第二波再改 12 篇（韩非子、王阳明、蒯因、罗素、卢梭、洛克、存在主义、克莱因、华生、阿施、谢林、洛夫特斯）。#328–#330 已织核心文与术语判定为已经像人，未磨。会话 #333 收人文化余篇并织法学/语言学八篇。会话 #334 盘点余量后织社会学十篇 + 工程机器人/航空，均 ≥3800。会话 #335–#367 继续六薄域织入；#368 把六域非前沿 MDX 305 篇全部收到 3800–4800。会话 #369 在六薄域之外按最薄优先织入成熟大域 **135** 篇（9 波 × 15）；#370 再织 **135** 篇（第 10–18 波）；#371 再织 **75** 篇（第 19–23 波）；#372 再织 **90** 篇（第 24–29 波）；#373 再织 **60** 篇（第 30–33 波）；#374 再织 **30** 篇（第 34–35 波）；#375 再织 **120** 篇（第 36–43 波）；#376 再织 **37** 篇（第 44–46 波）；#377 再织 **109** 篇（对话体 98 + 敏感 11），均 3800–4800；六薄域未回退。**六薄域外非前沿 3800 织入口径已清零。**

**余量口径（3800 织入目标，不是 check-content 的 2200）**：人文化「具有重要意义」≥2 已清零。六薄域非前沿 MDX（语言学 / 艺术 / 法学 / 工程 / 社会学 / 文学，305 篇）仍全部在 **3800–4800**。语言学基础审计深度区间现为 3802–4790。六薄域之外非前沿（排除 `frontier/`、旁白、CREDITS、人类历史 6 份编辑元文档）<3800 现为 **0/2256**。对话体与先前跳过的敏感篇均已按制度/伦理史织入（克隆无操作步骤、反恐无战术、医学无处方、二战写结构不写战役手册）。六域 `frontier/` 研究前沿仍按 1800 下限。文学候选已 launched；B4 宗教学已上线但**未**标 launched（否则推荐器空指针，除非同时写入 B5）。首页 RSC 84.1 / 95 KB。标题搜索走 `public/search.worker.js`。下一优先：阅读体验轮（真机 INP / Safari / 全站 axe），或按教科书目录把宗教从 36 篇再补缺口；**不要**把 `religion-studies` 标 launched，除非同时写入 B5。

**滚动计划（2026-08-16 拍板）**：每轮新增/深化 **2–3 万字（约 8–12 篇）**，内容轮与阅读体验轮交替，一轮一验收。织入打磨目标 **3800–4800 汉字**（`[\u4e00-\u9fff]`，YAML 后），一段 2–4 句、散文段 ≥40 汉字。

**执行约束（仍有效）**：不要重映射 HISTORY_TODAY 剩余时间线 dumps；不要把肯尼迪就任/遇刺改挂人物页；物理/宇宙学 3D 嵌套 `error.tsx` 保持白字深底；`curiosityArticleHref` 只认 ≥3 段路径；不要做用户系统 / 进度 / 分享 / i18n。

> **#248 / #249 教训（保留）**：当时最大缺口不是缺文章，而是已有文章没进图谱。#248 的「308 篇离图」只覆盖审计 scope 内 5 域；扩到 18 域后深度一层离图 **573**，再加嵌套知识库共 **1062**。**审计 scope 本身就是盲区**，"审计通过"只说明被审的部分通过了。图谱派生现已全域 100%（哲学曾短暂 98%）。

**B3 文学与叙事**：launchGate 见 `docs/文学与叙事立项口径.md`。路由 `/literature` 已上线；三十六篇 published（跨域节 36/36，图谱 36/36）。四个教学实验室挂首页。`literature-narrative` **已写入** `LAUNCHED_SUBJECT_CANDIDATE_IDS`。未改文学正文或实验室。

**B4 宗教学**：launchGate 见 `docs/宗教学立项口径.md`。路由 `/religion` 已上线；**36 篇 published**（30 篇板块 MDX 均 3800–4800，跨域节 30/30 中位约 590；6 篇 `frontier/` ≥1800，2020s 开放问题）。图谱 religion 36/36。四实验室已加厚（仪式案例、示意地图注解、经典开篇三镜、皮尤口径图）并在文章末加「动手看」。候选 `religion-studies` 仍是 `RECOMMENDED_SUBJECT_CANDIDATE`，**不要**标 launched，除非同时写入 B5。

---

## 📌 决策记录（已拍板，作为后续执行依据）

1. **性能预算：路由 CSS 已按预设触发提至 48 KB**（2026-08-02，`T-UX-05`）。40 KB 时代收官水位：共享首载 144.6/180 KB · 路由 CSS 39.8/40 KB · 搜索索引 515.5/560 KB brotli · 单块 179.4/285 KB。**触发依据**：六个领域（economics/medicine/chemistry/earth-science/computer-science/philosophy）的 layout 引入 katex.min.css（+~2.6 KB gzip）——132 篇含公式文章此前经 MarkdownRenderer 服务端渲染 KaTeX 却无样式表，公式呈无排版 HTML；备选方案（运行时注入样式表）会把预算红线换成每页公式 FOUC。提额后水位 42.4/48 KB。其余预算不变：索引超 90% → 提至 ~640 KB；**大数据 barrel 守卫**：`lib/__tests__/big-data-barrel.test.ts` 从 493 个 `"use client"` 模块做 import 闭包遍历（含动态 import），带阳性对照防 vacuous pass。
2. **Lighthouse TBT 门禁维持现状**（2026-08-01）。`/knowledge-graph` 常态 219–255ms、预算 400ms；runner 间方差可达 3 倍属 CI 环境问题，脚本的 trace 确认机制已能过滤单次抖动。决策：接受偶发重跑，不提预算、不改 workflow。若日后失败率显著上升，再按 `T-PERF-05` 记录的 (b)/(c) 方案复议。
3. **dialogues 体裁豁免已统一**（2026-08-01）。虚构对话体一律豁免跨域连接节：economics 1 篇、cosmology 6 篇的旧式空句节已删除（两域标题变体归 1，达标数不变）；psychology 8 篇已按标准写达的保留（删除达标内容是破坏性回退）。三态问题消除。
4. **`T-CONTENT-39` 余 2 对同题文章：已于 2026-08-02 按预定解法完成内容层合并**（`T-CONTENT-44`，会话 #219）。philosophy-of-mind、utilitarianism 的 concepts/isms 份独家材料（内格尔蝙蝠、赖尔行为主义、玛丽的房间、现象概念策略、利贝特实验；西季威克、三大经典反驳、黑尔两层理论、EA 与气候折现等）已逐条核实后织入 schools 规范节点版（合并后 6579 / 8219 汉字），旧文件与 data 条目删除，3 处旧路由引用改为 wiki 链或改指 schools 路由。slug 冲突与 2 条 `check-content` 告警随之消除，`[[philosophy-of-mind]]`、`[[utilitarianism]]` 裸链接恢复解析（合并前因重复 slug 被索引整体剔除）。`PHILOSOPHY_CANONICAL_NODE_SOURCES` 映射保持不变（canonical 仍指向 schools 份）。
5. **`T-HISTORY-04` 结论：不拆**（2026-08-01）。事件/人物/时间线详情数据已按「时代 × 内容类型」分片到 `content/human-history/data/generated/` 并经 per-era 动态 import 按需加载；human-history 全部 19 页共享首载 180KB gzip 且历史数据为 0（预算 ≤200KB），`references.js`（40KB）的消费模式是按 key 跨时代解析，按时代拆只会打散查找表、给 4 个消费点增加合并逻辑。三个后续优化点登记在「已知问题」。
6. **篇幅衡量标准统一为汉字数主口径**（`T-DOC-01`，2026-08-01）：`聚焦方向.md` 已改写——MDX/KB ≥2200 汉字（分档见 `check-content.ts` 的 `MIN_CJK_CHARS`）或达到行数下限；frontier ≥1800 汉字或 ≥60 非空行。「双薄才告警」，`MIN_LINES` 保留为公式/表格密集型文章的替代通道。

---

7. **`T-RENDER-01` 软 404：已按路径 B 修复**（2026-08-05）。根因：`loading.tsx` 在段级创建 Suspense 边界，Next 先发出 `200`，之后 `notFound()` 改不了状态码。用户拍板移除动态 `[slug]` 祖先链上的 **117** 个 `loading.tsx`，保留 **43** 个无动态子段的重页面骨架。代价：文章页与领域/板块列表页失去客户端导航即时加载态。不可行：给高基数路由返回全部 slug 或关掉 `dynamicParams`（15000 文件上限）。

8. **知识图谱静态数据预算落线**（2026-08-06，`T-GRAPH-17`）：嵌套知识库入图后，`/knowledge-graph/graph-data` 为 **4.31 MB raw / 489.9 KB brotli**，`bundle-check` 新增 **5 MB raw / 600 KB brotli** 双预算。原始体积防止字段或正文意外泄漏，Brotli 体积约束真实传输成本；两条线分别保留约 17% / 18% 余量。提额仍须按既有规则记入本节，不能静默修改。
9. **搜索索引预算按预设触发提至 640 KB brotli**（2026-08-13，会话 #264）。**触发依据**：决策记录第 1 条预设「索引超 90% → 提至 ~640 KB」——五薄域四批内容后实测 **512.3/560 KB（91.5%）**，2801 文档。提额前复核过管线无轻易可挤的水分（T-PERF-06 已做 docfreq 停用词 + 篇内去重 + 100 字标题预算，标题召回 100% 有回归断言钉住）。**下一次触发条件**：索引超 640 KB 的 90%（≈576 KB）时，先重新分析成分再决定是否提额或瘦身。

10. **human-history 数据层两条旧评估结论作废**（2026-08-11，`T-PERF-08` 勘查复核）：(a) `event-relationships.ts` 换 `EVENT_CATALOG` **不做**——它已不在任何客户端 chunk（唯一组件消费者 `subjects/history/components/EventTimeline.tsx` 是死代码，已删），省 gzip 的前提不成立，且换源会让 `gen-history-route-data.ts` 自引用自己生成的 catalog，威胁生成幂等；(b) `content/human-history/data/figures/` 桶（~651KB 源码）**保留**——它并非"仅测试引用"，而是 `gen-history-route-data.ts` 与搜索索引的构建期源数据，且不进任何客户端 bundle（`big-data-barrel.test.ts` 强制保证）。原评估第三条（/scholarly 按批动态化）已实施，见「已完成」。

---

## 🟢 可选扩展（P3，无排期）

国际化（英文）· 用户系统 · human-history Canvas→React 迁移 · Physics 3D 场景补全 · 更多非洲/拉美/日本历史事件 · 社会/语言学继续扩面（当前 66 / 65 篇）

---

## 已知问题

### #313 终审后的独立评估项

- **依赖复审（2026-09-05 已出回执，不宣称清零）**：`pnpm audit` 全树 42 条（1 low / 14 moderate / 25 high / 2 critical）；`--prod` 19 条 advisory。其中 **9 条 Next CVE 只出现在锁文件幽灵 importer `apps/portal`（next 15.5.18）**，根应用已是 **15.5.25**，GitHub checkout 不含 `apps/`。本机残留的未跟踪 `apps/portal/node_modules` 已删；**不要手工剪裁 lockfile importer**，等下一次自然依赖 bump 再让 pnpm 重生。根 importer 仍报但不可直接当公网洞：PostCSS 8.4.31 由 Next 内嵌、处理的是我们自己的 Tailwind CSS（不是用户上传 CSS / sourceMappingURL）；sharp 0.34.5 的 libvips 公告针对**非受信输入**，本站只转码策展 `content-assets`；browserslist / nanoid 在 Next 构建链，无用户可控 query/size；fflate 经 `@react-three/drei`，仓库内无 `unzipSync`。dev 树 critical 为 happy-dom / vitest UI，不进生产。未加全局 override。
- **低端设备性能画像**：搜索索引仍在主线程首次解析（Worker 被 Turbopack 的 `document.currentScript` 挡住）。`warmup()` 改为 `requestIdleCallback({ timeout: 200 })`，打开搜索的按键/点击不再直接扛 JSON.parse；`search()` 会取消空闲等待。这只前移并让出交互栈，**不等于**消除长任务。图谱 TBT 预算维持 400ms，不靠提预算解决。真机 INP 仍待测。
- **覆盖边界**：`a11y-scan` 已纳入文学首页/文章、宗教首页/文章、电网潮流、透视实验室、仪式结构、世界宗教地图、经典开篇比较器与世俗化指标。全量入口 × 双主题 × 双视口、以及真机 Safari / iOS，本轮未宣称通过。全站 canonical 与重复 URL 应另做路由清点。

| 问题                                        | 影响   | 说明                                                                                           |
| ------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------- |
| `/knowledge-graph` CI Lighthouse TBT 余量薄 | **中** | 预算 400ms；CI 慢 runner 曾到 362–800ms，本地生产约 24ms。接受偶发重跑，**不得靠提预算解决**。 |
| 审计脚本会误判旁白稿                        | 低     | `content/computer-science/` 下 6 个 `*.narration.md` 须从篇幅审计排除。                        |
| Mol\* 探测噪声                              | 低     | `localhost:9000` 探测属噪声；RCSB 真实结构需生产浏览器 + 网络冒烟。                            |
| human-history Canvas 非 React               | 低     | PageWrapper + 原生 JS，功能正常。                                                              |
| `reference/` 旧源码                         | 无     | 已从 tsconfig / Vitest / `.vercelignore` 排除。**禁止自行删除。**                              |

---

## ✅ 已完成（按阶段浓缩）

> 共 160+ 项。逐次路径与验证命令见 `工作日志.md` 与 `archive/`，此处只留阶段结论。

### 2026-09 增量加固（会话 #313）

- **输入与依赖边界**：搜索 limit、重复参数 500、枚举原型键与 level 类型校验；Next / MDX / ESLint 更新至 15.5.25，gray-matter 的 js-yaml 定向使用 3.15.2。
- **弱网与生命周期**：Service Worker 存储失败隔离、SWR 后台任务生命周期与不阻塞响应；预览失效结果、离开/卸载清理及失败重试；阅读存储与剪贴板异常降级。
- **搜索交互**：组合输入、关闭防抖、控件键盘事件、双层 loading、取消请求、组合键新标签、原生弹窗焦点及换词不误导航。
- **SEO 与验收**：首页/哲学家 canonical；无脚本正文、减少动态效果与原生导航进入生产 smoke。25 个独立事项全部有回执；120 个新增单测，最终 1337 tests / 165 files，14 条桌面/移动冒烟，构建与预算全绿。

### 2026-08 滚动计划与发现层

- **叙事打磨 `T-POLISH-01`–`15`**（08-18 → 08-27）：既有核心文织入至 3800–4800 汉字，不新建。R1–R7 各 12 篇，R8 收口，R10–R15 各 10 篇。R15 覆盖生命、化学、数学、心理、计算机与哲学，十篇合计净增 20,367 汉字；累计纠错写入工作日志里程碑（seL4 / 费马卒年 / 抗生素单位 / 伪引语 / 分解非 NP 完全 / Casgevy / HGP 成本 / AlphaProof 口径等）。
- **阅读体验 `T-READ-01`–`09`**：行长收敛（标准 ~44 字/行）；阅读三件套铺到自定义骨架页；参考文献装置性样式；跨域面板扩到 18 域；link-previews 按域分片；DOI 自动链接；`/daily` 浅主题 token 化；搜索/奇趣/随机死胡同关闭。诊断全文见 `archive/阅读体验诊断-T-READ-01.md` 与 `T-READ-06.md`。
- **滚动反馈 `T-PERF-09`**（#308）：全局导航、回到顶部、文章阅读进度与目录共用单一被动 scroll + rAF 快照；纯装饰进度条改用 compositor `scaleX()`，移除逐帧 React render 与 width 布局更新。
- **开发缓存隔离 `T-DEV-01`**（#309）：生产保留离线 Service Worker；开发态主动撤销同源遗留注册，受旧 Worker 控制时只刷新一次，避免缓存的 Turbopack chunk 覆盖新代码。
- **首页语气 `T-COPY-01`**（#310）：首屏从“探索人类知识的边界”收敛为“从问题出发”；正文、功能卡片、SEO、Open Graph 与 Twitter 摘要统一改为具体、克制的产品表达。
- **首页动效 `T-MOTION-01`**（#312）：首页低于首屏的标题与卡片改为 GSAP ScrollTrigger 进入视口时揭示；桌面首屏增加轻量滚动视差，移动端保持原生静态首屏。GSAP/ScrollTrigger 在水合后空闲时动态加载，减少动态效果、无 JavaScript 与搜索抓取均保留完整可见正文；未引入 Lenis，继续使用原生滚动。
- **深化轮 `T-CONTENT-69`–`74`**：五轮约 +45 篇 / 12.7 万字（心理·生命·CS → 化学·数学·哲学 → 生命·地球·医学 → 化学·宇宙学 → 政治·经济·心理），加系统剖析板块与 error/物种路由。
- **五薄域课程矩阵 `T-CONTENT-66`–`68`**：法学 / 工程 / 艺术 / 语言学 / 社会学按课程标准补主节点。
- **图谱 `T-GRAPH-16/17`**：18 域文章自动派生入图，覆盖率底线 100%；嵌套知识库入图；`derived-node-taxonomy.ts` 为唯一注册表。静态载荷双预算 5 MB raw / 600 KB brotli。
- **软 404 `T-RENDER-01`**：见决策记录第 7 条。
- **发布候选 `T-RELEASE-01`**（#263）与 **#306 生产部署**：`4242d452` + `fb3b5a6f`；CI 含 `/random` Location 头中文编码修复。

### 2026-08 上旬（领域接入与体裁）

- **法学 / 艺术 / 工程全量接入**（`T-SUBJECT-LAW/ARTS/ENG`）；图像权利管线首战（艺术 40 图）。
- **被推翻的理论** 三批共 36 篇，覆盖 18 域。统一写法：当时有多合理 → 什么击穿它 → 代价 → 留下的判断力。
- **系统剖析** CS `systems/` 板块 24 篇开源系统深读；互链消灭孤岛。
- **哲学四对同题合并**（`T-CONTENT-64`）与 **心理学 intelligence 合并**（`T-CONTENT-53`）：裸 wiki slug 从歧义丢弃恢复解析。
- **四域 frontier +24**、**五域剖析 +25**、**薄板块 +17**。
- **首页 RSC 标记收敛**（`T-PERF-07`）：104.5 → 85.4 KB，门禁收至 95 KB。
- **搜索索引瘦身**（`T-PERF-06`）：结构性标题 docfreq + 篇内去重 + 100 字 heading 预算；标题召回 100%。
- **六簇导航**（`T-INFO-01`）：`DOMAINS.cluster` 为唯一真相源。

### 2026-07 收口

- **跨域连接节全域手写**（`T-CONTENT-40`）：达标口径 ≥4 条、≥3 粗体 `[[slug]]`、≥400 汉字、无自链；`pnpm audit-cross-domain` 上线。
- **中文全文检索重建**（`T-SEARCH-01`）：bigram 两层，标题中段 recall 9% → 100%。
- **可访问性**（`T-A11Y-03`）：36 入口 × 双主题 × 双视口，serious/critical 为 0。
- **理科 + 计算机治理**（`T-CONTENT-32`–`38`）：算法手算走查、引用语义分离、模板化跨域改写。全文见 `archive/工作日志-2026-07-25-理科与计算机内容治理.md`。
- **产品边界清理**：按 `聚焦方向.md` 删除书签 / 阅读进度 / 标签 / 分享。

### 奠基（2026-06）

- 单一 Next.js 15 应用；20 个知识领域；`vercel deploy --prebuilt --prod --archive`（复盘见 `复盘-从一次Vercel部署翻车到CICD最优解.md`）。
- 知识图谱认知层与学习编排；安全加固 32 项；讲解（TTS）接入。
